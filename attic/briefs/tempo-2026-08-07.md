# Tempo-Analyse 2026-08-07 — wo die Minuten hingehen, und welcher Hebel sie zurückgibt

Slot 7 („⏱ tempo", lose Session, read-only + diese Datei). Owner-Auftrag wörtlich:
„guckt ob es nicht irgendwas an unserer aktuellen output rate / arbeits geschwindigkeit
aendern koennen. Ueberleg gut wie das am besten anzugehen ist."

Output-Rate = gelandete, verifizierte Arbeit pro Stunde Wanduhr. Erst gemessen, dann gerankt.
Quellen: `lane-outcomes.jsonl` (140 Zeilen), `post-land-audits.jsonl` (73),
`audit-adjudications.jsonl` (18), `e2e-trail/` (198 isolated-Läufe von 1005 Läufen gesamt),
`fleet.json` (146 Tasks), `git log`, `./state.sh` / `./register.sh` vom 2026-08-07 nachmittags.
Alle Rechenwege stehen bei den Zahlen; VERIFIZIERT und ABGELEITET sind markiert.

## 1. Das gemessene Zeitbudget

**Land-Kadenz (VERIFIZIERT, `lane-outcomes.jsonl` Feld `ts`, `disposition=landed`):**
08-06: 17 Lands über 15,6 h. 08-07 (bis ~16:30): 20 Lands über 14,9 h.
Inter-Land-Gaps 08-07 (min): 31 120 12 23 54 22 **267** 11 60 10 52 62 12 29 11 11 16 52 42.
Zwei Regime: In Bursts takten Lands bei **10–13 min** (8 der 19 Gaps ≤ 12 min) — das ist
exakt Audit (~9,4 min) + Gate (~1,5 min). Dazwischen Löcher von 52–267 min.

**Mutex-Belegung am 08-07 (VERIFIZIERT, drei Quellen addiert):**

| Verbraucher | Läufe | Minuten | Quelle + Rechenweg |
|---|---|---|---|
| isolated (Vorschauen + Beweisläufe) | 32 | **294** | Haupt-Trail `e2e-trail/isolated-*.jsonl`, Dauer = letzte−erste `ts` je Run |
| Post-Land-Audits | 20 | **188** | `post-land-audits.jsonl` Feld `ms`, Zeilen mit `at` am 08-07 |
| Gate-Suiten (claude-gate/clean-review/security) | 45/90/45 | **47** | Haupt-Trail, gleiche Rechnung |
| **Summe** | | **~529 min ≈ 8,8 h** | über 14,9 h Land-Spanne ≈ **59 % Mutex-Belegung** |

(Audits schreiben in den TMPDIR-Trail, nicht in den Haupt-Trail — keine Doppelzählung;
`docs/e2e-trail.md` §3. Alle Lane-Läufe landen dagegen im EINEN Haupt-Trail, weil der
common-dir eines Worktrees das Haupt-Checkout ist — ebd.)

**Audit-Dauer (VERIFIZIERT):** p50 8,6 min, p90 9,7 min, max 11,7 min, Summe 10,0 h über 73 Audits.
**Gate-Arbeit (ZITIERT aus `./state.sh`, `ca514eb`):** p50 78 s, p90 108 s, max 498 s (n=31);
Gate-WARTEN p50 0 s, 27/31 warteten null.

**Session-Zeit (VERIFIZIERT, mit Vorsicht — `sessionMs` enthält Idle/Warten):**
landed n=109: p50 73 min, p90 507 min, Summe 285,7 h. killed-empty n=14 mit ms: Summe 34,0 h.
killed-dirty n=6: Summe 21,6 h.

## 2. Die vier Verdächtigen — bestätigt oder verworfen

**a) Serieller Land-Takt — BESTÄTIGT, aber anders als gedacht.** Das Gate-WARTEN p50 0 s
zeigt: die Schlange vor dem Mutex ist NICHT das Problem — die Doktrin verhindert sie, indem
Sessions freiwillig ~12 min warten. Der Beweis steckt in den Gaps: 8 von 19 Gaps am 08-07
liegen bei 10–13 min = Audit+Gate-Dauer. **In Bursts IST die Audit-Dauer die Land-Kadenz.**
Und: der Sammel-Kollaps existiert bereits im Code und feuerte NIE — `server.ts:5094-5128`
(`drainPostLandAudits`): Lands, die während eines laufenden Audits ankommen, werden in
GENAU EINEN Folgelauf kollabiert, dessen `covers` alle nennt. Gemessen: `covers`-Histogramm
über alle 73 Audits = **{1: 73}** — die Batching-Maschinerie hatte nie die Chance, weil die
Doktrin Lands so weit auseinanderzieht, dass nie eines während eines Audits ankommt.
Die zweite Begründung der Doktrin (Verify stirbt in der Schlange, 469 s Lock-Warten am
08-06) ist seit `08dc17a` (gelandet 08-07 04:11) repariert: Warten und Arbeiten sind
getrennte Budgets, `waitedOut` ist nie `ok:false`.

**b) Spawn-Verschwendung — BESTÄTIGT, aber klein und schon adressiert.** 22 killed-empty +
6 killed-dirty = 20 % der Spawns. Muster (VERIFIZIERT, Branch-Namen + `ts`): (i) ein
5er-Batch vom 08-06 22:23 (`260806222332-ef99` … `260806223x-dc28`, je 11–20 min, alle mit
Brief, alle leer gestorben) — die Scout-Zeilen-Falle, die die Owner-Vorgabe vom 08-07
(„clarify first vor Dispatch") seither doktrinär schließt; (ii) zwei Zombies à 413 min
(`260806231635-0040`, `-b1c7`) und Einzelfälle bis 1145 min, die niemand bemerkte — dafür
ist `b210681` (Rückkanal: fertige Lane ruft zurück, gelandet 08-07 13:51) der Fix.
Kein neuer Hebel nötig; unter der Schnittlinie.

**c) Owner-Latenz — BESTÄTIGT als größter Tages-Skala-Faktor, aber kein Technik-Hebel.**
70 offene Zeilen: 32 `ready`, 26 `needs-you`, 12 ohne Analyse (`fleet.json`, Verdicts).
Zeilen sind jung (p50 15,7 h, 66/70 < 24 h) — die Queue ist kein Friedhof, sie churnt.
Aber: die Tageslücken zwischen Lands (52–267 min) sind Produktions- UND Entscheidungslatenz,
und 26 Zeilen warten explizit auf den Owner. Die Maschine landete 20/Tag; die reine
Mutex-Kapazität läge bei ~80 (Rechnung: 14,9 h / 11 min). Die Maschinen-Seite davon ist
heute gelandet (b210681, c72fd14); der Rest ist Owner-Zeit, nicht Code.

**d) Beweis-Kosten — BESTÄTIGT, konkret beziffert.** 56 von 198 isolated-Läufen rot (28 %).
89 von 198 Läufen sind aufeinanderfolgende Same-Tree-Wiederholungen (Re-Runs/Beweise).
Letzte 2 Tage: 66 Läufe, davon ~28 Wiederholungsläufe ≈ **130–155 min/Tag**; rot-getriggert
davon ~5 Reds/Tag × ~9 min ≈ **45–90 min/Tag**. Alle 11 Vorschau-Roten der letzten 2 Tage
liefen beim Same-Tree-Rerun grün. ABER (Ehrlichkeits-Fußnote): das EINE echte Rot von heute
(Audit 13:41, Baum `b210681`, adjudiziert `real`) hatte die Vorschau mit 1 Fail gezeigt —
und der Same-Tree-Rerun sprach es FREI (13:42 rot → 13:53 grün). Der Rerun-Beweis hat also
heute einmal falsch freigesprochen; erst die zwei identischen seriellen Läufe des Audits
stellten `real` fest. Von 18 roten Audits gesamt: 2 real, 5 flake, 2 stale-test, 9 unknowable
(davon 8 Backfill vor der signal-first-Retention). Familie sechs wird gerade geheilt
(Zeile `32c89530`, sent, Slot 1).

## 3. Rangliste — mit Schnittlinie

**A. Die 12-min-Takt-Doktrin streichen: Lands back-to-back, der Drain kollabiert die Audits.**
Gemessen: ~60–70 min/Tag Wanduhr in Bursts (8 doktrin-getaktete Gaps × ~8 min Differenz
11→3 min) **plus** ~50–75 min/Tag Mutex durch kollabierte Audits (bei 8 Burst-Paaren
fallen ~5–8 der 9,4-min-Audits zu Sammelläufen zusammen). Kosten: **null Code** — die
Maschinerie existiert (`server.ts:5094-5128`), das Gate ist seit `08dc17a` schlangenfest.
Es ist eine CLAUDE.md-Doktrin-Zeile („Der Land-Takt ist die härteste Grenze … Takt ~12 min"),
deren beide Begründungen von der Maschine überholt wurden. Risiko: ein rotes Sammel-Audit
nennt N Lands statt 1 (Bisect nötig, `undo-land` deckt nur das neueste) — bei Basisrate
2/18 echte Rote tragbar; und die Gate-WAIT-Statistik wird ehrlich schlechter (Budget 15 min,
nicht mehr tödlich).

**B. Die Tier-2-Vorschau aus der Lane-Verify-Zeile nehmen** (nur noch bei `e2eTouched`
oder auf ausdrückliche Anforderung). Die CLAUDE.md-Verify-Zeile endet heute auf
`&& ./e2e-isolated.sh` — jede Lane fährt den 9-min-Lauf, obwohl dieselbe Doktrin ihn
„verzichtbar" nennt und das Audit denselben Lauf nach dem Land ohnehin fährt. Gemessen:
~14–18 Erstläufe/Tag ≈ **165 min/Tag Mutex**; Ertrag der letzten 2 Tage: 0 echte Funde in
der Vorschau (11 Rote, alle Flakes; den echten Regress fand das Audit). Einschränkung auf
e2eTouched-Lanes (22/37 = 59 % der jüngsten Lands) spart ~**60–70 min/Tag**, Komplett-Aus
~165 min/Tag. Kosten: eine Doktrin-Zeile. Risiko: ein realer Bruch fährt bis zum Audit
(~9 min nach Land) statt vorher aufzufallen.

**C. Zeile `6b9f77d0` priorisieren — die Trail-Abfrage** („der Beweis, der heute sieben
Minuten kostet", status ready). Ersetzt den Same-Tree-Beweislauf durch eine Query über die
1005 protokollierten Läufe („ist Check X auf Bäumen ohne meine Änderung schon gefallen?").
Gemessen: rot-getriggerte Re-Runs ≈ **45–90 min/Tag** Mutex + Session-Aufmerksamkeit; dazu
der heutige Fehl-Freispruch (§2d) als Qualitätsargument — die Query sieht n Instanzen, der
Einzel-Rerun sieht eine Münze. Kosten: Zeile existiert bereits gebrieft; nur starten.
Risiko: gering, read-only-Feature.

**D. Der Tages-Skala-Befund gehört dem Owner, nicht der Technik:** 26 needs-you-Zeilen und
32 ready-Zeilen stehen vor einer Maschine, die in Bursts 5×/h landen kann. Die größten
Löcher im Tag (120/154/267 min) sind keine Suite-Minuten. Kein Bau-Vorschlag — nur die
Zahl: **Entscheidungsdurchsatz ist ab jetzt der Deckel, nicht die Maschine**, sobald A–C
stehen. (Maschinen-Seite dafür heute gelandet: `b210681`, `c72fd14`.)

---- SCHNITTLINIE — darunter nichts mehr vorschlagen ----

Genannt, aber bewusst NICHT über der Linie: Spawn-Verschwendung (§2b — Doktrin + Rückkanal
existieren seit heute); Suite-Parallelität (laut `docs/work-register-2026-08-06.md` §7
ausdrücklich „ungeprüft, nicht beerdigt" — bräuchte erst die Messung nach den Suite-Hebeln);
`dabd1880` (Analyse-Sweep-Konvergenz — hat seine eigene Zeile); `FLEET_DISPATCH_MAX_LANES`
2→3 (vor A+B erzeugt es nur Mutex-Schlange). Beerdigtes (§7: Auto-Rollback, rerere/hartes
Gate, Hook-Pflege, Stop-Hooks, K2-Richter, Eval-Gate) wurde nicht wieder aufgemacht.
Bereits gelandete Tempo-Hebel, nur zitiert: `d122e29` (56,8 s aus isolated, 20 s aus
security), `08dc17a` (Warte-/Arbeitsbudget), `b210681` (Rückkanal), Zeile `62302c47` done,
Zeile `00e5f771` done.

## 4. Nicht geprüft

- `sessionMs`-Semantik: enthält Idle/Owner-Wartezeit; als reine Arbeitszeit NICHT lesbar.
  Eine Lane-interne Zeitverteilung (Brief→Commit→Done→Land je Schritt) trägt das
  Outcome-Ledger nicht — jede Zeile hat EINEN `ts` + `sessionMs`, mehr nicht.
- Der TMPDIR-Audit-Trail (per-Check-Zeilen der Audits) — nur das Ledger-`ms` gelesen.
- Gate-Zahlen (p50 78 s, n=31) aus `./state.sh` zitiert, nicht selbst aus den Land-Notes
  nachgerechnet.
- Ob `d122e29`s Tick-Schnitt in den gemessenen ~9 min schon wirkt (Läufe nach 03:39 am
  08-07 weiter ~9–11 min; Wirkung nicht isoliert).
- `releasedBy`: 102/110 absent — Feld erst seit `0e345c6` (08-07), Verteilung nicht belastbar.
- Die 2 shelved Lanes; die watchonly-Suite; steward-arena-Läufe.
- Ob die 12-min-Doktrin außer den zwei gemessenen Begründungen (Audit-Serialität,
  Verify-Timeout) eine dritte, undokumentierte trägt — im Zweifel gilt Owner-Wort vor
  dieser Analyse.
