# Welle 1 — Act-Briefs zur Owner-Sichtung (Program `eeba7c04…`, ACP)

Status: **VORLAGE, kein Builder gestartet.** Der Program-Gate lautet „Vor Builder-Start werden
die Act-Briefs dem Owner vorgelegt" — dies ist diese Vorlage. Untracked wie die uebrigen
Analyse-Artefakte des Tages.

Baum: HEAD `c684e89`. Alles unten am HEAD nachgeprueft, nicht aus dem Handoff uebernommen.

## 0. Zwei Korrekturen am Schnitt der Vorgaengerin — VOR der Freigabe zu entscheiden

**(K1) `ACP-17` ist KEINE Lane. Sein Write-Set ist gitignored.**
Der Handoff sagt „Fragment in `rulebook.ts`". Gemessen:

- Die sieben Fragmente liegen in `rulebook/*.md`, nicht in `rulebook.ts` (`ls rulebook/`;
  `rulebook.ts:5` ist die Render-Zeile, `:66` `renderRulebook`).
- `.gitignore:40` ignoriert `rulebook/`, `.gitignore:36` ignoriert `CLAUDE.md`.
  `git ls-files rulebook/` ist LEER, `git check-ignore -v rulebook/einstieg.md` bestaetigt es.
- `server.ts:3930` kopiert `rulebook/` beim Lane-Spawn als Snapshot — genau wie `CLAUDE.md`.

Folge: eine Lane sieht ihre Aenderung nie in `git status`, und sie stirbt mit dem Worktree.
Das ist dieselbe Klasse wie die bereits im Regelbuch stehende `CLAUDE.md`-Falle.
**Damit sind zwei der drei Welle-1-Acts host-seitig, nicht lane-faehig** (ACP-14 war schon so
markiert, ACP-17 nicht).

**(K2) Welle 1 ist damit nicht „drei parallele Lanes", sondern:**

| Act | Write-Set | Traeger |
|---|---|---|
| ACP-10 | `src/protocol.ts` + `e2e/prompts.ts` (getrackt) | **echte Lane** |
| ACP-17 | `rulebook/*.md` (gitignored) | Haupt-Checkout — Lane nur als Patch-Text-Produzent |
| ACP-14 | `~/.claude/**` (ausserhalb des Repos) | **Owner-Akt** |

Empfehlung: ACP-10 als Lane briefen und starten. ACP-17 direkt im Haupt-Checkout ausfuehren
(vier belegte Faktkorrekturen, kein Produktcode, kein Gate deckt die Datei) — dafuer brauche
ich eine Owner-Zeile, weil der Program Brief der MAIN Produktcode untersagt und die Grenze
„Regelbuch-Fragment ist kein Produktcode" eine Auslegung ist, keine Messung. ACP-14 gehoert
ohnehin dem Owner.

---

## 1. ACP-10 — der ctx-Nenner der Bruecke

```yaml
schema: fleet.act/v1
trace: { programId: eeba7c04caae64d79969199b, actId: ACP-10, parentActId: null, attemptId: 1 }
role: { principal: specialist, mode: builder }
goal:
  importance: >
    Ein pi-Slot auf claude-bridge/claude-* publiziert heute ctx: null. Der 25/30-Kontextentscheid
    (Handoff §1(2)) ist eine MESSUNG, kein Gefuehl — ein Slot ohne Nenner kann ihn nicht einhalten
    und faellt auf Selbstauskunft zurueck.
  observableOutcome: >
    Ein laufender pi-Slot mit --model 'claude-bridge/claude-opus-5' zeigt auf GET /api/sessions
    ein ctx-Objekt mit windowTokens ungleich null, und der pct-Wert liegt innerhalb ±2 Punkten
    der Selbstauskunft im pi-Footer derselben Pane.
  successCriterion: >
    contextWindowFor("claude-bridge/claude-opus-5") liefert den Wert, den die Pane misst;
    bun e2e/pins.ts ALL PASS; die neue Zeile ist in e2e/prompts.ts gepinnt.
  nonGoals:
    - Kein Aufweiten von MODEL_RE. claude-bridge/* bleibt fuer den claude-Harness ein
      ABGELEHNTES Modell (e2e/security.ts:517 und :1000 pinnen genau das) — contextWindowFor
      ist ein Nenner, kein Validator, und darf diese Grenze nicht beruehren.
    - Kein Anfassen von server.ts.
    - Kein 200k-Fallback fuer irgendeinen Namen.
subject:
  repo: /Users/owner/claude-fleet
  baseHead: c684e89
  objects:
    - src/protocol.ts:171-183   # contextWindowFor
    - src/protocol.ts:140-171   # CLAUDE_CONTEXT_WINDOWS, inkl. des fable-Praezedenzfalls
    - e2e/prompts.ts:977-1035   # die Pin-Familie „context-fill DENOMINATOR"
  inputArtifacts:
    - docs/harvest-portabilitaet-J-2026-08-21.md:80-82,219   # die Live-Messung
authority:
  mode: mutate
  writeSet: [src/protocol.ts, e2e/prompts.ts]
  forbidden: [server.ts, src/client.ts, rulebook/, CLAUDE.md]
  externalActions: none
context:
  requiredAnchors:
    - AGENTS.md § Portable operating contract
    - AGENTS.md § Verify
  decisionRefs:
    - Handoff 2026-08-21 §1(2) — 25/30 ist die Kontextregel, die den Nenner braucht
  omitted:
    - Der Rest des Programs. Dieser Act braucht ihn nicht.
```

**Die EINE Frage, die der Builder entscheiden muss, und sie ist nicht trivial.**
Die naive Fassung — Provider-Praefix abschneiden, dann in die Tabelle sehen — ist gemessen
FALSCH: `claude-opus-5` steht dort auf `CONTEXT_WINDOW_BASE` (200 000), waehrend der
pi-Footer derselben Bruecke `13.0%/1.0M` meldet
(`docs/harvest-portabilitaet-J-2026-08-21.md:219`). Ein Praefix-Strip publiziert also
zuverlaessig einen um Faktor fuenf falschen Prozentsatz — exakt der Fehler, den die
`claude-fable-5`-Zeile 2026-08-19 schon einmal bezahlt hat (`e2e/prompts.ts:1000-1013`).

Zulaessig sind genau zwei Formen, und die Wahl gehoert begruendet in den Report:
1. **Benannte Zeile** wie beim fable-Praezedenzfall: `claude-bridge/claude-opus-5` explizit mit
   datierter Messung und dem Satz „wird die Bruecke umgehaengt, ist diese Zeile falsch".
2. **Weiterhin `null`**, falls die Messung die 1M nicht traegt — dann ist der Act ein
   Nachweis, dass ACP-10 nicht baubar ist, und das ist ein gueltiges Ergebnis.

Ein stiller Fallback auf 200k ist in beiden Faellen verboten; die Pin-Zeile
„a claude id the table does not name is null, never the 200k base window"
(`e2e/prompts.ts:1017`) ist die Doktrin und bleibt gruen.

```yaml
capabilityRequirements: { tools: [read, edit, bash], modalities: [text], runtime: bun }
routingHypothesis:
  harness: claude
  model: claude-sonnet-5[1m]
  reason: >
    Zwei getrackte Dateien, ein enger Schnitt, die Entscheidung ist im Brief vorgedacht.
    Hypothese, kein Qualitaetsbeweis.
  fallback: claude-opus-5[1m], falls der erste Versuch die Nenner-Frage falsch aufloest.
proof:
  - command: bun e2e/pins.ts
    expect: ALL PASS
  - command: >
      bunx tsc --noEmit --strict --target esnext --module esnext --moduleResolution bundler
      --types bun e2e/pins.ts src/client.ts src/share.ts server.ts fleet-e2e.ts
      fleet-e2e-claude-gate.ts fleet-e2e-clean-review.ts fleet-e2e-security.ts
      fleet-e2e-postland-audit.ts fleet-e2e-harness.ts merge-prompt.ts
    expect: exit 0
  - command: ./e2e-security.sh
    expect: ALL PASS   # haelt die MODEL_RE-Grenze, die dieser Act nicht verschieben darf
  - note: >
      Die volle Kette zuerst ueber GET /api/self/gate erfragen (localProof.steps) und die dort
      empfohlene Beweismenge fahren; localProof: null ⇒ volle Kette aus AGENTS.md §Verify.
  - note: >
      Der LIVE-Nachweis (ein pi-Slot meldet ctx ungleich null) ist NICHT Sache der Lane —
      er braucht einen Deploy und gehoert der MAIN. Die Lane liefert Code + Pin + die
      Begruendung ihrer Nennerwahl.
output:
  schema: fleet.act/v1.response
  artifacts: [commit]
  commitRequired: true
communication:
  questionRecipient: Program-MAIN, Slot 3
  replyTo: clarification
  stopConditions:
    - Der gleiche fix-run-fail-Zyklus fuenfmal ⇒ melden, nicht weiterwuerfeln.
    - Wenn die Messung die 1M NICHT traegt ⇒ stoppen und berichten, nicht raten.
  wakeOn: owner-send
```

---

## 2. ACP-17 — Regelbuch-Korrekturen (Haupt-Checkout, siehe K1)

Vier Korrekturen, alle bereits mit Messung belegt (Handoff §3); keine davon braucht eine
neue Untersuchung.

| # | Datei | Ist | Soll | Beleg |
|---|---|---|---|---|
| 1 | `rulebook/einstieg.md:57` · `rulebook/deploy.md:70` | `undo-land` deckt genau EIN Land | Stack der Tiefe 3, kontiguitaetsgeprueft, `pop()` einzeln | `server.ts` `UNDO_STACK_MAX :9785`, `:17742` |
| 2 | `rulebook/einstieg.md:74-116` | Schwelle ~44 % / Anker ~36 % | 25 % = Uebergabeentscheidung + HANDOFF vorbereiten · 30 % = keine neue unklare Tiefenarbeit; `ctx: null` bleibt UNKNOWN; nie als blinder Timer | Owner-Entscheid, Handoff §1(2) |
| 3 | Token-Hygiene (Fragment noch zu waehlen) | Die `${VAR:+}`-Regel deckt `ps` nicht ab | Zusatz: *niemals Prozess-Kommandozeilen ungefiltert ausgeben* | Handoff §6, eigener Vorfall |
| 4 | `rulebook/einstieg.md:92` · `deploy.md:23` · `lane-discipline.md:54,62` | hartcodierte Tailscale-IP im Mess-Schnipsel | Platzhalter, wie `watchdog.sh` ihn schon fuehrt | die Public-Pflichtprobe deckt die Fragmente nur deshalb nicht auf, weil sie gitignored sind |

**Nicht in diesem Act:** der Supervisor-Effort-Absatz. Er haengt an ACP-12 (nativer
claude-Effort-Adapter) und wird erst wahr, wenn der gelandet ist.

**Pflichtschritt nach jeder Fragment-Aenderung:** `CLAUDE.md` neu rendern (Zeile in
`rulebook.ts:5`), sonst driftet das Generat gegen die Quelle und
`GET /api/self/gate` meldet jeder Lane `rulebookDrifted: true`.

**Verifikation:** `bun e2e/pins.ts` (RULE_VERIFY vergleicht die Schrittkette ueber
`watchdog.sh`, `AGENTS.md`, `verify-proportion.ts`) und die im Deploy-Fragment vorgeschriebene
Public-Pflichtprobe (`git grep -inE` auf Domain und Tailscale-IP), die leer bleiben muss.
Ihr Muster wird hier ABSICHTLICH nicht ausgeschrieben — genau das Zitieren des Musters ist
der Grund, warum die Probe auf dem getrackten Baum heute rot ist (§4).

---

## 3. ACP-14 — Ambient-Steuer senken (Owner-Akt)

Write-Set liegt in `~/.claude/**`, ausserhalb dieses Repos. Das Regelbuch verbietet einer Lane
ausdruecklich, dort zu schreiben („Worktree isolation covers FILES IN THIS REPO only … stop and
report instead of touching it"). Inhalt: Hook-Erfolgs-Echo kuerzen, blockierende Sprueche in
voller Laenge belassen.

Ich schlage vor, dass ich dazu einen konkreten Diff-Vorschlag als Text vorlege, sobald ACP-10
laeuft — ausfuehren wuerdest ihn du.

---

## 4. Nebenbefund beim Redaktions-Check: die Public-Pflichtprobe ist auf dem getrackten Baum ROT

Gemessen am 2026-08-21 auf HEAD `c684e89`. Das Deploy-Fragment schreibt nach jeder Aenderung
eine `git grep`-Probe auf Domain und Tailscale-IP vor, die LEER sein muss. Sie ist es nicht:

| Datei | Art des Treffers | eingetragen |
|---|---|---|
| `docs/supervisor-succession.md:238,242,249,278` | die literale Tailscale-IP in vier curl-Beispielen | `0d18f11`, 2026-08-17 |
| `docs/worktrail-audit-III/private-repo-g.md:338,341` | literale IP **und** Port in zwei Worktrail-Zeilen | `0dc741d`, 2026-08-19 |
| `docs/feedback-plan-agent-operating-system-2026-08-11.md:107` | zitiert das Probe-MUSTER samt Domain | aelter |
| `docs/triage/batch-F-wissen.md:40` | zitiert das Probe-MUSTER samt Domain | aelter |

**Nicht live exponiert:** `origin/main` steht auf `b43496a` (2026-08-10), und die Probe ueber
`origin/main` ist LEER. Alle vier Treffer sind nach dem letzten Push entstanden. Es ist damit
ein **Publish-Blocker**, keine bestehende Veroeffentlichung.

Zwei getrennte Klassen, und sie brauchen verschiedene Antworten:

1. **Echte Werte** (`supervisor-succession.md`, `private-repo-g.md`) — gehoeren auf Platzhalter,
   wie `watchdog.sh` sie fuehrt.
2. **Zitate des Probe-Musters selbst** — die Probe findet sich hier selbst. Solange das Muster
   irgendwo ausgeschrieben steht, ist die Probe strukturell nie leer. Entweder duerfen die
   beiden Zeilen das Muster nicht mehr ausschreiben, oder die Probe muss ihre eigenen
   Fundstellen ausnehmen. Diese Datei schreibt es deshalb nicht aus.

Gehoert NICHT zu diesem Program. Hier als Befund abgelegt, nicht repariert.
