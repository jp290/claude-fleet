# Stand 2026-08-21 — Compact-Snapshot Claude Fleet

> **Redigiert 2026-08-31 beim Tracken in das public Repo:** Tailscale-IP → `100.64.0.1`
> (der Platzhalter, den `watchdog.sh` und `SHARING.md` führen), `/Users/<account>/` → `~/`,
> die Leck-Guard-Muster durch ihre Beschreibung ersetzt. Der Inhalt ist sonst unverändert.

Diese Datei ist ein **lokaler, ungetrackter Arbeitsstand**. Sie ist keine normative Quelle und darf
wegen privater Altinhalte weiter unten nicht ungeprueft in das public Repo gelangen. Fuer Regeln gilt
`AGENTS.md`, fuer die Architektur `SYSTEM.md`, fuer die aktuelle Controller-Uebergabe der oberste Block
von `HANDOFF.md`. Dieser Abschnitt ist nur die kompakte Wiederaufnahmehilfe nach einem Compact.

## Jetztzustand — 2026-08-21, nach ACP-18

- `main` steht auf `92bce16`. Der letzte Code-/Lane-Tip `aedc24d` ist gelandet, auditiert und deployed;
  der eine Commit Abstand ist nur das anschliessende Handoff-Dokument (`codeBehind:false`,
  `bundleStale:false`). Kein weiterer Deploy ist dafuer noetig.
- ACP-18 hat das rote Integrations-Audit geschlossen: Ursache war ein Rennen in der **Testsonde**, nicht
  im Produkt. `waitForLabel` lieferte frueher einen Slot vor Existenz seiner tmux-Pane; der Fix wartet
  auf die Pane und laesst `respawnScreen` als Sonde selbst scheitern. Land `aedc24d`; Post-Land-Audit
  `green`, 1.265.185 ms, 2.774 Checks, 0 Fehler; Deploy `9279b26b`, `ok:true`.
- Aktive ACP-Program-MAIN ist **Controller V in Slot 3**, frisch gegruendet und bei der letzten Messung
  14,4 % Kontext. Program `eeba7c04…` ist occupant-genau an Slot 3 gebunden. Sie arbeitet an **ACP-13
  (Bindungs-Reparatur)** und hat bereits 3 live / 9 stale / 1 unbound Bindungen sowie 0 Session-ID-
  Abweichungen unter den live Bindungen gemessen.
- Feste Reihenfolge nach ACP-13: **ACP-11 → ACP-12 → ACP-15 → ACP-16**. Erst danach Studio-Feuerprobe.
  Kein neuer Architektur-Act soll diese serielle `server.ts`-Kette kreuzen.
- Supervisor-Deadman `cd22090c` prueft vier Stunden lang alle 20 Minuten, insgesamt 12 Laeufe; bei der
  letzten Messung waren 2 gelaufen und 10 uebrig. Er ist nur Sicherheitsnetz: Events/Watches sind die
  Primaersteuerung. Er darf weder dispatchen noch committen, landen, deployen, killen oder restarten.
- Gerade lief keine Suite und kein Land wartete.

## Noch offen, aber kein aktueller Blocker

1. Der historische rote Audit auf `850d27b` ist trotz belegter Ursache noch nicht adjudiziert. Der neue
   Tip ist gruen; die fehlende Adjudication erzeugt nur wiederholtes Supervisor-Rauschen.
2. Die fertigen GLM-Sessions in Slot 4 (iOS-Transfer) und Slot 7 (Architecture Gap Critic) wurden in
   `~/[privater Owner-Ordner]/harvest/` gesichert, leben aber noch und gehoeren in einen Cleanup.
3. Die 15 ungetrackten Dateien im Haupt-Checkout sind unveraendert. Beschlossener Schnitt: fuenf rohe
   Architektur-Papiere nach Public-Hygiene als NONNORMATIVE Historie archivieren; zehn lokale/private
   Root-Dateien recoverable nach `[privater Owner-Ordner]/inbox/2026-08-21` verschieben; nichts loeschen.
4. Supervisor-Metadaten und echte Pane widersprechen sich (API meldet `fable`, Pane zeigte Opus 5); die
   gewuenschte native Sonnet/high-Gruendung wird in ACP-12 mechanisch geschlossen.
5. `dispatch` war bei der letzten Messung aus, waehrend bereits eine Queue-Zeile wartete. Nicht manuell
   als Abkuerzung einschalten: ACP-16 baut die intelligente Project-MAIN-Freigabe unter dem Master Stop.
6. GLM-Vorschlaege bleiben unpromotet: F2 (Dispatch-Sichtbarkeit) sinnvoll mit ACP-16; F3
   (Regel-Frische fuer Nicht-Lanes) und F1 (typisierte Specialist-Ablage) spaetere Owner-Entscheide;
   F5 reist allenfalls mit ACP-16; F4 bleibt advisory/spaeter.

## Wiederaufnahme in einem Satz

Zuerst Controller V/ACP-13 anhand echter Pane und Fakten weiterfuehren; danach die festgelegte serielle
Kette ACP-11/12/15/16, waehrend der Supervisor nur Ausfaelle meldet — keine neue Grundsatzanalyse und
kein weiterer Agent, bevor diese Kette einen konkreten unbekannten Rand erreicht.

---

# Historischer Stand 2026-08-19, spaeter Abend — Uebersicht + Plan Private-spiel-a

## Die drei Studios laufen, jedes mit gebundener Programm-MAIN

| Studio | Slot | Modell | Repo | Stand |
|---|---|---|---|---|
| 🏎 Private-repo-d `a923fe12` | 2 | Opus 5 (1M) | `~/private-repo-d` | Lane 1-4 gelandet; 3 Kernzeilen offen, alle aussehensseitig |
| 🧟 Private-repo-g `d565f710` | 3 | Fable 5 | `~/private-repo-g` | Architektur promotet (AR1-AR7), Lane-01 gebrieft |
| 🎮 Arcade `7c257d44` | 5 | Opus 5 (1M) | `~/private-repo-n` | beide Aufgaben gebaut, gelandet, BEURTEILT |

Vor drei Stunden hatte NULL von acht aktiven Programmen eine lebende Bindung.

## Was heute sonst durchging

- Vier Lands, alle Gate-gruen: `fbf44b1` · `54bae42` · `d8dc1d3` · private-repo-l `6ec3dfe`.
- Der Land-Stau ist strukturell entschaerft: `FLEET_VERIFY_WAIT_MS` steht am LIVE-srv auf 2.700.000
  (an pid 25832 gemessen). Der erste Anlauf hatte nicht gewirkt — ein Watchdog-Prozess von 08:26
  lebte weiter; Slot 4 hat ihn per Kickstart ersetzt und neu deployt.
- Aufgeraeumt: Waisen-Worktree entfernt, 287 MB toter Scratch gereapt, vier Sessions abgetreten
  (s3, s5-alt, s7, Sol) nach getrackter Uebergabe.

## Private-spiel-a: der Plan

Der Kritiker hat die Arbeitsliste schon geschrieben, mit Frames als Beleg statt Meinung.
Quelle: `~/private-repo-n/tasks/private-repo-l/CRITIC.md`.

Sein Urteil im Kern: **ambitioniert, und das Schwerste ist gelungen** — die Arena ist ein gebauter
Ort mit lesbarem Plan, die Puls-Phase wird vom Saal angekuendigt statt von einem Banner, der Treffer
hat Stufen (Schockwellen-Ring gegen stumpfen Schmierer), das Werkzeug ist Hardware statt Etikett, die
Kamera lebt.

**Vier benannte Schwaechen, das ist die Ausarbeitung:**

1. Es erklaert sich in WORTEN — permanente Steuerungszeile und schwebendes HIT / BUMP / BOARD DOWN
   tun Arbeit, die Ring und Schmierer schon leisten.
2. Der Combo ist eine ZAHL AUF EINER PLATTE, ohne Traeger in der Welt.
3. Der RIVAL-TELL fehlt in jedem aufgenommenen Frame.
4. Der ENDZUSTAND wurde in 545 s ueber drei Laeufe nie gesehen, obwohl der Hook beweist, dass ein
   Lauf endete und neu startete.

**Reihenfolge:**

- **A — eine Lane raeumt die vier Punkte ab.** Done-Kriterium je Punkt ist ein FRAME, der das
  Gegenteil zeigt, nicht eine Codezeile. Punkt 4 braucht zusaetzlich einen Probe-Lauf, der den
  Endzustand wirklich erreicht; ist er unerreichbar, ist DAS der Befund.
- **B — erst danach Varianten**, zwei bis drei, jede auf GENAU EINER benannten Achse verschieden.
  Sonst sind es drei zufaellige Builds und kein Vergleich. Die Achsen sind Geschmack und gehoeren
  dem Owner.
- **C — jede Variante eigene Ledger-Zeile und eigener Kritiker-Durchgang** gegen DENSELBEN
  Phase-1-Massstab.

**Frist: 2026-08-20 23:07 UTC.** Lieber ein sauberes A als drei halbe Varianten.

**BLOCKIERENDE FRAGE, die die Arcade-MAIN gerade klaert:** erlaubt die Ausschreibung mehrere
Einreichungen je Aufgabe? Wenn nur EINE zaehlt, sind die 2-3 Versionen keine Einreichungen, sondern
KANDIDATEN, aus denen der Owner eine waehlt. Das ist ein anderer Plan, und er muss vor der Arbeit
feststehen.

## Beim Owner offen

1. Zwei Kart-Geschmacksfragen: Double Dash zwei Fahrer je Kart = Kernmechanik oder Stilzitat ·
   bleibt "2000er PC-Grafik" die Richtung? Private-repo-ds Appeal-Gate ist daran mechanisch zu.
2. Die Varianten-Achsen fuer Private-spiel-a, sobald die Einreichungsregel geklaert ist.
3. `.env`: `~/private-repo-d` und `~/private-repo-g` in `FLEET_VERIFY_CMD_REPOS` eintragen. Ohne das laeuft
   eine Fleet-Lane dort ins `exit 42` (verify SKIPPED, landet nie unbeaufsichtigt). Beide haben ein
   taugliches Skript: private-repo-d `bun test` / `bunx tsc --noEmit`, private-repo-g `verify`.
4. Programm-Freigaben, die den heutigen Aerger strukturell beheben: `bf01ef52` (Bindungs-Zug gegen
   die fuenf Zombie-Programme), `83075480` (Durchlauf-Regel), `b3d042ec` (Suite-Contention).
5. Die Agenten-Session-Idee: mein Vorschlag ist, zuerst den BRIEFKASTEN zu bauen (ein Berichts-Ledger
   plus ein `produces`-Feld an der Lane), nicht die neue Session-Art.

## Links (Tailscale)

    Private-spiel-a (private-repo-l)   http://100.64.0.1:8101/private-repo-l/index.html
    Private-spiel-b (private-task-06)    http://100.64.0.1:8101/private-task-06/index.html
    private-repo-d, aktueller main http://100.64.0.1:4410
    private-repo-c (aelter)       http://100.64.0.1:4321
