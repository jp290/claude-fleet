# Akt-2-Worker R10–R13 — Ineffizienz, fehlende Werkzeuge, Kontext-Packs (GLM-Lane-Audit)

---
frage: Wo verbrannten die vier Akt-2-Lanes R10–R13 (Private-repo-o, 2026-08-30) Minuten, Calls und Kontext ohne Nutzen; welche Löcher hatten Brief bzw. Environment; und — Sonderfrage R11 — hat die R11-Session je ein gerendertes Bild ihres Ergebnisses angesehen, konnte sie es, und war ihre Wirkungslosigkeit aus der Session heraus erkennbar?
urteil: Die vier Lanes waren diszipliniert und berichtsehrlich, aber JEDE bezahlte dieselben vier Env-Sonderkosten (fehlendes node_modules, Report-Limit 4000, Shell-/Hook-Stolperer, Scratchpad-Modulpfade) mit zusammen ~20 Calls und ~6 Minuten; der teuerste Einzelbefund des Tages ist kein Session-Fehler, sondern ein Brief-Loch: R11 hat ihr Ergebnis nie im Bild gesehen und konnte die Wirkungslosigkeit doch rechnerisch erkennen — die Bezugsflächen-Anweisung „Zahlen aus der Spielkamera" gehört als Pflichtfeld in jeden Render-Brief UND als stehendes Capture-Werkzeug ins Spiel-Repo.
bereich: [game-maker, lane-lifecycle]
belege: Transcripts ~/.claude/projects/-Users-owner-private-repo-o-worktrees-game-maker-private-repo-o-fresh-worktrees-fleet-260830{095227-9c4c,103650-9efb,105346-9de2,115331-d5c5}/*.jsonl (Index über Skript; Zeilennummern = JSONL-Zeilen); fleet.json (Brieftexte 78436516/740561d1/10db781e/abd3c246); Spiel-Repo git log/show b300caf, 7f28375, b020fd1, 53c92b9, 89ec073 (Checkpoint), f4973b5; Sidecar-cost-state je Sessiondir.
nicht-gemessen: Ob das Playwright-MCP der R11-Session zur Verfügung stand, ist ABGELEITET (in R10s Session 20 min vorher per ToolSearch gefunden, gleiche Maschine/Harness/Tag; R11 rief nie ToolSearch). Modellmix (R10-Commit nennt Opus 4.6, R12-Commit Opus 5) nicht bewertet. Kontext-Füllstand der Lanes: keine Sensoren in Transcripts, nur Result-Bytes als Proxy. apiDur≈Wall-Time in R12 als „Denkzeit" gedeutet, nicht pro Call gemessen. Die SHA-Umschreibungen beim Land (df076fc→b300caf, 5f0a04c→b020fd1, 72b3bea→53c92b9) nur an Endpunkten verglichen, Server-Log nicht gelesen. Spiel-Repo-Quellcodes (pose.ts, camera.ts, tools/serve.ts) nicht direkt gelesen — alle Code-Aussagen stammen aus Transcript-Belegen oder Briefen/Checkpoints.
stand: 2026-08-30
---

Coverage-Plan: Ich lese die vier Briefe, die fünf relevanten Commits samt Checkpoint 89ec073, und indexiere alle vier Worker-Transcripts per Skript (Tool-Sequenz, Result-Bytes, Zeitachse); gesucht sind Umwege/Retries ohne vorab formuliertes Kriterium, ad-hoc gebaute Mess- und Sichtwege, und Selbstbeschaffung in den ersten ~25 Calls je Session als Brief-/Env-Löcher; gut heißt: je Session ein gerankter, quellenbelegter Befundkasten plus Kontext-Pack-Vorschlag.

## R10 — 78436516 „Tausch: absteigender Körper liegt flach"

**Faktenkasten.** Transcript `a818986c-…jsonl` (1 Datei, 3,0 MB). Zeit 09:52:34 → 10:14:59 = **22,4 min**; cost-state: **12,49 USD**, apiDur 1291 s, toolDur 62 s. **106 Tool-Calls** (69 Bash, 9 browser_navigate, 9 browser_take_screenshot, 9 Read, 8 Edit, 1 ToolSearch, 1 browser_resize), **1078 KB Result-Bytes — davon ~968 KB = 9 als Bild gelesene PNG-Screenshots**. Commit der Lane `df076fc`, gelandet als `b300caf` (pose.ts +82, render.test.ts +136, spec.ts +19 — Commit-Message inhaltlich identisch mit dem Pane-Bericht).

**Befunde (nach Kosten gerankt).**

1. **Der Screenshot-Weg war komplett ad-hoc gebaut — kein stehendes Werkzeug.** Nach `bun tools/serve.ts --entry src/render/dev.ts --page src/render/dev.html --port 5174` (Z201) und ToolSearch auf die Playwright-Tools (Z205) musste die Lane erst MAL DAS BILD FINDEN: `find / -name "swap28-before.png"` (Z217, ~26 s Vollplatten-Suche), dann Finden im Repo-Worktree (Z221). Danach ein Dreisprung pro Bild: take_screenshot → `cp` ins Scratchpad → Read (z. B. Z214/226/236, Z379/382/386, Z539/542/552). 9 Bilder × 3 Calls = **27 Calls für 9 Bilder, ~968 KB Kontext**. Und die Screenshots landeten als `.playwright-mcp/` IM Worktree und mussten vor dem Commit entsorgt werden (`rm -rf .playwright-mcp`, Z582–587). Vorschlag: stehendes `tools/capture.ts` (serve + Playwright + definierte Regionen → Zahlen/Bilder nach AUSSERHALB des Worktrees); Briefzeile im Render-Profil: „Bilder bekommst du so, nicht anders."
2. **node_modules fehlt im Frisch-Worktree** (Z109–135): Scratchpad-Messskript scheitert erst am relativen Modulpfad, dann an `Cannot find package 'three'`; `bun install` (39 s? nein — 39 ms, aber 4 Calls Diagnose davor). Vorschlag: Env-Vorbereitung Schritt 0 im Profil oder bei Lane-Erzeugung.
3. **Kleine Shell-Stolperer:** `grep --include=*.ts` ungequotet → zsh-Fehler (Z164–166); GNU `timeout` existiert auf macOS nicht — R10 traf das nicht, R11 schon (s. dort). Report-POST 1× am 4000-Zeichen-Limit (Z635).
4. **Ein echter Fix-Zyklus:** erster verify-Lauf nach Test-Edit rot in T1 typecheck (Z509–521), 1 Iteration, sauber behoben. Keine Retry-Schleife.
5. **Positiv / Ehrlichkeit:** numerisch VOR dem Bild gemessen (Z109–175); Messgröße (a) (Kopfabstand durch ChaseCamera) ehrlich verworfen, weil sie am kaputten Stand grün war („114 von 900 Zeilen"), stattdessen (b) Kopffreiheit mit Verankerung an zwei akzeptierten Stationen; 3 Mutationsproben gefahren (Z498–506); „Left for you" nennt den unausgebauten Rest (steigender Körper schwebt 0,29 m, durch render.test.ts:243 gepinnt) statt ihn zu verschweigen. Pane-Schluss „R10 landed as `df076fc`" (Z651): „landed" war zum Zeitpunkt noch falsch — Land brachte `b300caf`. Formulierungsdetails, kein Befund.

**Kontext-Pack-Vorschlag (was ab Minute 0 hätte dabeisein müssen).** Die ersten ~25 Calls: 10 Lektüre-Reads (Brief nennt pose.ts/index.ts/render.test.ts — camera.ts, build/figure.ts, geom.ts selbst gewählt, legitim für die Messgrößen-Wahl), dann 4 Calls Mess-Setup-Fehlversuche (Modulpfad, fehlendes three), dann Messungen. Das Loch ist ENV, nicht Wissen: (1) deps installiert, (2) das serve+capture-Rezept inkl. dev-URL-Parametern (`?swap=<pct>&hold=1&cam=…&camh=…`) und Zielport für PNGs außerhalb des Worktrees, (3) Report-Limit-Hinweis. Hätte ~8 Calls und 0 Reibung gekostet statt ~12.

## R11 — 740561d1 „Werttrennung Licht/Schatten" — SONDERFRAGE

**Faktenkasten.** Transcript `db17c756-…jsonl`, 10:36:58 → 10:47:52 = **11,0 min**; cost-state: **4,54 USD**, apiDur 653 s, toolDur 8 s. **46 Tool-Calls — ausschließlich Bash.** **60 KB Result-Bytes.** Commit `7f28375` = Land-SHA. Parallel-Sidecar: Pre-Land-Clean-Review-Session (`e40cf343`, „Lichtanlage Werttrennung", 0,35 USD, 0 Tool-Calls — Diff-Review ohne Werkzeuge).

**SONDERFRAGE — die drei Antworten:**

1. **HAT R11 je ein gerendertes Bild ihres Ergebnisses angesehen? NEIN.** Tool-Zensus: 46× Bash, null Read-Image, null browser_*, null Serverstart; grep über den Index nach `camera|kamera|capture|screenshot|bild|pixel|azimut` liefert außer `.gitignore`-Auflistung (Z121) und einem spec.ts-Kommentar (Z45) nichts. Die einzige „Messung" war ein handgebautes Formel-Skript (s. u.).
2. **KONNTE sie es? Werkzeugseitig ja, briefseitig wurde ihr jeder Anlass genommen.** DER WEG STAND BEREIT: R10 hatte 22 Minuten vorher in derselben Fleet-Umgebung `tools/serve.ts` gestartet (R10-Z201) und die Playwright-MCP-Tools per ToolSearch gefunden (R10-Z205–206); ein Dev-Harness mit parametrisierbarem Startbild existiert (`?seed=1&t=0`-äquivalent über dev:render; Checkpoint 89ec073 nennt dev:render 5174). R11 rief **nie ToolSearch** und **listete tools/ nie** (ihr erstes `ls` zeigt nur Top-Level und src/render, Z19). Ihr Brief sagte: „Du beurteilst NICHT, ob es gut aussieht. … Es gibt in diesem Repo KEINE Pixelmessung … Erfinde keinen Breaker, der so tut, als messe er das Bild. Über das Aussehen urteilt MAIN" — das ist kein Verbot des Ansehens, aber es nimmt einer Bildprüfung Zweck UND Auftrag. Der Brief FEHLTE der entscheidende Satz, den erst R12s Brief hatte: „RECHNE FÜR DAS, WAS DIE KAMERA SIEHT."
3. **War die Wirkungslosigkeit aus der Session heraus erkennbar? JA — rein rechnerisch, ab 10:40:24 Uhr (Turn ~20).** R11s eigenes Messskript (Z148) steht der Sonnenvektor drin: `Lsun = (-0.62, tan 52°, 0.78)`. Die Kamera steht bei yaw=0 HINTER dem Kart und blickt +z (src/render/camera.ts:131–151 — von R10 um 09:53:02 gelesen, von R11 NIE). Also: Die Kamera sieht die -z-Normalen; die Sonne beleuchtet die +z-Normalen — **die Kamera sieht per Konstruktion die Schattenseite.** R11 verstärkte das unsichtbare Licht (sun 1.55→2.10) und dämpfte das sichtbare (rim 0.75→0.30). Ein Read von camera.ts plus drei Zeilen `N·view`-Rechnung im bereits existierenden Skript hätten es gezeigt. Das grüne verify konnte es NICHT zeigen (alle Bildbreaker messen Projektion, keine Pixel — das wusste der Brief und schrieb es hin). MAINs Nachmessung (Checkpoint 89ec073) bestätigt exakt diese Kausalität: JEDE sichtbare Fläche dunkler, keine heller.

**Was R11s Skript tatsächlich maß:** Bezugsnormale waren `sunSide = normalize(Lsun ohne y)`, `darkSide = -sunSide`, `top = UP` (Z148) — WELT-Richtung, nicht Kamerahalbkugel. Damit war „Werttrennung 1.32 → 2.61" wahr UND wirkungslos zugleich.

**Befunde (nach Kosten gerankt).**

1. **Brief-Loch Bezugsfläche (s. o.):** Folgekosten = R11-Land wirkungslos (11 min, 4,54 USD) + MAIN-Pixelmessung + komplette Folgelane R12 (17,8 min, 5,98 USD). Der teuerste Posten des Tages. Vorschlag: Brief-Pflichtfeld für Render-Lanes (Vorlage: R12-Brief-Abschnitt „RECHNE FÜR DAS, WAS DIE KAMERA SIEHT" + „nenne den schlechtesten Heading").
2. **Env-Reibung gebündelt:** `timeout 600 …` → command not found (Z83); `bun run verify | tail` mit erhöhtem Timeout → PreToolUse-Hook-Block `guard-tail-pipe-on-longrunner.sh` (Z87 — der Hook feuert nur bei deklariertem Langläufer: run_in_background oder Timeout > 120 s; Skriptkopf gelesen); Umleitungs-Workaround (Z91); `Cannot find package 'three'` → `bun install` (Z101–112). ≈ 5 Calls.
3. **Report-Trim:** 4 POSTs, 3 fehlgeschlagen (4705 → 4177 → 4114 → 3994 Zeichen; Z300–310). ~1,5 min.
4. **Scratchpad-Modulpfad:** erster Versuch mit relativem Pfad scheiterte (Z148–150) — dieselbe Lektion, die R10 und R13 im selben Minutenrahmen bezahlten.
5. **Positiv / Ehrlichkeit — vorbildlich:** R11 las three-Shader-Quellen in node_modules (gradientmap_pars_fragment, lights_pars_begin; Z70–144) und fand damit eine DRITTE Ursache (Toon-Rampe über die volle Kugel indiziert — Schattenseite auf LACK strukturell unmöglich). Mutationsprobe gefahren (Z255–262). Und der Bericht BENANNTE die tödliche offene Frage selbst: „Was ich nicht belege: kein Pixel gemessen, Build nicht gefahren. Ob die Plastizität aus 5,4 m Verfolgerkamera ankommt … dazu sage ich nichts" (Z318). Der Bericht war ehrlich — der BRIEF hatte die Bezugsfläche nicht definiert, auf die die Ehrlichkeit hätte zielen sollen.

**Kontext-Pack-Vorschlag.** (1) Die Bezugsflächen-Anweisung mit Kamera-Anker (`src/render/camera.ts:131–151`, Blickachse bei yaw 0 = +z, „schlechtester Heading nennen"); (2) deps installiert; (3) Hook-/timeout-Muster („Läufe mit erhöhtem Timeout ausgeben in Datei umleiten, nie pipen; GNU timeout existiert hier nicht"); (4) Report-Limit 4000 mit `wc -c`-Vorcheck; (5) optional: der eine Satz „Bilder ansehen ist erlaubt und kostenlos via <capture-Rezept> — behaupten darfst du daraus nichts über MAINs Urteil." Damit wäre die Session bei gleicher Laufzeit mit einer Kamera-bezogenen Zahl statt einer Welt-bezogenen Zahl geendet.

## R12 — 10db781e „Schlüssellicht-Azimut zur Verfolgerkamera"

**Faktenkasten.** Transcript `4f9ec219-…jsonl`, 10:53:54 → 11:11:39 = **17,8 min**; cost-state: **5,98 USD**, apiDur 1063 s ≈ Wall-Time (zwei lange reine API-Phasen 10:54–11:02 — Entwurfsdenkarbeit, nicht Untätigkeit). **39 Tool-Calls — ausschließlich Bash. 51 KB Result-Bytes.** Commit `5f0a04c`, gelandet als `b020fd1`. Clean-Review-Sidecar `884955a1` (0,06 USD).

**Befunde (nach Kosten gerankt).**

1. **Report-Trim-Schleife, 8 POST-Versuche:** Erstversuch abgewiesen (4276 > 4000), dann Kürzen in Etappen 4276 → 4216 → 4206 → 4124 → 4039 → 4004 (abgewiesen) → 3997 ok (Z213–273), inkl. 2 Skriptfehler beim Kürzen selbst (Quoting `= `, Z237–239; Python-Assertion, Z248–250). **~2,7 min reine Report-Reibung, 6 fehlgeschlagene POSTs.** Vorschlag: `wc -c`-Vorcheck als Profilzeile oder API-seitig längeres Limit / Auto-Split.
2. **node_modules zu spät:** Lichtanlage-Tests rot mit „Unhandled error" — Ursache fehlendes three; Diagnose über package.json/cat/verify.ts-Grep bis `bun install` (Z134–157) = 4 Calls. Dieselbe Warteschleife wie R10/R11/R13, nur später im Lauf entdeckt.
3. **Hook-Block** beim `bun run verify | tail` mit Timeout 600000 (Z181–185) — dieselbe Env-Stolperkante wie R11; jede Frischsession lernt sie neu.
4. **Ein echter Fix-Zyklus:** T1-typecheck-Fail nach Test-Rework (Z185–191), 1 Iteration. Keine Schleife.
5. **Positiv:** Der Brief war der beste der vier (MAIN-Pixelmesstabelle, Ursachenanalyse, Bezugsflächen-Anweisung, Verbotsliste) — und die Session lief dementsprechend fast ohne Selbstbeschaffung: die ersten ~25 Calls sind Reads genau der im Brief genannten Dateien (spec.ts, index.ts-Lichtblock, Lichtanlage-Tests, camera.ts:131–151, materials.ts) plus die verlangte Rechnung (sichtbare Halbkugel 10,9 % → 67,1 % Lichtseite; schlechtester Heading yaw 70°, Wert 1.333 — Z75–77, Z194–196). Höhenanteil Ziffer für Ziffer erhalten, GRUND-Invarianz auf 2,1e-7 relativ BELEGT statt behauptet (Z160). `rim <= fill`-Schranke bewusst ersetzt mit Begründung statt angepasst (Brief DONE-2 erfüllt). Schluss ehrlich: „Über das Bild behaupte ich nichts — … ich habe den Build nicht gestartet."

**Kontext-Pack-Vorschlag.** (1) deps installiert; (2) Report-Limit + `wc -c`-Vorcheck; (3) Hook-/Pipe-Muster. Mehr brauchte diese Session nicht — der Brief selbst war der Kontext-Pack. Das ist das Gegenexperiment zu R11 und der stärkste verfügbare Beleg dafür, dass das Pflichtfeld im Brief der richtige Hebel ist.

## R13 — abd3c246 „Streckenbindung des Rundenfortschritts"

**Faktenkasten.** Transcript `309e36dc-…jsonl`, 11:53:38 → 12:08:12 = **14,6 min**; cost-state: **6,67 USD**, apiDur 859 s, toolDur 21 s. **56 Tool-Calls** (50 Bash, 4 Edit, 2 Read), **114 KB Result-Bytes**. Commit `72b3bea`, gelandet als `53c92b9` (Checkpoint f4973b5: „R13 gelandet, Restbefund Reihenfolge, Owner will A/B"). Report-Status **needs-main** (berechtigt, s. Befund 5). Clean-Review-Sidecar `871eab9e` (0,27 USD).

**Befunde (nach Kosten gerankt).**

1. **Der Repo-Testpilot selbst war Gesetzesbrecher — Vorbefund, kein Session-Fehler:** R13 maß als Erstes (gute Reihenfolge!), dass der Verfolgungsfahrer in `testkit.ts` 1875 von 5164 Ticks BIS 34,5 m NEBEN der Bahn fährt (Z66–73). Unter der neuen Regel wären zwei bestehende „eine Runde"-Tests falsch geworden. R13 straffte den Piloten (Querbeschleunigung 11 → 4 m/s²; lat=4 ⇒ 0 Ticks daneben, Runde sogar schneller: 42,56 s statt 43,03 s; Z214–234) statt die Erwartungen zu senken — richtig entschieden. Aber: ein stehender Sensor („Pilot-Abweichung von Mittellinie" im Profil/verify-Vorbericht) hätte das VOR der Lane gezeigt. Vorschlag: Pilot-Lateral-Stat als stehende Zahl im Game-Maker-Profil.
2. **Env-Reibung wie immer:** Scratchpad-Modulpfad-Erstversuch rot (Z55–57), `grep --include=*.ts`-Glob-Fehler (Z76–78), node_modules erst nach rotem Testlauf entdeckt (Z162–188, 4 Calls), Report-POST 1× am Limit (Z394–396).
3. **Mutationsprobe 1 zu grob gewählt** (`const onTrack = true` — riss ALLE Breaker mit, Z326–330), sauber wiederholt mit `PROGRESS_GRACE_M = 1e9` (nur T17 rot, Z331–334). Gute Disziplin, kostete 1 Extra-Loop; die Brief-Vorgabe „nimm deine Bedingung heraus" war mehrdeutig genug, um beide Lesarten zuzulassen — eine halbe Briefzeile („nur die Toleranz aufziehen") hätte den Loop gespart.
4. **Keine Planlosigkeit:** Kern vorab als Bedingung formuliert, Prototyp der Abkürzung GEGEN den ungegatelten Code gefahren (Z122–145: „finished true, naive 620 vs 728,9 gefahren"), T17 mit BOTH-Richtungen (Abkürzung kommt nicht ins Ziel UND saubere Runde kommt ins Ziel) plus 4 Nebenassertionen. Genau der Test, den der Brief verlangte.
5. **Positiv / Ehrlichkeit — das beste Verhalten des Tages:** Der Abkürzungsvorteil IST nach dem Gate nicht null, sondern −2 s Zeit (48,51 s vs 50,55 s sauber; Haarnadel übersprungen, Meter auf der Startgeraden nachgeholt, Ziel bei s=118,7 in Runde 2). R13 maß das, dokumentierte es mit Zahlen im T17-Header, BAUTE es nicht aus (Mechanik = Owner-/Folgefrage) und meldete needs-main: „The decision I owe you." Das ist Kriteriumstreue statt Kriteriums-Drift — der Fall, für den der Status existiert.

**Kontext-Pack-Vorschlag.** (1) deps installiert; (2) die Pilot-Lateral-Stat als stehende Zahl („dein Testpilot fährt heute 36 % der Ticks im Gras — bei Bahnbindung zuerst denken"); (3) Report-Limit; (4) Shell-Glob-/Modulpfad-Muster. Die ersten ~25 Calls sind sonst sauber brief-gedeckte Reads und Messungen.

## Sammelabschnitt — was davon ins stehende Game-Maker-Profil gehört

1. **ENV-VORBEREITUNG „Frisches Lane-Worktree":** Schritt 0 = `bun install` (alle 4 Sessions bezahlten je 3–6 Calls + teils einen roten Testlauf; Install selbst nur ~40 ms). Dazu die Shell-Zeilen: GNU `timeout` existiert nicht; Globs in grep-Optionen quoten; Läufe mit erhöhtem Timeout NIE in `| tail` pipen (Hook `guard-tail-pipe-on-longrunner.sh` blockt), sondern in Datei umleiten; Scratchpad-Skripte importieren Repo-Module nur mit absoluten Pfaden. Eine Profilzeile, vier Mal weniger Reibung pro Tag.
2. **BRIEB-PFLICHTFELD für Render-Lanes: „Zahlen aus der Spielkamera."** Formulierungsvorlage steht fertig in R12s Brief (Bezugsflächen = sichtbare Halbkugel bei definierter Startbedingung; schlechtesten Heading nennen). R11 ist der Beweis der Notwendigkeit, R12 der Beweis der Wirksamkeit (fast null Selbstbeschaffung). Antwort auf die Sonderfrage: **erst das Brief-Pflichtfeld** — es kostet nichts und hätte den Tagesverlauf gekippt; **zweites, robusteres Standbein** ein stehendes Capture-Werkzeug, das auch eine Brief-Lücke nicht mehr teuer machen kann.
3. **STEHENDES WERKZEUG `tools/capture.ts`:** serve + Playwright + benannte Bildregionen → Zahlen (Leuchtdichte je Fläche) und/oder PNG nach außerhalb des Worktrees. Ersetzt R10s 27-Call-/968-KB-Tanz (inkl. Vollplatten-`find` und `.playwright-mcp`-Entsorgung vor dem Commit) und hätte R11 die Bildfrage sichtbar gemacht, ohne dass irgendjemand MAINs Urteilsrecht antastet. R10-Z201–237 ist die funktionierende Wegskizze.
4. **REPORT-LIMIT 4000 ZEICHEN:** 12 fehlgeschlagene POSTs an diesem Tag (R10:1, R11:3, R12:6, R13:1), zusammen ~5 min und ~16 Calls. Profilzeile („Report in Datei, `wc -c`, erst POSTEN wenn ≤ 3950") oder API-Anhebung — Ersteres kostet nichts.
5. **NICHT beanstandet — funktioniert:** Verify-Nutzung (4/4 zitieren vollständige ALL-PASS-Läufe mit Exit-Code, genau wie verlangt), Schreibflächendisziplin (kein Lane-Fall über die Grenze; R10 nennt die Versuchung selbst und widersteht), Berichts-Ehrlichkeit gegen Commits (alle vier Pane-Berichte decken sich mit den gelandeten Diffs; einzige Kosmetik: „landed" vor dem Land bei R10), Mutationsproben (3 von 4 Sessions fahren sie unaufgefordert sauber).

**Tagesbilanz der gemessenen Reibung:** ~20 Calls und ~6 min Env-/Report-Reibung über vier Lanes — klein gegen den EINEN strukturellen Verlust: R11s wirkungslose Land mit Folgelane R12 und MAIN-Nachmessung, verursacht durch eine fehlende Brief-Zeile über die Bezugsfläche. Die Lanes waren nicht das Problem; der Pack war es.
