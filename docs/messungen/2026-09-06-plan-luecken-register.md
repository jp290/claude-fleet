---
frage: Welche Teile der benannten Plaene haben noch keinen konkreten Ausfuehrungs- oder Abnahmeauftrag?
urteil: Fuenf verbleibende Dispositionsfragen; Wellen und Program-Rueckwege haben bereits Traeger. Kein fehlender Typname beweist eine fehlende Funktion.
bereich: [programs, context, queue, studio]
belege: [fleet.json, lane-outcomes.jsonl, post-land-audits.jsonl, audit.jsonl, context-receipts.jsonl]
nicht-gemessen: Kein neuer Canary, kein Live-Browser, keine vollstaendige Server- oder Fremdrepo-Pruefung; keine Tokenkostenmessung kuenftiger Schnitte.
stand: 2026-09-07
---

# Plan-Luecken: konkrete Traeger statt weiterer Planstapel

Astra Review-MAIN, Program `eec69528`, Task `1af3fa1f`. Direktausfuehrung statt Lane auf ausdrueckliche Controller-/Owner-Richtung; nur diese Datei, kein eigener Commit/Land/Deploy. Dateiname aus dem Auftrag beibehalten. Codeanker beziehen sich auf `453092cb242a472fe9876b30222bfde0283c5b04`. Lokale Uhr der Datenprobe: `2026-09-06T22:36:13Z`; Gespraech datiert 2026-09-07. Keine minutengenaue Gleichzeitigkeit aller Quellen behauptet.

## Ergebnis und Begriffsgrenze

**Die Ausgangsthese „in KEINEM aktiven Program“ ist zu stark.** Fleet-Betrieb `f170dc46` hat einen allgemeinen Auffangauftrag; Fleet-Architektur `e3b3a064` haelt Zielbild und neue Charters. Das ist noch kein benannter Bau-/Abnahmeauftrag mit Done. Dieses Register misst diese engere Luecke. Empfehlungen sind keine Freigaben. Reihenfolge: Tasks und Studios zum Laufen bringen, dann optionale Plattformmechanik.

`eeba7c04` steht `complete`, aber sein gespeichertes Erfolgskriterium verlangt die Vorbereitung von **ACP-01/02**, nicht die Umsetzung aller neun Acts. Seine Schliessung widerlegt den Status spaeterer Ziele nicht. Der Program-Brief selbst unterscheidet Vorlaeufer und Zielmodell (`docs/agentic-control-plane-program-2026-08-20.md:7`, `:319`). Historische Coordinator-Regeln daraus ersetzen nicht den heutigen AGENTS-Vertrag.

## Rangiertes Register

Lane-Zahlen sind **Schnittschaetzungen**, keine bestellten Ressourcen; Tokenkosten je Zeile **unknown**. Owner-Handarbeit beschreibt den Mechanismus, keine gemessene Eingriffszahl. „Eigenes Program“ heisst: von Fleet-Architektur erst schneiden und zur Promotion vorlegen, nicht jetzt parallel gruenden.

| Rang | Plan, Codeanker, Zweck | Verbleibende Luecke / Unsicherheit | Owner-Handarbeit und Kosten des Liegenlassens | Bau-/Betriebskosten und Risiko | Verify / Done des vorgeschlagenen Schnitts | EIN Vorschlag |
|---|---|---|---|---|---|---|
| 1 | Queue-Plan `:88` Slice 3; `server.ts:8322` ANALYSIS_TICK_MS, `:8340` BRIEF_TICK_MS, `:8493` tickBriefSweep, `:8529` tickAnalysisSweep. Analyse und Briefkompilierung sollen unterschiedliche Arbeit leisten. | Beide Mechanismen existieren; Analyst live aus. Compiler Default aus, aktueller Live-Wert hier nicht belegt. In den geprueften offenen Auftraegen kein begrenzter Wiedereinschalt-/Ablehnungsversuch mit Abnahme. `d2b69d3d` ist nur Prozessentwurf. | MAIN/Owner liest und schaerft Zeilen selbst; automatischer Compiler-Ausfall ist nicht durch schlechte Analyst-Ergebnisse begruendet. Die Ersparnis durch Aktivierung bleibt ungemessen. | Geschaetzt 1 Mess-/Betriebsschnitt; getrennte Schalter, begrenzte Stichprobe, Rollback. Risiko: Modellkosten, Brief-Drift und veraltete Analyse; kein blindes Einschalten. | Zuerst beide Live-Schalter mit Quelle belegen, dann dieselben freigegebenen Beispielauftraege raw/kompiliert vergleichen; erhaltene Absicht, frische Attribution, Kosten und Falschalarme protokollieren. Entscheidung pro Werkzeug, keine Kopplung. | **Zeile in Fleet-Betrieb `f170dc46`**: begrenzter Betriebsversuch, nicht pauschale Aktivierung. |
| 2 | ACP Acts 5–7/9 `:319-398`; Hub `:37-47`, `:91-102`; `server/types.ts:389` ClarificationRequest, `:1344-1356` Studio; `context-manifest.ts:77` planRepoContext. Ein Studio braucht dieselbe nachvollziehbare Arbeitsidentitaet ueber Kontext, Routing und Ergebnis. | Bausteine sind vorhanden. Kein gepruefter konkreter Auftrag verbindet **denselben** Studio-Schritt ueber Rollen-/Kontextauslieferung, zwei Harnesses, Frage/Antwort und getrennte Retry-/Nachfolgeidentitaet. Vollstaendige funktionale Abdeckung bleibt unknown, nicht „alles ungebaut“. Architektur-Program plant, Review `457511cc` misst nur. | Rollen-/Modellwahl, Quellen und Ergebnisidentitaet werden von MAIN/Owner zusammengetragen; falsche Gleichsetzung von alter und neuer Instanz kann alte Evidenz gueltig erscheinen lassen. Kein Nachweis, dass dies in jeder aktuellen Session passiert. | Geschaetzt 1 Vertrags-/Probeentwurf, danach 1–3 getrennte Reparaturschnitte nur fuer reproduzierte Luecken; echte Harness-Aufrufe separat budgetieren. Risiko: zweites Act-System neben Tasks. | Ein vereinbartes Studioszenario mit vorhandenen Task/Program/Receipt-IDs; Create/Assign/Result/Retry sowie Frage/Antwort und Resume/Restart/Succession A/B abnehmen. Erst danach entscheiden, ob neue Typen ueberhaupt noetig sind. Bestehende Game-/Lifecycle-Proben wiederverwenden. | **Eigenes Program „Studio-Vertragslauf“ vorschlagen**, erst nach Abgleich mit `b2aa5b45`/`66499a03`, keine Wiederholung ihrer Produkt-/Lifecycle-Proben. |
| 3 | Queue-Refinement `:57-68` Verwerfen-Empfehlung; `analysis-prompt.ts:90-103`, `:153`; `server.ts:25945-25999` taskAct. Eine Queue muss auch begruendet kleiner werden koennen. | Archivieren/Loeschen existiert bereits. Analyst fordert ready/needs-you, keinen typisierten Verwerfen-Vorschlag. N2 `f98facad` baut erledigt/widerlegt/offen, schliesst Archivierung ausdruecklich aus. Ein eigenstaendiger Empfehlung->Owner-Entscheid-Pfad ist nicht beauftragt. | Owner liest und archiviert einzeln; schwache Ideen bleiben zwischen benoetigten Notizen liegen. 141 pending Notizen sind Bestand, **kein Beweis**, dass alle nutzlos sind. | Geschaetzt 1 enger Schnitt nach N2; Token unknown. Risiko: falsches Verwerfen oder Verlust des Entscheidungsmotivs; kein Auto-Verfall und kein Auto-Loeschen. | Vorhandene Archiv-/Unarchive-Tuer nutzen; Empfehlung mit Grund/Quellbeleg sichtbar, bestaetigter Entscheid nachvollziehbar, Ruecknahme pending statt queued. Widerspruch des Owners und unveraenderte offene Notiz negativ testen. | **Zeile in Fleet-Betrieb `f170dc46`** nach N2, ohne dessen Zustandsmodell zu duplizieren. |
| 4 | ACP Act 8 `:362-381`; `server.ts:11502-11512` CLEAN_REVIEW_MODE, `:17277-17349` runCleanReview. Unabhaengige Kritik kann Candidate-Risiken finden, darf aber keine Promotion ersetzen. | Ephemerer Worker und off/shadow/gate existieren. Live off ist belegt; ununterbrochenes Off seit Juli nicht gemessen. Vorhandene Historie widerlegt „nie ein Reviewer gelaufen“; eine aktuelle reale Feuerprobe des kompletten ACP-Policy-Vertrags ist hier nicht belegt und nicht konkret beauftragt. | Owner/MAIN interpretiert Kandidaten selbst. Ohne gemessenen Zusatznutzen wuerde Aktivierung nur eine weitere bezahlte Pruefstufe einschieben. | Geschaetzt 1 begrenzter Shadow-Auswertungsschnitt; echter Modellverbrauch unknown. Risiko: falsche Entwarnung, zusaetzliche Gate-Wartezeit. Kein Gate-Modus ohne Promotion. | Historische Shadow-Inputs gegen die korrigierte Fork-Basis abgrenzen; frischen festgelegten Candidate nur shadow bewerten, Fehler/Timeout/unparseable und Identitaetswechsel nachweisen; Nutzen gegen vorhandene deterministische Checks beurteilen. | **Zeile in Fleet-Betrieb `f170dc46`**, nach Land-Pipeline-M1–M3, nicht deren Neubewertung. |
| 5 | Queue-Refinement `:61-64`, `:95-97` L-Workspace; Hub `:95-102` offene Modell-/Routingfragen. Diskussion ist sinnvoll, wenn mehrere Beitraege zu einem begrenzten Entscheid fuehren. | Hub-Richtung bestaetigt Diskussionsbedarf, aber Pack=Plan=Envelope und Entscheiderrechte bleiben offen. Clarification ist bereits adressierte Frage, nicht Mehrparteien-Thread. Kein konkreter Ausfuehrungsauftrag fuer Task-/Pack-Thread mit dauerhaftem Verdict gefunden. | Owner traegt Diskussionen zwischen Sessions; ohne Subject/Entscheidungsabschluss entstehen neue Notizstapel. Aktuelle Inbox-Rueckwege sind separat beauftragt und werden nicht als ungetragen gezahlt. | Geschaetzt 1 eigener Clarify-/Vertragsakt vor jeder Implementierung; Baukosten unknown. Risiko: paralleler Speicher neben Tasks/Inbox und unbegrenzte Diskussionsschleifen. | An einem Task/Pack Subject, Teilnehmerrechte, eine Abschlussentscheidung und Rueckverweis definieren; Wiederholung/Retirement/fehlende Quelle pruefen. Vorhandene Inbox/Comments zuerst auf Eignung pruefen; keine neue Plattform allein wegen des Namens. | **Eigenes Program „Task-/Pack-Diskussion“ vorschlagen**, nach funktionierendem Rueckweg und explizitem Owner-Vertrag. |

## Ausgeschlossen: bereits getragen oder Ausgangsbehauptung widerlegt

- **Wellen-S2/S3:** `233e1c2b` nennt Flaechenbestaetigung und start wave im Intent. Eigene offene Ausfuehrungszeilen habe ich neben M1–M3/N1/N2/S1 nicht gefunden; das ist eine verbleibende Zerlegung im vorhandenen Program, kein Anlass fuer ein neues. S1-Land-Note `49d93bc` hat `verify.ok:true`, Exit 0. Kein neuer Review der Wellenregeln.
- **Program-Inbox/Antwortwege:** `c464af30`, `417d2be5`, `74319808`, `288f6359`, `8e1e0be4`, `18e87e67`, `c62aa3e9` tragen Folgeschritte in Fleet-Betrieb. Inbox-Land `453092c` ist keine gruene Abnahme: Note `stale:true`, `waitedOut:true`, Exit null. Das ist ein Beweisstatus, keine ungetragene neue Funktion.
- **Act 7 ist nicht „keine adressierte Frage“:** `openClarification` (`server.ts:6295`) persistiert worker/receiver/provenance; `replyClarification` (`:6477`) bindet die antwortende Session und begrenzt Doppelantworten. `audit.jsonl` enthaelt 10 clarification_open und 10 clarification_answered; diese Zaehler beweisen keine vollstaendige End-to-End-Korrelation.
- **Kein Neubau nur wegen fehlender Namen:** Die getrackte TS-Deklarationssuche findet kein AgentInstance/ContextEnvelope; Attempt trifft eine lokale E2E-Hilfsform. StudioStageSpawn, ContextPlan/Manifest und ClarificationRequest sind gelesene funktionale Vorlaeufer. Rollen-Sicht ist nicht automatisch Role Bootstrap, ContextPack nicht automatisch ContextEnvelope.
- **Act 9 teilweise getragen:** `b2aa5b45` verlangt einen Game-Workflow-Trockenzyklus mit Kontextmessung; Game-Programs verlangen reale Produkt-/Critic-Belege; `66499a03` fordert den autonomen Program-Lifecycle. Rang 2 verlangt nur deren noch unbelegten gemeinsamen Studio-Vertrag, keine erneute allgemeine Feuerprobe.
- **Kein Register-Grossumbau:** Generalsanierungs-Abschlussmessung `:25-58` nennt den rescopten Split und offene Masse; keine Wiederaufnahme von P5 oder Flake-Arbeit. Die alte ACP-Program-Evidence und seine 18 gelandeten Outcomes wurden als Vorgeschichte, nicht als Beweis aller neun Acts gelesen.

## Messung und Wiederholung

Gelesen: die drei benannten Plaene, Hub-Doc als vierte Pflichtquelle (Portfolio diesmal nicht erforderlich), gezielte Codefenster oben, aktive Program-Charters und zugeordnete offene Task-Briefe. Quellensuche zuerst graphify; Graph liefert Anker, keine Laufzeitwahrheit. Git-Commit-Bodies zur Plan-/Metadatenhistorie gelesen; keine alte Zahl als aktuelle uebernommen.

```sh
git rev-parse HEAD
git show main:docs/agentic-control-plane-program-2026-08-20.md
git show main:docs/plan-queue-refinement-2026-08-11.md
git show main:docs/queue-wellen-2026-09-06.md
git show main:docs/fleet-hub-overlay-2026-09-06.md
bun task-metadata.ts --state fleet.json --default-repo /Users/owner/claude-fleet > /tmp/astra-plan-task-metadata.json
./state.sh > /tmp/astra-plan-state.log 2>&1
git notes --ref=fleet/land show 49d93bc
git notes --ref=fleet/land show 453092c
```

`fleet.json`: 11 Programs mit status=active (keine Behauptung ueber lebende MAINs), 141 Tasks mit kind=notiz/status=pending; davon 33 ohne files in der Ausgabe von task-metadata, 108 derived. 43 auftrag mit status pending/queued/sent, alle 43 derived, **0 confirmed**. Supersedierte, noch offene Zeilen sind im Nenner enthalten; kein Mass unabhaengiger verbleibender Arbeit. Metadaten-Snapshot meldet keine errors. Die alten 128/66 und 0/31 sind damit nicht der heutige Zaehler. studios ist leer; das beweist fehlende Registrierung, nicht fehlende Game-Arbeit.

Live-Sensor aus state.sh: ANALYSIS_MS=0, CLEAN_REVIEW=off; BRIEF_MS wird dort nicht ausgegeben, daher live unknown trotz Code-Default 0. Ledger gelesen/gefiltert: 817 Outcomes, 507 Audits, 40214 audit-Zeilen. Beide Trail-Register inventarisiert (6448 lokale JSONL-Dateien, 36 im TMPDIR-Auditregister), **nicht voll ausgewertet**. Keine daraus erfundene Canary-Abwesenheit. Keine Suite-/Host-Konfiguration geaendert, keine Modelle gestartet.

## Verify und offene Grenze

`GET /api/self/gate` verweigert der MAIN erwartbar mit `not a lane — the gate judges a lane's land`. Danach die vereinbarte Docs-Kurzkette gefahren: `bun install --frozen-lockfile && bun e2e/pins.ts`, Exit 0, Originaltail **ALL PASS**, Log `/tmp/astra-plan-luecken-verify.log`. `git diff --check` ohne Ausgabe. `git status --short` nennt ausschliesslich diese neue, noch ungetrackte Datei; das ist die ausdrueckliche Uebergabeausnahme, kein landbarer sauberer Lane-Baum. Kein git add/commit durch Astra. Diese Notiz ist kein vollstaendiges Outside-in-Abschlussregister und keine Owner-Promotion. Integration/Commit erfolgen durch den Controller nach Pruefung auf laufende Lands.
