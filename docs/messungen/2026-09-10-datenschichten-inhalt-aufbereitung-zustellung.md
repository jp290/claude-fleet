---
frage: Welche Fakten und Instruktionen erreichen Fleets Rollen über welche Aufbereitung, Zustellung und Leser?
urteil: Die leichten Linsen existieren, aber MAIN-Rail und Inbox-Zustellung widersprechen sich; Zustellbelege und Controller-Nachfolge bleiben lückenhaft.
bereich: [datenlayer, zustellung, rollen]
belege: [server.ts#sendText, server.ts#tickInboxNudge, server.ts#openFleetReport, server.ts#PROGRAM_MAIN_RAIL_BLOCK, server.ts#programExecutionView, lane-signals.ts#attentionAnswerMessage, task-notes.ts#renderNotesBlock]
nicht-gemessen: Owner-GETs und fremde Self-Projektionen mangels Rollencredential, vollständige Kontext-Replays, tatsächliches Verstehen und private Recovery-Inhalte.
stand: 2026-09-10
---

## §1 Schichten-Matrix

Messung tatsächlich **2026-09-09, ab 21:31 CEST**, Dokumentdatum gemäß Auftrag 2026-09-10. Code: `42692a0a`; Live-Dateien: Haupt-Checkout `/Users/owner/claude-fleet`. Kein Nachweis, dass dessen HEAD identisch mit dem laufenden Server-Build ist.
Die Abstraktion rollenbezogener Projektionen soll existieren: Sie begrenzt den Entscheidungskontext und erhält Herkunft und Autorität, ohne alle Speicherbestände in jede Pane zu kopieren.

Methode: Dateigrößen sind UTF-8-Dateibytes; JSON-Teilbestände sind **kompakt reserialisiert** (`ensure_ascii=False,separators=(',',':')`), deshalb nicht additive Ausschnitte der eingerückten `fleet.json`. GET-Größen zählen Antwortkörper, keine HTTP-Header. Push-Größen zählen UTF-8-Text, keine JSON-Hülle. Kein Tokenmaß.
`C/M/L/S/T` = Controller / Program-MAIN / Lane / Supervisor / Steward. „Leser“ meint gebauten oder vorgeschriebenen Zugang, **nicht nachgewiesenes Lesen**. `unknown` ist keine Null.

Bekannt: [Audit 09-04 §A, B1–B8](2026-09-04-datenschichten-audit.md), [Schnittliste §Messbasis, Posten 1–4](../schnittliste-kommunikation-datenschichten-2026-09-07.md), [Mindestform und C4](2026-09-07-datenvertraege-umsetzungsplan.md), [Program-Ansicht §1 L0–L9](../program-ansicht-informationsschichten-2026-09-05.md). Diese Vorarbeiten wurden über `git show main:<pfad>` gelesen; S3 vom 09-08 nur als Überschriftenliste. Alte Fehlerbehauptungen werden nicht als heutige Befunde übernommen.

| Schicht | Inhalt (Fakten) | Writer / Autorität | Aufbereitung (Route/Skript) | Bytes heute | Zustellung push/pull + Gate | Leser je Rolle | Verlustart |
|---|---|---|---|---|---|---|---|
| `fleet.json` | Aktive Belegungen, Tasks, Programs, Kommunikation, Jobs, Integrationszustand; auch Credentials | Server nach autorisierten Routen; Speicherung ist keine Owner-Promotion | `server.ts#queueStateSave`; selektive Routen statt Rohdatei | 1.679.950 Datei | Pull indirekt; Rollen-/Tokenprüfung | C/M/L/S/T jeweils Projektion; Boot lädt Rohbestand | Vollserialisierung je Save; Retention der Teilbestände, stale Bindung; bekannt §A |
| `tasks` | Exakter Auftrag, optional Brief, Status, Provenienz, Notizen | Owner; gebundene MAIN darf eigene pending Arbeit anlegen; Notiz bleibt Vorschlag | `createTaskForMain`, `capTasks`, `briefAndSend`, `/api/tasks`, `/api/self/program-execution` | 621.022; 200 Zeilen, 136 notiz-pending, 44 auftrag-pending, 0 queued | Pull; Lane-Gründung pusht ausgewählten Brief | M eigenes Program, L eigene Aufgabe/Quellen; C/T/S begrenzte Sichten | Nichtterminales verdrängt Historie; Quellenhalter verlängern Retention; bekannt B4, heute zusätzliche Quellenbindung |
| `programs` | Absicht, Erfolgskriterium, Entscheidungen, Evidenz, Promotion, MAIN-Bindung | Vorschlag der Session; Bestätigung/Promotion Owner; Runtime-Bindung Server | `programContent`, `programExecutionView`, `/api/self/programs` | 265.350; 69, davon 63 complete | Pull; JSON-Ausschnitt im MAIN-Brief | M eigenes, C eigene Vorschläge/Bindungen, S Querblick; L Auftrag | Kopierter Gründungsstand altert; Leser muss aktuelle Projektion nachziehen |
| Program-Inbox / Handover | Typisierte Referenz, Zeitpunkt, readBy/readAt, dropped; Nachfolge-Verpflichtungen | Producer-Routen / Succession; Read-Receipt durch gebundene MAIN | `appendProgramInbox`, `programInboxView`, `standardHandoverLines` | Inbox-Felder zusammen 566; 2 Einträge, beide gelesen; kein gespeichertes `handover`-Feld | GET `/api/self/inbox`; separater POST read; optional 195-B-Nudge | M; C nur bei eigener MAIN-Bindung; L/S/T keine eigene Program-Inbox daraus | 100 Einträge: zuerst gelesene, sonst älteste ungelesene weg; `dropped`/`inboxLost` benennen Verlust; fehlendes Subject bleibt unknown |
| `fleetReports` | Worker-Claim, Commit/Verify im Text, Herkunft, Abnahme | Lane behauptet; Empfänger/Owner entscheidet | `openFleetReport`, `fleetReportMessage`, `/api/self/fleet-report`, Inbox-Join | 179.222; 44 Reports, 23 ohne Entscheidung | Aktives Program: Inbox-Pull; Legacy: Event-Push + GET; unterschiedliche Schuldenbudgets | L eigene, M adressierte; Owner verwaiste über eigene Tür | Receiver-Tod, Retention, fehlender Pull; 23 unentschieden beweisen nicht 23 ungelesen |
| `events` / `watches` | Beobachtete Übergänge, Subject/Receiver-Identität, Zustellstatus/Ack, Reservation | Server aus Subscription oder Fachereignis; Ack bestätigt Empfang, keine Abnahme | `tickWatches`, `slotDeliveryBudget`, `/api/self` GET | 214.009 / 4.047; 114 Events / 13 Watches; 98 acknowledged, 15 inbox, 1 receiver-gone | Pane oder Inbox; 5 offene Schulden+Reservationen je Slot; siehe G1 | C/M/S abonnierte Rückwege; L darf nicht abonnieren; T eigene zulässige Türen | `send-uncertain` ≠ geliefert; Receiver/Subject gone; Deckel; unbekannte Intervalle |
| Clarifications / Attentions | Frage, Antwort, Provenienz, zuständige Identität | Worker fragt MAIN; MAIN fragt Owner; Antwort trägt jeweilige Autorität | `clarificationWatchMessage`, `clarificationReplyMessage`, `attentionAnswerMessage`, Self-GETs / Inbox | 35.999 / 52.254; 12 / 21 Zeilen | Frage/Antwort je Pfad Push oder Program-Inbox; Requester-/Receiver-Prüfung | L↔M und M↔Owner; C nur mit MAIN-Bindung; S keine Owner-Attention | Kein Empfänger, unbeantwortet, verweigert, Nachfolge; heutige Program-Succession nicht pauschal als Verlust aus altem B1 übernehmen |
| `auditPings` / Autos | Zustellversuch eines roten Audits; terminierter Auftrag | Server-Prädikat / autorisierter Schedule-Writer | `tickAuditPing`, `tickAutos` | 23.867 / 4.575 | Push; G0, Idleness, Zeit-/Run-Limits | Audit-Ping: C/M und auch geeignete S als Nicht-Lanes, nicht T; Autos adressierter Slot | Kein Empfänger bleibt pending; Bestätigung des Sends ist nicht Adjudikation |
| Jobs / Helper / Merge-Zustand | Suite/Command-Lauf, Ergebnisse, Artefaktmetadaten, Land-Verdikt | Server/Helper misst; Ergebnis erteilt keine neue Promotion | `programExecutionView`, Job-GET, `deliverMergeVerdict` | laneSuiteJobs 85.659; commandJobs 9.853; merges 2.262; mergeParked 11.077 | Pull plus terminaler Push; Jobartefakte werden im Text **benannt**, nicht hochgeladen | M Integrationsentscheidung, L eigener Lauf, S/T Zustand | Helper nicht erreichbar, Artefakt bleibt remote; Verdikt maximal 2 Versuche, dann nur Pull |
| `lane-outcomes.jsonl` | Ausführung/Land, SHAs, Verify, Program-/Task-Join | Serverausgang; technischer Beleg | `readLedger` → `programExecutionView` | 1.204.832; 882 Zeilen | Pull; Projektion begrenzt historische Zeilen | M/S/T/C nach autorisierter Sicht; L eigene Belege | Retention/fehlender historischer Join; bekannt B4 |
| Audit-/Deploy-Ledger | Auditresultat, Checks, Covers; menschliche Adjudikation; Laufstand | Suite/Server misst; Adjudikator deutet, Owner autorisiert Deploy | `readLedger`, `auditWatchMessage`, `auditPingMessage` | post-land 2.178.899/552 Zeilen; adjudications 62.385/178; deploys 54.478/196 | Pull plus Watch/Ping; Auditoutput ist Daten | M Integration, S/T Betrieb, C autorisierter Abschluss; L angeforderter Beleg | Zeile erst bei Laufende; fehlend ist nicht rot; Rotation 5 MB + eine Generation |
| `audit.jsonl` | Betriebsereignisse, Holds, Kill/Land-Provenienz | `server/audit-log.ts#audit` | `server/persist.ts#readLedger`, Audit-/Statistikreader | 1.410.716/10.401; `.1` 5.000.098/41.760 | Pull, kein automatischer Rohtext-Push | C/S/T Betriebsanalyse; M/L gezielt | Zweite Rotation überschreibt ältere Generation; bekannt B7 |
| `context-receipts.jsonl` | Ausgewählte/ausgelassene Anker, Gründe, HEAD, Hashes, Briefbytes, Quellenzuordnung | Brief-Renderer; Auswahl-/Bauquittung, kein Verstehensbeweis | `briefAndSend`, Gründungsrenderer, `programExecutionView` (20 jüngste Receipts) | 1.036.944; 658 Zeilen; 569 renderer=v2 | Pull; Hash+Bytes statt Quellenprosa | M Rekonstruktion; L Notizberechtigung via `laneNoteIds`; Owner Receipt-Route | Bestehendes Receipt beweist nicht beobachtete Acceptance; Code liest Rückgabewert hier nicht aus |
| ContextPlan / Manifest | Quellen, Zweck, Harness/Modus/Capability, Status, beobachteter Hash | Fleet-Seed/Repo-Manifest; advisory Pointer unter Owner-Regeln | `context-plan.ts#contextOmissionFor`, `context-manifest.ts`, `renderContextAnchorBlock` | eigener ausgelieferter Ankerblock 948 | Push im Brief, Quelle später Pull/Dateilesen; Weglassungsleiter, Manifest max. 65.536 B | C/M/L/S nach Gründungsweg; kein eigener T-Plan hier nachgewiesen | Ausgewählt ≠ geöffnet; erster Weglassungsgrund, kein Vollbericht aller Gründe |
| Notizvorschau / Volltext | Id, erste Aussage, gemeinsame Dateien, ggf. explizite Aufgabe | Notizautor behauptet; Zuordnung autorisierte Task-Tür; Verdict separater Akt | `task-notes.ts#renderNotesBlock`, `/api/self/notes` | eigene Vorschau 1.627; eigener GET 15.898 | Push: 5 Vorschauen, je max. 300 Zeichen, 3 Dateinamen; Pull auch expliziter Overflow | L belegte Quellen; M Zuordnung; übrige Rollen keine garantierte Vollverteilung | Unzugeordnete Notiz ohne passende Fläche findet keinen Lane-Leser; 136 pending ist **kein** Ungelesen-Zähler |
| Regelwerk / Rollenkarte | Normative Invarianten, Rolle, Werkzeuge; kaum Laufzeitfakten | Owner-promotierter Vertrag / generierter privater Overlay | Loader gemäß `AGENTS.md#Loader boundary`; `laneRulebookFor`; `docs/controller.md` | AGENTS 21.795; CLAUDE MAIN 83.891, diese Lane 36.492; Controller-Karte 8.624 | Dateiload: Codex/Pi AGENTS; Claude Overlay plus portable core; kein regelmäßiger Server-Push | alle eigener Vertrag; nur Claude lädt CLAUDE automatisch | Snapshot-Drift, Doppelinstruktion; Dateibytes sind kein Nachweis vollständiger Modellaufnahme |
| HANDOFF / Git / Messregal | Restabsicht, Entscheidungen, Quellbelege, Commits | Agent schreibt Claim, Git belegt Version, Owner promotet Regeln | `buildSuccessionBrief`, `standardHandoverLines`, `state.sh`; Index als Auswahl | HANDOFF 457.857/5.651 Zeilen; kein vollständiger Lesepayload gemessen | Pull; generic Succession fordert nur Kopf, Program-Succession zusätzlich eigenes Record | C/M/S Nachfolge; L gezielter Pointer; T eigene Kontinuität | Altes Prosa-State; Kopf nicht ganze Datei; fehlender expliziter Join zur Nachfolgerin |
| `state.sh` / `register.sh` / `ctl.sh` | Frisch projizierte Git-/Ledger-/Queue-Fakten und mechanische API-Verben | Skript rendert Beobachtung, urteilt nicht über Produktqualität | benannte Skripte; `ctl.sh#CTL_VERBS` | **Dateien** 24.438/412 Z. und 19.927/361 Z.; Ausgabegrößen unknown | Pull per Tool; `register.sh` auftragsgemäß nicht ausgeführt | C/M/S Gründung; L gezielte Erdung, T Betrieb | Skriptdatei ≠ Ausgabe; kein passender CLI-Leser für mehrere neue Self-Türen |
| Prompt-Journal / Transcript / Pane | Tatsächlich protokollierter Text, Quelle, Occupant, teilweise delivery; sichtbarer Composer | `logPrompt`, Harness-Transcript, tmux; kein zusätzlicher Autoritätsgeber | `streams/prompts.jsonl`, Continuity-/Composer-Reader | Journal 19.038.944/12.331 Zeilen; Transcriptbytes nicht erhoben | Push schreibt/landet im Composer; Reader Pull; G0/G1 unten | jeweiliger Agent; C/S/T autorisierte Beobachtung | Journal ohne delivery ist kein Acceptance-Beweis; Composer-Text ist kein angenommener Turn |
| `e2e-trail/` | Einzelcheck-Spuren | Suite-Emitter misst | `server.ts#trailStats` | 7.069 Dateien, 555.629.773 B | Pull; `TRAIL_MAX_FILES=400` | Betriebsanalyse C/S/T, M/L gezielt | mindestens 6.669 Dateien außerhalb eines 400-Dateien-Reads; bekannte B5-Lücke, keine neue Prävalenzbehauptung |
| Steward-Journal / Inspektion | Eigene Betriebsnotizen, Vorschläge, Rückblick | Steward behauptet; Vorschläge sind keine Freigabe | `handleStewardRoute`, Digest/Continuity; Digest kann weiteren Reader starten | steward-journal 94.011/182; inspektion-register 9.576/26 | Pull; scoped Steward-Token; kein aktiver Steward im Slotbestand | T; übrige Rollen kein automatischer Rohjournal-Leser | Stehende Rolle abwesend; alter Bestand nicht automatisch übernommen; früheres „kein Writer“ hier nicht erneut geprüft |
| private Note-Recovery | Außerhalb des Program-Records gehaltenes Arbeitsgedächtnis; ausschließlich Schlüssel gesichtet | Ad-hoc-Dateischreiber, keine nachgewiesene Promotion | kein Treffer in geprüften Server-/Context-/Skript-/Continuity-Readern | 1.609.504 Datei; **60 Schlüssel** | nur explizites Dateilesen, kein registrierter Push/Pull | C kann sie kennen; kein nachgewiesener offizieller Nachfolgereader | Nachfolger kennt Pfad nicht; nicht Board, Program-Record oder ContextPlan; Inhalte ausdrücklich ungelesen |

**Transport-Gates.** G0 = `canDeliver`: Automation, Harness-Policy, lebender Agent, blockierter Bildschirm, Quiet Hours, ggf. Idle; `sendText` ergänzt serialisierte Eingabe, erneute Occupant-/Pane-Prüfungen, Composer-Vorprüfung, Arrival/Enter/Acceptance. Nur Codex deklariert hier Screen-Readiness; „pending“ bei etablierter Pane ist kein „blocked“. Fresh founding wartet strenger via `waitForFoundingReadiness`. Composer fehlt: Acceptance kann nicht beobachtbar sein; das ist kein Harness-Pass.
G1 = abonnierter Watch: Harness-Work-Policy und Quiet Hours ausgenommen, Kill-Switch/Liveness/Idle bleiben; `send-uncertain` wird **vor** Paste gespeichert, fremder Composer setzt pending ohne Versuchskosten zurück. Inbox-Nudge nutzt G0, aber kein Event-Receipt und kein `rollbackOwnPayload`. Merge-Verdikt nimmt Kill-Switch/Harness/Quiet Hours aus, behält Liveness/Idle und maximal zwei Versuche.

**Live-Grenze.** Owner-GET `/api/sessions`, `/api/tasks`, `/api/programs`: jeweils **401** mit dieser Lane-Credential; die angegebenen Owner-Pollbytes sind nicht nachgemessen. Eigene GETs: `/api/self` 295 B, `/api/self/notes` 15.898 B, `/api/self/fleet-report` 14 B, `/api/self/attention` und `/api/self/clarifications` je 15 B. Inbox/program-execution/supervisor-view verweigern diese Lane mit 409. `/api/self/events` ergab 401: das gebaute Pull-Gegenstück heißt GET `/api/self`, nicht ein erfundener events-GET.

## §2 Rollen-Linsen und Push-Templates

**Kein unbelegter Byte-Gesamtkontext:** Gründung unten = Fleet-Brief plus getrennt benannter Repo-Loader. Je gewöhnlichem Turn liefert Fleet **0 zusätzliche Pflichtbytes** aus einem eigenen Turn-Renderer; ereignisbedingte Texte und vom Agenten geholte Toolantworten kommen hinzu. Ob/wie viele frühere Bytes das Harness erneut ans Modell sendet, bleibt unknown. Keine Cache-/Tokenkosten aus Dateigrößen ableiten.

Instruktion/Fakt (`I/F`) wird hier operational gezählt: Handlungs-/Rollen-/API-Anleitung = I; variable Zustandsdaten samt identifizierender Hülle = F. Anker sind **Auswahlmetadaten**, gesondert A, weder 948 B Quelleninhalt noch 948 B Laufzeitfakten. Ein dynamischer Owner-Auftrag kann selbst Instruktion enthalten; „variable Zeichen“ bedeutet nicht automatisch Fakt. Prozentwerte beziehen sich daher nur auf abgegrenzte Templateblöcke. Normative Überschneidung mit AGENTS ist semantisch geprüft, kein behaupteter byte-identischer Dublettenprozentsatz.

| Rolle | Bytes Gründung | Bytes je Ereignis | I/F im abgegrenzten Push-Template | Schicht ohne garantierten Rollenleser |
|---|---|---|---|---|
| Controller | **0** eigener typisierter Founder; manueller Owner-Brief unknown. Generic Succession ohne Carry **336**. Codex/Pi zusätzlich **21.795** AGENTS; Karte bei Lesen **8.624** | Legacy-Report **332–333**, Transition-Journal **1.744–2.041**; Generic Succession mit Carry historisch **574–917** | Generic Succession: **234 I / 102 F = 69,6/30,4 %** (F nur Übergabehülle, keine Belege); fachlicher manueller Brief unknown | Fremde Program-Inbox und volle Reports ohne Bindung; private Recovery nicht automatisch in Nachfolge. Eigene MAIN-Bindung fügt M-Linse hinzu |
| Program-MAIN, Standard/Fleet-Frame | **5.145 I + P + A**; P = Program-JSON, A = Anker. Mit A=948 und fünf aktiven Standard-Programs: **9.103–18.166**; Codex inkl. AGENTS **30.898–39.961** | Inbox-Hinweis **195** + variabler GET; Legacy-Report **332–333** + GET; Watch unten | Gründer **28,3–56,5 % I**, Rest P+A; Rail allein **4.705 I / 0 F**. Inbox **100 I / 95 F = 51,3/48,7 %** | Globale Recovery, fremde Program-Volltexte, ungeöffnete Anker; eigenes Program hat Reader, tatsächlicher Pull nicht bewiesen |
| Lane, diese Gründung | **13.586** = Auftrag 9.570 + Notizen 1.627 + A 948 + Footer 1.441; Codex inkl. AGENTS **35.381** | Clarification-Answer nach aktuellem Renderer/gespeicherten Antworten **156–4.080**; Merge-Verdikt historisch **554–2.549** | Footer **1.441 I / 0 F**; **10,6 %** des Gesamtbriefs allein dieser Footer. I/F des vom Auftraggeber verfassten Haupttexts nicht automatisch klassifiziert | Fremde Tasks/Reports, Program-Inbox (409), Supervisor-Sicht (409), unangepinnte/unpassende Notizen, Recovery |
| Supervisor | Founder **2.280** = **1.332 I + A 948**, Codex inkl. AGENTS **24.075**; Bind-Brief **1.458**, Succession ohne Carry **2.353** | Eingehender generischer Audit-Ping möglich: historisch **1.279–4.029**; eigener Querblick-GET unknown. Ausgehender Nudge historisch **644–750** | Founder **58,4 % I / 41,6 % A**, keine Program-Fakten. Nudge-Frage ist dynamisch, daher kein globaler I/F-Prozentsatz | Task-Bodies im Querblick, fremde Inbox-Inhalte, Owner-Attention-Rückweg, Recovery; ausgehende Nachricht ist keine empfangene Schicht |
| Steward | **0 beobachtete** aktive Gründung; rollenbezogener manueller Brief unknown; kein aktiver `⚙ steward` | **0 journalisierte Steward-Pushes im 7-Tage-Fenster**, kein Beweis unmöglicher Zustellung; Sessions/Digest-GET unknown | Ausgehender Continue-Push **74 I / 0 F**; Verification-Suffix davon **52 I**; andere Templates unten | Program-MAIN-Inbox/Promotion ohne Bindung; aktueller eigener Betriebsleser fehlt mangels aktiver Rolle; Recovery |

P wurde aus `programContent` aktuell gerendert, ohne Nebenwirkungen; die fünf Standard-Programme haben kein `studioId`. Game-Maker-/Studio-/Target-Repo-Zuschläge sind **nicht** in obiger Spanne. CLAUDE-Dateien dürfen nicht pauschal statt AGENTS addiert werden: MAIN-Overlay 83.891 B, Lane-Overlay 36.492 B, dazu gezielt portable core; dessen tatsächlich gelesener Umfang ist hier unknown.

**Push-Katalog.** `R` = aktueller reiner Renderer mit gespeicherten Eingabefakten; `J` = tatsächlich journalisierter Text im rollierenden 7-Tage-Fenster vor der Messung, kann ältere Renderer enthalten; `S` = offen benannter synthetischer Zahlenbeispieltext, kein Zustellbeleg. Keine Serverinstanz oder Send-Route für die Messung gestartet. `[fleet]`-Konsolenlogs sind keine Paste-Templates.

| Template / Symbol | Byteumfang | I/F und Pull-Gegenstück |
|---|---|---|
| `server.ts#buildProgramMainBrief` / `PROGRAM_MAIN_RAIL_BLOCK` | R siehe Rollen; Rail-Teile **907 + 903 + 2.895 = 4.705** | Rolle/Loop I; Program P nur dortiger Snapshot. GET program-execution/programs |
| `buildProgramMainSuccessionBrief` | J **9.390–18.632**, n=40 | Program + Carry + optional Handover + Instruktion; aktueller Gesamtquotient unknown. GET program-execution/inbox |
| `buildSuccessionBrief`, `buildSupervisorBrief`, `buildSupervisorSuccessionBrief`, `buildSupervisorBindBrief` | R siehe Rollen | Instruktion + optional Carry/Anker; HANDOFF-Kopf / Supervisor-View |
| `briefAndSend` / `LANE_EXIT_FOOTER` / `renderNotesBlock` | eigene **13.586**, Teile oben | Task nicht mit unveränderlicher Instruktion verwechseln; GET notes |
| `tickInboxNudge` | J **195**, n=1 | **100 I + 95 F**; enthält nur Zahl/Program, keinen Report. GET inbox + POST read |
| `fleetReportMessage` | R **332–333**, 52 gespeicherte Events; J n=174 | jüngster J **205 I + 127 F**; **61,7 % I**; GET fleet-report + Event-Ack |
| `lane-signals.ts#laneWatchMessage` | R done-looking **620–621**, n=8; host-commit-Zweig ohne aktuelle Stichprobe | Prädikat + Deutungsgrenze + Lese/Ack-Anleitung; GET self; kein Arbeitsabschlussbeweis |
| `mergeWatchMessage` / `auditWatchMessage` | R **483–545** / **486–554** | Terminalfakten + Ack/Deutungsgrenze; GET self, Program-Projektion/Audit-Reader |
| `deployWatchMessage` / `commandJobWatchMessage` | S **527** / **469** | S: Id 24 ASCII, SHAs 40, deploy done/true, Job green/cmd X/exit 0/keine Artefakte. GET deploys bzw. self/jobs; max. 8 Artefaktvorschauen |
| `clarificationWatchMessage` | R **875–1.013**, n=2 | Frage + Herkunft + Reply-Anleitung; GET self/clarifications; Ack beantwortet nicht |
| `clarificationReplyMessage` / `attentionAnswerMessage` | R **156–4.080** / **854–3.545** | Bei Id 24 B: **78 + Q + Antwort** bzw. kind=decision **77 + Q + Antwort**; Q ist kollabierte alte Frage, keine neue Tatsache. GET clarifications/attention bzw. Inbox |
| `supervisorTransitionMessage` | J **1.744–2.041**, n=2; S **402** | S: Event-Id24, Watch-Id8, Slot2, awaiting=X,text=X. Hülle/Ack plus dynamische Aussage; GET self |
| Supervisor nudge in `server.ts#supervisorNudge` | J **644–750**, n=3 | Frage + Principal-Hülle; keine Owner-Anweisung; keine Empfangsbytes des Supervisors |
| `backlogNudgeMessage` | R **815** für aktuelle offene Aufträge | Zahl + drei ≤100-Zeichen-Ausschnitte + Instruktion; Pull register/Queue; G0, nicht T |
| `auditPingMessage` | J **1.279–4.029**, n=59 | Fakten + bis 15 Outputzeilen + Adjudikationsinstruktion; Audit-GET; G0, ein ausgewählter Nicht-Lane-Leser |
| `migrateMessage` | S **301**, pct=60,used=150000,window=258400 | **72 F + 229 I** bei diesem Beispiel; Context-Sensor plus Succession-Auftrag; nur bei messbarem Fill |
| `mergeVerdictMessage` / `deliverMergeVerdict` | J **554–2.549**, n=52 | Branch/Verdikt/ggf. Verify-Tail + nächste Handlung; Pull Merge-Status; G1-Abweichung oben |
| `renderStewardMessage` | Continue **74**, Commit **107**, Handoff **119**, Verify **133**; State-Relay **18 + Detailbytes** | Jeweils inkl. 52-B-Verify-Suffix, außer State-Relay; Pulse variabel: Gitfakten + Output + Frage + Antwortformat, kein J im Fenster. Scoped Send-Gates, Owner-/MAIN-Warten blockiert |
| Autos / Owner-/Share-Send | dynamischer Text; kein fester Umfang | Senderauftrag, kein servereigenes Fakt-Template; G0 mit pfadspezifischen Ausnahmen; Journal/History statt neuer fachlicher Pull-Projektion |

**Reader-Lücke an der Bedienung:** `ctl.sh#CTL_VERBS` enthält genau zehn Verben; inbox, tasks, program-execution, attention und release fehlen. Self-Türen existieren, CLI-Komfort nicht. Die Ausgangsbehauptung „37 Python-Heredocs in neun Controller-Turns“ wurde **nicht** nachgemessen und begründet hier keine Tool-Call-Einsparzahl.

## §3 Fünf rangierte Schnitte

Rang: möglicher stiller Ausfall einer Entscheidung vor Wiederholungs-/Lesebytes; Bytes unterschiedlicher Nenner werden nicht gegeneinander als Laufzeitkosten verrechnet. Alles Vorschläge, keine neue Promotion oder Queue-Mutation.

### 1. MAIN-Rail an den wirklichen Report-Rückweg binden

`server.ts#RAIL_TAIL` + `openFleetReport` + `programInboxFor`.
**Gegenfall:** Aktives Program, Worker meldet complete: Code setzt `receiver:null,eventId:null`, schreibt Inbox-Referenz und kein panegebundenes Report-Event. Der Rail sagt dagegen, Worker-Reports kämen von selbst in diese Pane und man solle ohne Watch warten. Generic/alte Events und aktuelle Program-Reports sind verschiedene Wege. Die dokumentierte Nachfolge liest inbox bereits; der Gründungsloop verspricht mehr als dieser Producer liefert.
**Kosten:** Ein ausgefallener 195-B-Nudge kann eine vollständige Ergebnisentscheidung still blockieren. In den gemessenen 44 Reports ist noch **kein** `basis:program`, sondern 29 program-main/15 owner-inbox; der neue Gegenfall ist am Code verifiziert, **kein behaupteter Live-Verlust dieser 44**. Live-Inbox: 2 gelesene Einträge, attention-answer/ambient-land, kein Report.
**Schnitt / Verify:** Rail nennt getrennt Program-Inbox, Wake-up und bewussten Read-Receipt. Pure Renderer-Pin plus Program-Test: Report nach aktiver Gründung, Nudge gehalten, GET liefert Report trotzdem; fremde MAIN erhält ihn nicht. Erst reale Acceptance-Probe bestätigt Wake-up. **Task: keine exakt passende ermittelt**; C4-Bezug `506fdce6` vorhanden, keine Umwidmung.

### 2. Inbox-Nudge braucht einen ehrlichen Zustellbeleg

`server.ts#tickInboxNudge` + `sendText` + `logPrompt`.
**Gegenfall:** `sendText` liefert `{acceptance:"unobservable"}` ohne Exception; der Nudge speichert trotzdem seinen Dedupe-Key. Umgekehrt wirft der Send nach Paste wegen verbliebenem Payload: der Catch setzt keinen Versuchsstempel, der nächste Tick stößt wieder auf den belegten Composer. Kein eigener Rollback-Opt-in. `tickWatches` unterscheidet diese Zustände bereits.
**Kosten:** Owner-Beobachtung aus dem Auftrag: `inboxNudgeSend` ×8, 49 sichtbare Composer-Zeichen, Fleet-Text im Composer; hier eingeordnet, nicht reproduziert. Journal-Probe C heute **4/493** unsichere/unbeobachtete Sends (489 sent, 3 uncertain, 1 unobserved), aber der einzige journalisierte Inbox-Nudge hat **kein delivery-Feld**. Dieser Nenner kann die Nudge-Verlustquote strukturell nicht messen.
**Schnitt / Verify:** Vorhandene Zustellzustände wiederverwenden, Acceptance unterscheiden, eigene Payload-Rücknahme nur bei nachgewiesener Eigentümerschaft. Positiv/negativ je Claude/Codex/Pi: leerer/fremd belegter/eigener Rest-Composer; keine fremden Entwürfe löschen, kein zweites blindes Enter. Journal zählt auch gehaltene/unbeobachtete Versuche adressierbar. **Task: keine exakt passende ermittelt**; alte `e9e22694` ist ein anderer, datierter Codex-Befund.

### 3. Controller-Arbeitsgedächtnis braucht einen benannten Nachfolgereader

`server.ts#buildSuccessionBrief` + `standardHandoverLines` + `renderContextAnchorBlock`; externe Datei `~/.local/state/claude-fleet/note-recovery-2026-09-08.json`.
**Gegenfall:** Neue Controller-Session folgt Generic Succession: state, register, HANDOFF-Kopf, Queue. Keiner dieser geprüften Reader nennt die private Recovery-Datei. Aus der bloßen Existenz der Datei folgt keine Zustellung an die Nachfolgerin.
**Kosten:** **1.609.504 B / 60 Schlüssel** außerhalb des nachgewiesenen Nachfolgepfads; Kosten ist potenziell verlorene Entscheidungsherkunft, nicht 1,6 MB bewiesener Pflichtkontext. Nur Schlüssel und Dateigröße geprüft, keine Inhalte oder Nutzungsfrequenz.
**Schnitt / Verify:** Owner entscheidet, welcher bereinigte Rest in ein vorhandenes Program-Handover oder versioniertes Artefakt gehört; keine Rohkopie als Pflichtbrief. Frische Nachfolgerin findet diese ausgewählten Fakten allein über ihren regulären Brief/Record; fehlende Quelle bleibt unknown. **Task: keine**.

### 4. Wiederholte Abschluss- und Rolleninstruktion auf den Pflichtvertrag zurückführen

`server.ts#PROGRAM_MAIN_RAIL_BLOCK` + `LANE_EXIT_FOOTER` + `AGENTS.md#Portable operating contract`.
**Gegenfall:** Codex-Lane lädt 21.795 B AGENTS und erhält nochmals 1.441 B Commit/Report/Idle-Anleitung; Standard-MAIN bekommt zusätzlich 4.705 B Rail mit Rollengrenzen und Loop. Die Mechanik ist hilfreich, aber Teile duplizieren portable Regeln; der in Schnitt 1 belegte Widerspruch zeigt den Preis unabhängiger Prosa.
**Kosten:** Gemessene zusätzliche Instruktionsblöcke **1.441 B je Lane-Gründung** bzw. **4.705 B je MAIN-Gründung/Nachfolge**; nicht alles davon ist entbehrlich (Adresse, konkrete Türen und Rückweg müssen auffindbar bleiben). Im eigenen Brief allein Footer **10,6 %**. Keine behaupteten Re-Read-Bytes je Modellturn.
**Schnitt / Verify:** Ein kanonischer Vertrag mit kleinem rollenbezogenem Akt-/Reader-Block; Golden-Renderer je Harness, Invarianten/Adresse/Rückweg bleiben erreichbar, Quellenüberlauf wird nicht still gekappt. Bytevergleich gleicher Fixtures vor/nach Änderung, plus Gegenfall aus Schnitt 1. **Task: `21ade485` pending**, C4-Anschluss `506fdce6`; bestehende Briefdichte-Frage, kein neuer Auftrag.

### 5. Alte Fragen in Antworten nur als begrenzte Identifikation wiederholen

`lane-signals.ts#attentionAnswerMessage` + `clarificationReplyMessage` + `oneLine`.
**Gegenfall:** Eine lange bereits gestellte Frage kommt mit der Antwort vollständig zurück; `oneLine` kollabiert Whitespace, kürzt aber nicht. Identität steht bereits als Request-Id im Text; die neue Tatsache ist die Antwort.
**Kosten:** Aktueller beantworteter Speicherbestand: 14 Attentions enthalten **16.743 B** alte Frage (501–1.931 B je Antwort), 12 Clarifications **8.287 B** (49–1.741 B). Das sind **25.030 B erneut renderbare Frage**, keine behauptete historische Send-Summe. Antworttexte wurden nicht als neue Instruktionsdubletten gezählt.
**Schnitt / Verify:** Begrenzter Vorschautext plus Request-Id und funktionierender Volltext-Pull; Prüfpaar lange Unicode-Frage/kurze Antwort, leere/fehlende Pflichtdaten werden korrekt behandelt; Antwort bleibt vollständig, Rollenautorität sichtbar. Byte-/Längenassertion darf Zeichen nicht mit UTF-8-Bytes verwechseln. **Task: `b5665e17` pending**; bekannt als Idee, neu hier die Bytekosten.

## §4 Nicht gemessen / inferiert

- Übergreifende Owner-Projektionen wurden mit 401 abgewiesen; keine fremden Credentials aus `fleet.json` übernommen. MAIN-/Supervisor-/Steward-GET-Gesamtbytes, Owner-Pollintervall und vollständige Tool-Calls je Controller-Entscheidung bleiben unknown. 6 Saves/90 s wurde nicht wiederholt; nur Vollserialisierung im Code verifiziert.
- Die Ausgangswerte zu `state.sh`/`register.sh` können **Ausgaben** gemeint haben; hier wurden nur **Dateien** gezählt. Daraus folgt kein Skriptdefekt. HANDOFF/CLAUDE wurden nicht vollständig gelesen; private Recovery ausschließlich über `json.load(...).keys()`, keinerlei Inhaltszitat.
- I/F ist nachvollziehbare Segmentierung, kein Modellurteil über jedes Wort. Anker A und dynamische Program-/Auftragstexte wurden nicht als ausschließlich neue Fakten ausgegeben. Semantische Dublettenquote des gesamten privaten Overlays nicht erhoben.
- J-Größen sind historische Payloads im 7-Tage-Fenster, keine Zusicherung des aktuellen Renderers; R ist reine Aufbereitung ohne echten Send. S-Beispiele beweisen nur Länge der genannten Eingaben. Nicht alle seltenen Varianten (Game-Maker, Studio, Host-Commit, Pulse, Target-Repo) wurden ausgeführt; ihre Gesamtlängen bleiben variabel/unknown.
- „Kein Leser“ ist auf die genannten Rollenpfade und geprüften Reader beschränkt; Ad-hoc-Dateilesen anderer Sessions wurde nicht überwacht. Die Recovery-Schlüssel beweisen keinen Inhalt, und eine fehlende Program-Handover-Zeile beweist keine verlorene Nachfolge.
- Keine Acceptance-/Lifecycle-Suite, kein Kill/Reboot, keine Queue-/Notiz-Verdicts: die engere Stopplinie verbietet Änderungen an Queue-Zeilen. Verifikation dieses Dokuments ist ausschließlich die beauftragte Docs-Kurzkette; Produktgegenfälle bleiben vorgeschlagene Proben.
