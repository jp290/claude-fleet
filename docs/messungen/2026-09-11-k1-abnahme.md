# K1 (`f4889e3c`) — Abnahme mit drei benannten Lücken, und warum der Land noch nichts bewirkt

*Program-MAIN Fleet-Betrieb (Slot 1), Program `f170dc46`, 2026-09-11 ~12:4x. Gegen Position 1 von
`docs/arbeitsfolge-fleet-2026-09-11.md` @`0c47a6b7`. Report `e2fb8fe4…`, Lane Slot 4.*

**Land: grün.** Kandidat `124b2df3` → main `bb28cc42` (Rebase; die Lane-Shas `d93b925c`/`124b2df3`
existieren auf main nicht mehr, sie heißen dort `2618dc50`/`bb28cc42`). Land-Note: volle Kette, alle
sieben Schritte, `exitCode 0`, `ms 150794`, `waitMs 0`, `hubPush ok`. `actor {kind: main, slot 1,
program f170dc46, task f4889e3c, sessionIdMatch: exact}` — **kein `suspect`-Flag**, anders als die
drei Owner-Route-Lands vom selben Tag.

**Am Baum geprüft, nicht dem Report geglaubt:** Write-Set exakt die vier Dateien des Briefs
(349+/12−); Proof-Log-Tail `ALL PASS`, 4107 PASS, **null** Zeilen `^FAIL` oder `N FAILURES`;
Trail-Zeile trägt `tree 124b2df3…, dirty:false`; die zweite run-id im Log ist ein Fixture-Literal
(`e2e/lane-suite.ts:314`), kein verschränkter zweiter Lauf. Stopplinie gehalten: kein
`ACCEPT_WAIT_MS`, keine tmux-Löschänderung, keine Readiness-Sonde, der Zustand ist eine
prozesslokale `Map` ohne persistiertes Retry-Feld.

## Position 1 ist damit NICHT vollständig erfüllt. Drei Lücken:

**L1 · Das Halbierungsziel ist erfüllt, und zwar schärfer als der Brief verlangte.** Der Brief sagte
wörtlich „**mindestens** halb so viele Send-Proben … wie die Tickzahl" — eine UNTERgrenze, die schon
der unreparierte Code erfüllt. Die Lane hat das als Schreibfehler erkannt und das tatsächliche Ziel
der Quelle gebaut (**höchstens** die Hälfte): 5 Proben und 5 Hold-Zeilen auf 30 Ticks. Das ist die
einzige der drei Lücken, die zugunsten der Arbeit ausfällt, und sie ist eine Korrektur am Brief,
nicht an der Umsetzung.

**L2 · „Recycelter Empfänger erbt keinen Retry" ist NICHT live bewiesen.** Der Plan verlangt einen
„Recycling-Gegenfall" in `e2e/watch.ts`. Gebaut ist stattdessen ein struktureller Beweis (der
Schlüssel IST das Occupant-Tripel Slot+`openedAt`+`sessionId`), eine globale Trail-Invariante über
alle Holds dieser Familie und ein Pin. Die Begründung der Lane ist am Code haltbar: ein Event
überlebt seinen Occupant nicht (`markFleetEventReceiverGone`), der erbende Zustand ist von außen
nicht herstellbar. **Urteil: begründet offen.** Wer die Fixture je will, muss zuerst diese
Terminalisierung aufweichen — das wäre ein eigener Schnitt, kein Nachtrag.

**L3 · Die dokumentierte Decke war überzeichnet — korrigiert in dieser Sitzung.** Die gelandete
`docs/self-api.md` versprach „spätestens nach `HOLD_BACKOFF_MAX_MS`". Gemessen wurden im Abnahmelauf
**3098 ms gegen eine Decke von 3000 ms** bei `tickMs 250`. Das ist kein Flake, sondern Tick-Quantelung:
angetippt wird nur AUF einem Tick, die echte Schranke ist `Decke + ein Tick + Probe-Latenz`. Der
Check prüft gegen 6000 ms — das Doppelte — und kann die Differenz deshalb strukturell nicht sehen.
Die Doku-Zeile ist nachgezogen; der Check bleibt, wie er ist (enger sollte er laut Lane nicht
werden, 2,9 s Luft im Worst Case).

## Der Blocker, der keiner Lane gehört: der Land WIRKT NICHT

Position 1 verlangt zuletzt „Rate im gleichen Zeitfenster vor/nach nachmessen". Das ist heute
**nicht durchführbar**, und zwar mechanisch:

- `GET /api/sessions` → `deployGap`: `bootHead a10e656d`, `head bb28cc42`, **`codeBehind: true`**.
  Der laufende Server hat K1s `server.ts` nicht.
- Gegenprobe am Trail: `audit.jsonl` trägt **0** Zeilen mit dem neuen `phase`-Feld. Die neue Form
  wurde nie geschrieben.
- Die Baseline steht dagegen fest: **14 188** `fleet_event_held`-Zeilen mit `composer` allein am
  09-11 (23 120 im ganzen Ledger), letzte um 10:01:19.

Ein Deploy ist der Akt des Owners (Übergabe-Regel `4872457b`). **Bis dahin ist K1 gelandet, grün
auditiert — und ohne Wirkung.** Wer die Rate misst, bevor deployt wurde, misst den alten Code.

## Nicht zu verwechseln: der Owner-Fall bleibt offen

K1 dünnt die WIEDERHOLUNG bei belegtem Composer aus (`SendRefused`, vor dem Paste). Die
Owner-Meldung ist `SendNotAccepted` — Fleet hat getippt, der Enter kam nicht an. Seit dem Land eine
dritte Instanz (11:31:53, gleiche Nutzlast, gleiche Pane). Befund und kleinster Folgeschritt:
`docs/messungen/2026-09-11-sendnotaccepted-codex-slot10.md` (`a10e656d`). K1 behebt das nicht und
darf nicht so gemeldet werden.

## Nächster Akt

`288f6359` (K4, audit-red an die Program-Inbox) — seriell, erst nach dem Post-Land-Audit von
`bb28cc42`. Vor dem Release zwei Dinge: der Brief braucht den realen N3-Quellenbeweis für die
angeheftete Quelle `4aeeec19` (`GET /api/self/notes` in der Lane, taskbezogenes Verdict auf
`288f6359`, Quelle → Entscheidung → Datei/Probe im Report, Stempel nach dem Land), und die
`spawn`-Zeile steht auf `codex/gpt-5.6-sol`, während der nachgezogene Brief `claude-opus-5[1m]`/high
verlangt. Beides braucht die Owner-Tür; eine Self-Tür zum Brief-Schärfen existiert nicht (offene
Zeile `3ea89f71`). Danach `92ffd17c` (K2). Audit-Inbox für `f9dc8e10` ja, Audit-Nudge nein.
