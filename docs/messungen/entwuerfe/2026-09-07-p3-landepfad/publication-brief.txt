P3 · SERIELLE OPUS-PUBLIKATION · DOKUMENT, KEINE PRODUKTIMPLEMENTIERUNG

Program 6360c36105e50a705db275c1 (Astra-Tagesmandat P3 — Landepfad adversarial). Lies AGENTS.md Portable operating contract/Verify, dann das vollstaendige akzeptierte Dokument und das unabhaengige Review-Receipt unter /tmp/astra-p3-2026-09-07/. Quellstand der Analyse ist absichtlich c7184f853a3501a4f97e48df3d35e60ed0476b5b (im Dokument gepinnt; nicht aktualisieren). Akzeptierte Draft-Datei: /tmp/astra-p3-2026-09-07/2026-09-07-landepfad-adversarial.md; SHA256 __HASH__. Indexsatz: /tmp/astra-p3-2026-09-07/index-line.txt (genau EINE Zeile, beginnt mit "- "), SHA256 __IDXHASH__. Review-Receipt: /tmp/astra-p3-2026-09-07/independent-review.md, exaktes ACCEPT sha256:__HASH__ lesen.

Exklusiver Schreibscope: docs/messungen/2026-09-07-landepfad-adversarial.md (neu) und genau EIN neuer Satz aus index-line.txt am Ende von docs/messungen/INDEX.md. Du bist nicht allein im Checkout: keine fremden Edits zuruecksetzen, keine weiteren getrackten Dateien anfassen, keine parallele Indexarbeit. Draft-Bytes unveraendert kopieren (cp, dann cmp); nicht formatieren, nicht umschreiben, keine Zahlen/Quellen/Prosa aktualisieren. Ein schon bestehender identischer Indexsatz wird nicht verdoppelt. Bei notwendiger Byteaenderung STOPPE mit konkret benoetigter Korrektur (needs-main): sie braucht erneute unabhaengige Abnahme.

Done: beide Hashes stimmen (shasum -a 256), die Dokumentdatei ist byteidentisch zum Draft (cmp), der Index verweist genau einmal, beide Aenderungen sind von DIR committet (normale Hooks, kein --no-verify), git status --porcelain ist leer, und der Diff enthaelt keine Hostnamen/IPs ausser 127.0.0.1/Tokens/Klarnamen (git grep -inE 'example.com|100\.64\.0\.1' leer).

Verify: lies GET /api/self/gate (self-token; Host literal wie im Regelbuch). Gemaess Owner-Tagesmandat NUR Dokumentbeweis, seriell, Stop beim ersten Fehler: (1) bun install --frozen-lockfile, (2) bun e2e/pins.ts. Originalausgaben in eigene absolute Logpfade ausserhalb des Repos (/tmp/p3-publication-<task>-install.log, -pins.log), Exitcodes sichern; nur den Tail lesen. Kein e2e-*.sh, kein bun server.ts, keine Suite, keine Live-/Host-/Owner-Mutation, kein Land, kein Deploy. Fordert das Gate mehr als install+pins fuer diesen docs-only-Diff, melde den Widerspruch (needs-main), starte keine Suite.

Report via POST /api/self/fleet-report: Git-Commit-SHA GETRENNT vom Datei-SHA256, git diff --stat, cmp-Ergebnis, Indexsatzpruefung (grep -c), beide Original-Logpfade und woertliche Tails (Pins endet ALL PASS), Exitcodes, sauberes git status. Dann warten. Review ist vor der Publikation passiert; du beweist und publizierst genau jene Bytes. Land wartet auf Annahme durch die P3-MAIN und den Controller.
