# Independent review — P3 landepfad-adversarial (2026-09-07)
Reviewer: codex gpt-6-astra (zweite Astra, unabhaengig)
Draft sha256 geprueft: 88746f13ba89b36e58fa0ee936b952f7651ac85763b5dd95e3be5b5c91d2d4e6

## Maengel (nach Schwere; leer, wenn keine)

1. [schwer] §6 — „`verify.out` trägt sie; `runVerify` zählt sie ohnehin für `suiteWait`“ — Quelle sagt: `const VERIFY_OUT_CAP = 2048` (`server.ts:11725 @c7184f8`); die Ausgabe wird durch `retainRunOutput` gekürzt (`server.ts:12107 @c7184f8`). `suiteWait` zählt Lock-Erwerbe, keine PASS-Zeilen: `stages++` (`server.ts:11837 @c7184f8`). Die Note liefert deshalb keine vollständige Untergrenze je Wrapper; eine vorhergehende proportionale Note enthält überhaupt keinen Security-Wrapper-Lauf. Auch die gelesene volle Note am Quell-Pin enthält gekürzte Ausgabe. L6s fehlende Baseline wird weder definiert noch getestet. Kosten: Der vorgeschlagene Schutz kann gerade das entfernte Prüfprogramm weiter grün durchlassen; der Done-Satz ist durch die Skizze nicht gewährleistet.

2. [schwer] §2.5 — „Erst nach erfolgreichem Hub-Push der lokale `resetIntegration`“ — Quelle sagt: Auf dem Holder-Pfad folgen auf `if (cur.out !== mainAfter)` ein separater, asynchroner Reset (`server.ts:3904`, `server.ts:3905 @c7184f8`); ohne Holder erfolgt `git(..., "branch", "-f", main, mainBefore)` ohne aktuellen Erwartungswert (`server.ts:3910 @c7184f8`). Die vorgeschlagene Lease schützt ausschließlich den Hub-Ref. Während dieses zusätzlichen Netzwerkfensters können lokale Lands weiterlaufen; ein Repo-Mutex einschließlich Undo oder eine atomare lokale Ref-Prüfung fehlt. Die Done-Sätze prüfen nur einen fremden Hub-Commit vor dem Undo. Kosten: Die Reparatur lässt ein Überschreibfenster für zwischenzeitlich gelandete lokale Commits offen.

3. [schwer] §5 — „Repro-Skizze, deterministisch über den vorhandenen Latch“; „beide Lands haben Note oder Verdict“ — Quelle sagt: Ohne vorhandene Latch-Datei kehrt die Funktion sofort zurück (`server.ts:11788 @c7184f8`); `.reached` enthält nur einen Zeitstempel (`server.ts:11789 @c7184f8`). Der gemeinsame Release liegt vor `advanceIntegration` (`server.ts:18694`, `server.ts:18695 @c7184f8`) und kontrolliert weder den Gewinner noch den Kill zwischen Advance und Record. Dafür existiert ein anderer Test-Hebel: `LAND_PAUSE_MS` (`server.ts:11765`, `server.ts:18764 @c7184f8`). Zudem persistiert jeder Job schon vor der Rebase `status: "interrupted", landed: false` (`server.ts:18349`, `server.ts:18351 @c7184f8`). „Note oder Verdict“ kann somit trotz verlorenem Land-Record erfüllt sein. Kosten: Der Abnahmetest kann den defekten Stand passieren lassen. Er muss den tatsächlich bewegten SHA und dessen vollständige Wiederherstellung nach einem kontrollierten Abbruch prüfen.

4. [mittel] §1.3/§1.6 — „die Kette ist gespawnt“; „Der zweite Commit lag vor dem Spawn — … ausgeschlossen“ — Quelle sagt: `gateRun(() => holdSuiteLock(VERIFY_WAIT_MS))` veröffentlicht dieselbe „land gate“-Zeile bereits während des Wartens (`server.ts:18541 @c7184f8`); der eigentliche Verify folgt erst bei `server.ts:18549 @c7184f8`. Das Signal beweist für volle Gates keinen Spawn. Der Skript-Exit prüft außerdem nur den Fehlerzähler für `main == C2`; Note, Audit-Kettenwahl und rote Nachprüfung werden lediglich ausgegeben. Kosten: Exit 0 ist kein automatischer Nachweis aller beanspruchten Repro-Eigenschaften. F1s Codebefund bleibt bestehen.

5. [mittel] §1.3 — „inhaltsblind wie das echte `pins.ts` gegenüber Servercode“ — Quelle sagt: `const serverU = universe("server", ["server.ts", …])` (`e2e/pins.ts:111 @c7184f8`); beispielsweise wird der proportionale Befehl aus diesem Quelltext gelesen und geprüft (`e2e/pins.ts:1135`, `e2e/pins.ts:1148 @c7184f8`). Nicht ausführen ist nicht gleich nicht prüfen. Die stärkste Alternative für Variante B ist ein regulärer Fixture-Kurzkettenlauf im veränderlichen Worktree, dessen späteres PASS auch mit C2 auf Platte erwartbar ist. Das Transkript schließt diese Lesart nicht aus. Es belegt die übernommene Kettenwahl, keine allgemeine Inhaltsblindheit der echten Kurzkette. Kosten: Die Fixture wird als stärkerer Stellvertreter dargestellt, als sie ist.

6. [mittel] §8 — „eine der drei `AGENTS.md`-Überschriften … rot im Fleet-Frame-Check“ — Quelle sagt: Der Pack mit `## Landing` hat `triggers: ["landing"]` (`context-packs.ts:130`, `context-packs.ts:133 @c7184f8`); die Gründung verwendet `["always", "verification"]` (`server.ts:18896 @c7184f8`). `anchorsResolve` prüft nur selektierte Packs (`e2e/programs.ts:639 @c7184f8`). Der angeführte Fleet-Frame-Check deckt somit nicht alle drei Überschriften. Kosten: Für `## Landing` wird ein konkreter roter Folgetest behauptet, den der angeführte Mechanismus nicht liefert. Die sieben Anker sind korrekt gezählt; ihre nachgewiesene Testabdeckung ist kleiner.

7. [mittel] §4.1 — „Kein Surface meldet es“ — Quelle sagt: Der Schreibfehler wird einmal an `logError("eventLog", e)` weitergegeben (`server/persist.ts:30 @c7184f8`). Dieser Fehlerkanal führt sichtbare Fehlerzustände (`server/errors.ts:64 @c7184f8`); `/api/sessions` liefert `errors: errorsView()` (`server.ts:25051 @c7184f8`). Richtig bleibt: Der Audit-Aufrufer erhält keine Ablehnung und kann Covers verbrauchen. Kosten: Das Dokument verwechselt fehlende transaktionale Fehlerbehandlung mit vollständiger Unsichtbarkeit und übersieht einen vorhandenen Diagnosekanal.

8. [mittel] §4.4 — „`appendEvent` bekommt eine Variante … (`appendEventStrict`)“ — Quelle sagt: `export function appendEventStrict(...)` existiert bereits (`server/persist.ts:39 @c7184f8`) und ist importiert (`server.ts:99 @c7184f8`). Außerdem veröffentlichen beide Audit-Pfade `lastPostLandAudit = row` vor dem Append (`server.ts:14055`, `server.ts:15223 @c7184f8`). Ein bloßer Wechsel auf den strikten Append plus späteres Splice definiert weder den Fehler-/Retry-Pfad noch den Umgang mit dieser vorzeitigen Ergebnisveröffentlichung. Kosten: Der Done-Satz „nach Reparatur entsteht die Zeile“ verlangt Fortschritt, dessen Auslöser die Skizze nicht festlegt.

9. [mittel] §2.2 — „Die 344 gedroppten Einträge sind die zweite Todesart: ein Direkt-Commit auf main“ — Quelle sagt: `n` wird für jeden Drop-Grund addiert (`server.ts:12408 @c7184f8`), auch bei Überschreiten der Stack-Kapazität (`server.ts:12436 @c7184f8`) und bei `killUndoStack` (`server.ts:12447 @c7184f8`). Selbst der jüngste Gap kann laut Code auch durch konkurrierende Lands entstehen (`server.ts:12417 @c7184f8`). Der folgende Hinweis auf den kumulativen Zähler korrigiert die vorausgehende Ursachenzuschreibung nicht. Kosten: Ein bestätigter Umfang wird ohne Ursachenhistorie als Beleg für eine bestimmte Todesart verwendet.

10. [mittel] §3.4 — „Ein RED-Audit auf dem Tip blockt … (`lastPostLandAudit.mainSha === target …`)“ — Quelle sagt: `lastPostLandAudit = row` wird repoübergreifend überschrieben (`server.ts:14055`, `server.ts:15223 @c7184f8`). Ein späteres Audit eines anderen Repos verdrängt damit das relevante Rot. Die vorgeschlagene Bedingung sucht keine zum Deploy-Repo und Ziel passende Zeile. Kosten: Der versprochene RED-Blocker bleibt umgehbar; der Done-Satz mit nur einem RED-Tip entdeckt das nicht.

11. [mittel] §3.4 — „marker older … — a boot in between failed“ — Quelle sagt: Die Markerzeit wird beim Schreiben nach dem Build gesetzt (`server.ts:23337 @c7184f8`); das Altersbudget ist eine Konstante (`server.ts:23083 @c7184f8`). Daraus folgt kein beobachteter fehlgeschlagener Boot: Auch verzögerter Restart oder eine Unterbrechung des Rechners sind damit vereinbar. Kosten: Die Reparatur würde aus fehlender Zeitnähe eine ungemessene Fehlerursache erzeugen. `ok:null` wegen eines alten Markers ist begründbar; die behauptete Ursache nicht.

12. [mittel] §1.7/§7.2/§7.3/§10–§12 — „Schritt 3 als Folgezeile“; „Vorschlag … kein Done-Satz“ — Quelle sagt: Das Gate liest weiterhin aus `cwd` (`server.ts:12012 @c7184f8`); ererbte Holds werden pro Job ausgewertet (`server.ts:18536 @c7184f8`), und die sofortige zweite Hold-Absage bleibt `false` (`server.ts:17491 @c7184f8`). Für den Snapshot-Schritt fehlt ein eigener Uncommitted-Gegencheck; L7b/L7c und der zusätzliche Event-Bindungsvorschlag haben ausdrücklich keine falsifizierbaren Done-Sätze. §12 benennt zudem nicht die unvollständige PASS-Baseline, die nicht kontrollierte Kill-Reihenfolge und die Grenzen des Repro-Exitcodes. Kosten: Nicht überprüfte Reparaturteile erscheinen in der Schnittliste als übergabefähige Arbeit.

13. [leicht] §1.1/§2.2/§7.1/§8 — weiterhin ungenaue Zeilenangaben — Quelle sagt: Der frische Confirm-Verify wird bei `server.ts:17828 @c7184f8` aufgerufen; `17819` ist Kommentar. `pushLandToHub` liefert `ok:false` bei `server.ts:12637 @c7184f8`; `12634` wartet auf den Exitcode. Das Shell-`mkdir` steht bei `e2e-stage.sh:267 @c7184f8`, nicht 268. Die Manifest-Blob-Lesung steht bei `server.ts:19018 @c7184f8`; `19021` ist leer. Kosten: Die behauptete vollständige Zitatreparatur ist nicht erreicht.

## Geprueft und korrekt (nur, was du wirklich gelesen hast)

- Dokument vollständig gelesen; SHA-256 vor und nach der Prüfung identisch.
- F1: Der gelesene Clean-Pfad bindet keinen vor dem Gate gemessenen Lane-Tip an den späteren Branch-Advance. Der Confirm-Kontrast enthält zusätzliche Identitätsvergleiche, lässt aber ebenfalls ein anschließendes Fenster.
- Die proportionale Audit-Auswahl übernimmt die Cover-Stempel ohne erneute Diff-Klassifikation.
- F2: Auch die beiden fail-closed-Fehlertexte von `remoteHoldsLandedRange` führen in der Undo-Route zum Löschen des Stacks.
- Nachgerechnet: **482** Notes mit Verify, **396** mit Zeitinformation, **7** zeitliche Treffer; alle sieben menschlich bestätigt, davon drei rot, zwei `waitedOut`, zwei grün mit `stale:true`. **60** Notes sind proportional.
- Über die letzten 60 Commits am Quell-Pin: **14** Land-Notes, **14** erfolgreiche Hub-Pushes, keine fehlenden oder fehlgeschlagenen Push-Ergebnisse.
- Erlaubte Zustandsfelder geprüft: Stack-Tiefe **1**, Drop-Zähler **344**. Angegebener Bereich: **1** Commit, **0** außerhalb sämtlicher Remote-Tracking-Refs. Die Contains-Ausgabe hat zwei Zeilen: einen Branch und dessen symbolischen HEAD-Verweis, nicht zwei unabhängige Branches.
- Die Größenordnung der Gate-Arbeitsmediane seit Septemberbeginn ist nachvollziehbar: ungefähr eine Sekunde proportional und 110 Sekunden vollständig; Arbeitszeit bedeutet hierbei `ms - waitMs`.
- F3: Deploy bindet den normalen Zielpfad nicht an Land-Note oder Audit. Die vollständige Blocker-Menge umfasst reservierte/laufende Lands sowie lokale Audit-Aktivität. Beim Boot fehlt eine Altersprüfung; nach dem Build fehlt eine erneute Blockerprüfung.
- F4: Laufzeit-Tip, unveränderte Covers, optionales `clonedSha` ohne Einfluss auf das Audit-Verdikt, Remote-Verbrauch vor Append und mögliche grüne Zeile mit `ran:0` sind bestätigt.
- F5s repoweise Intent-Map hat keinen Belegtheitsguard. F6s Gate führt Kandidatendateien aus.
- F7.1: Eine fehlgeschlagene erste Dateischreibung kann ein PID- und Birth-loses Verzeichnis hinterlassen; beide gelesenen Reaper behalten diesen Zustand.
- `verify-proportion.ts` vollständig, `e2e-stage.sh:1–330` vollständig gelesen. Rename-Klassifikation, Ticket-Tie-Break und vorgezogene Birth-Ermittlung stimmen.
- Sieben Seed-Anker in fünf DOC_RULE-Dateien bestätigt; Auswahlregeln, zitierte Ankerprüfungen und relevante Pins gelesen.
- Der angegebene Quelldiff vom Pin zum während der Prüfung vorhandenen HEAD war leer.
- Hygieneprüfung: Drei Suchmuster ohne Treffer; das Auth-Header-Muster trifft in Dokumentzeilen 240 und 242 auf variable Vorlagen. Dort steht kein konkreter Credentialwert.

## Nicht geprueft

- Keine Ausführung des Repro-Skripts, keine Suite, kein Serverstart, kein Deploy, kein Undo und keine Fehlerinjektion. Keine Dateien geschrieben.
- Ursprüngliche Rohartefakte und tatsächlich ausgeführte Skriptbytes des behaupteten Scratch-Laufs; geprüft wurden eingebettetes Skript und Transkript.
- Der Originaltext der Ablehnung von 07:31 mit sechs Mängeln lag nicht vor. Die vorhandene frühere Ablehnung mit 19 Punkten ersetzt diesen Abgleich nicht.
- Live-Konfiguration, Prozessumgebung, tatsächliche Remote-Serverstände, historische Hub-Konfigurationen und Ursachenhistorie der Drops.
- Vollständige Implementierungen sämtlicher Wrapper, Harnesse und Konfliktpfade; keine vollständige Repoabdeckung durch die gezielten Quellenlesungen.
- Experimentelle Auswirkungen einzelner Ankeränderungen, produktive Häufigkeit der Race-Fenster und Verhalten bei Dateisystemfehlern, Signalunterbrechungen oder PID-Recycling.

## Verdict

REJECT — Die nicht tragfähige L6-Baseline, die unvollständige lokale Absicherung von L2 und der nicht deterministische beziehungsweise zu schwache F5-Abnahmetest verhindern die Annahme.