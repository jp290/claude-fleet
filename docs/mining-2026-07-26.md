# Ledger mining, 2026-07-26 — what the data says before the prompt work starts

The agreed program (owner, 2026-07-26 evening): mining first, then two prompt sites — the
Rundgang's refeeding and ②'s enrichment. This is the mining result. Everything below was
recomputed from the ledgers and the code this evening; nothing is quoted from HANDOFF memory.

Recompute (HANDOFF §1 block, run 2026-07-26 ~21:40):
`rows 51 | K1 41/20 | clean 38/10 | unknown 0 | undos 0 | K2 25/25 | raw 8 | would_stop EVER 0`

## Findings, ranked

### 1. The tier-2 audit silently missed the four newest lands — the deploy ritual kills it

The four `data-saver-*` lands (18:31–18:37) have **no post-land audit rows and never will**:
`auditQueue`/`auditDraining` are in-memory (`server.ts`, `schedulePostLandAudit` →
`drainPostLandAudits`), and `server.log:683` records `18:37:52 [watchdog] srv was down,
restarted` — seconds after the fourth land. A srv death destroys every queued/pending audit;
the restarted server has no knowledge of them and nothing re-audits on boot. No row, no
"unknown" marker — the gap is indistinguishable from "nothing landed."

The likely cause is the **documented deploy flow itself**: land server-touching work → `tmux
kill-session -t srv` → watchdog respawn. Land-then-immediately-deploy is the *common* case for
server.ts lanes, and it races the audit every time. (Inferred: the log only says "srv was
down"; the coincidence with the standard ritual is not proven.)

Cost: the post-hoc sensor that the half-autonomy trial leans on skips exactly the lands that
precede a deploy — the highest-stakes ones.

### 2. Both red audits were never followed up — sensor fired, nobody looked

`post-land-audits.jsonl`: 9 green, 2 red. The reds (15:57 `…7ebd` 6 FAILURES, 16:05 `…947c`
1 FAILURE) are the first reds ever recorded. As of tonight: kept test instances still sit
untouched in TMPDIR (`fleet-e2e-instance-62913`, `-10113`), no serial same-tree re-run was
done (the required flake proof, CLAUDE.md/verify-tiering §11.7), and `main` has since been
built on. Differing failure counts on consecutive runs match the load-noise signature — audit
#1 was still running while the `…947c` land + its ② review executed — but per house doctrine
these fails are **ours until proven flake, and nobody ran the proof**.

Why nobody looked (finding 5): the results live on a surface no ritual reads.

### 3. ② has never seen a non-degenerate input — all 25 passes are the same sentence

Every valid shadow verdict rests on one argument: *"main gained zero commits since the fork,
so there is nothing to collide with."* All 25. The condition is **deterministically
computable** (`base..main` empty — runCleanReview already computes exactly this at
server.ts:3579 and hands it to the model as prose).

Consequences:
- K2 25/25 is now met (new vs. HANDOFF §1's snapshot), but it measures availability on a
  degenerate input distribution. `would_stop EVER = 0` has never been a choice — ② has never
  been shown a land where stopping was arguable.
- Discrimination cannot be measured on production traffic at all until a lane lands while
  main has moved. Only the seeded drill can measure it. Enrichment work must not claim
  production evidence either way.
- Routing consequence for the prompt work: a deterministic `main-unchanged-since-fork`
  fast-path would have covered 25/25 of the actual history without any model call. ②'s seat
  is only earned where that predicate fails — which is exactly the enriched-prompt case
  (concurrent-lane picture). The enrichment target is narrower and sharper than "make ②
  smarter": it is "make ② good at the only case that reaches it."

### 4. The tool-call kill-signal does NOT fire — ② demonstrably reads the repo

The earliest cheap falsifier for context-enrichment ("does ② ever look?") is answered by
existing rows: 4 of the 8 `raw:true` rows carry substantive `rawAnswer`s — *"Confirmed: only
fleet-e2e.ts consumes this endpoint's shape, and both files were changed together in this
lane"*, *"merge-base equals main's HEAD"*. That is real Read/Grep and real git, matching its
tool floor (`REVIEW_TOOLS`, server.ts:2713 — anchored Read/Grep/Glob + git status/diff/log).
Feeding it more context is not stocking a pantry for a sensor that doesn't eat.

Correction of this session's own earlier claim ("two failure modes"): the 8 raw rows split
**3 / 4 / 1** — 3 undiagnosable (pre-`rawAnswer` schema), 4 contract-missed *with substance*
(the pre-carve-out parser bug; today's parser at server.ts:3608 would pass all 4), and exactly
**1** true empty answer in production (`post-land-audit` row). Production empty-rate 1/33 vs
the drill's 2/2 → `cc913fe1` is most likely drill-context-specific, as judge-calibration.md
suspected. The production reviewer, post-fix, is 23/23 valid.

### 5. The refeeding gap is confirmed and specific: the Rundgang reads none of the ledgers

`GET /api/steward/digest` returns: prior journal record, slots view, `sinceLastLook`,
`deployGap`, `bundleStale`, `continuity`, worker digest (server.ts:5094–5112). It carries
**no** outcome rows, no shadow verdicts, no dispositions, no post-land audit results — and
`rundgang.md` instructs "one call gathers everything." So the two red audits (finding 2) were
structurally invisible to the only unprompted-attention ritual in the house. The routes exist
(`/api/steward/outcomes`, `/api/post-land-audits`); nothing consumes them.

### 6. Path-zone reality check: server.ts is in 29 of 46 measured lands

`filesTouched` frequency (46/51 rows carry it): server.ts 29, fleet-e2e.ts 12, src/client.ts
10. An allow-zone ("these paths may auto-land") would cover only the ~10 docs-only lands.
Zones remain useful only as deny-list (paths that always stop) — as necessary condition,
never sufficient. This settles the zone question with numbers instead of design taste.

### 7. Housekeeping observed in passing

- A stray `bun server.ts` from 2026-07-21 still runs out of a dead session's scratchpad
  (`…561a/…/scratchpad/enhtest`, pid found via lsof) — not on the live port/socket, harmless,
  should be killed. The third bun process is the separate dexter project. No duplicate fleet
  server exists.
- `dispositions.jsonl` has exactly 1 row since 2026-07-25 — the disposition rail is live but
  essentially unused so far.

## What this means for the two prompt sites (the agreed next steps, unchanged)

1. **Rundgang refeeding** (finding 5, fixes finding 2's recurrence): the pulse must consume
   post-land audit results and the outcome ledger delta since the prior journal record — new
   audits (result + covers), new shadow verdicts, new lands. Whether that enters via the
   ritual text (extra GETs with the steward token) or via the digest payload is an
   implementation choice; the ritual text edit is the zero-server-change start.
2. **② enrichment** (findings 3+4): lane brief + concurrent-lane picture, per
   supervision-decomposition.md — but aimed at the non-degenerate case only, with the
   deterministic `base..main`-empty fact stated as fact, not re-derived by the model. Honesty
   condition stands: no tuning against a fixture whose verdict has been seen; production
   traffic cannot measure discrimination (finding 3).

Not part of the two sites, but named because the data demands it: finding 1 (audit queue
dies with srv) is a **code** gap, not a prompt gap. The trial's "we'll see what breaks"
already broke here — twice reds unread, four audits vaporized — before any autonomy was
added. It should be queued as its own lane brief, not folded into the prompt work.

## Verified / inferred

Verified by reading code or rows tonight: runCleanReview + shadowOf (server.ts:3575–3639),
REVIEW_TOOLS (2713), recordLand → schedulePostLandAudit (2799–2813), the in-memory audit
queue (2886–2911), tickDispatch spawns-and-briefs-only (1535–1589) and mergeJob's single call
site (6112, the owner route), the digest payload (5068–5112), rundgang.md's data plane, all
ledger numbers above, server.log:676–691, watchdog.sh:97.

Inferred, not proven: that the 18:37:52 srv death was the deploy ritual; that the two reds
are load-flakes (the serial same-tree proof was never run — they are formally still ours).
