# Product Studio as a Fleet working-circle profile

**Status:** owner-reactivated working-circle program, 2026-08-16. The Program/Origin,
ContextPlan/receipt, target-repo MAIN-bootstrap, succession, execution-view and clarification
foundations now exist, and private-repo-h has supplied the first real production-and-tasting
trail. Private-repo-h Visual Act 1 has since proved reproducible capture and real Fable image consumption;
the owner moved the cut line before its Builder pass. The current order is communication truth,
repo-local context and measured harness routing, then one small artillery calibration game.

Owner direction that reactivated the program: *"lass uns diese Idee nun festhalten, uns einen
Überblick verschaffen was alles sonst noch so ansteht und was wir schon getan haben, und dann
alles sauber angehen."* This document records direction and order. It is **not** a second work
register: `state.sh`, `register.sh`, the queue and the current product repository remain the live
facts.

## The idea in one sentence

Claude Fleet should be able to run a durable, nearly autonomous **Product Studio working
circle** whose specialized roles, tools and session context are composed for a concrete product;
the owner remains the source of direction and taste, while Fleet handles preparation,
production, integration, proof, iteration and maintenance.

The first goal is not a universal studio. It is the smallest credible product experiment that
can earn money, while leaving behind reusable studio capability even if the product is simplified
or changed.

## Why the hypothesis is active now

The old reactivation gate was: real Program/Origin artifacts, inspectable Context Plans and a
target-repo MAIN bootstrap, or an explicitly started product experiment. Both sides are now true.
Fleet can preserve owner intent, found and succeed a Program-MAIN in its target repository, deliver
and receipt context pointers, reconstruct execution, carry worker clarifications, and observe the
lane/merge/audit/deploy lifecycle. private-repo-h then exercised that spine through a complete
W0-W7 slice and an owner tasting.

The tasting exposed the missing layer rather than invalidating the spine. The build was functional
and its workers largely followed the promoted plan, but the owner found it underwhelming: the robot
moved too quickly, the sensor read too small, the UI looked weak, the whole presentation felt
desolate, and the path from this slice to an impressive game was not visible. A 3D or 2.5D direction
might have helped, but that is a hypothesis to test, not a migration to infer.

The plan had already named Canvas2D as a reversible product hypothesis. Its structural mistake was
**when** that hypothesis met taste: visual legibility was marked owner/visual while the only owner
gate sat after W7. Reversibility did not recover the time spent proving the wrong representation.

## Current truth at the cut line

| Layer | Today | Honest boundary |
|---|---|---|
| Program / Origin | Built and used by private-repo-h. Its W0-W7 Program is complete; Visual Loop v0 is a separate active Program. | Owner promotion remains the only source of binding intent. |
| ContextPlan + delivery receipt | Built as a fresh projection with immutable delivery evidence. | Packs still point only into Fleet. A target-repo Program-MAIN now omits them honestly; foreign task dispatch still forces Fleet context. |
| Program-MAIN lifecycle | Target-repo bootstrap and program-aware succession are built. | A profile-specific Director contract is not built. |
| Harness continuity | Codex automatic and attended conversation recovery are built. | Equivalent continuity is adapter-specific, not a universal session claim. |
| Program execution and help | ProgramExecutionView and worker-to-MAIN clarification are built on existing facts/events. | MAIN-to-owner attention is not typed; clarification has three named transport/view edges; `done-looking` is not semantic completion. |
| Harnesses | Claude, Codex, Pi and Pi/Z.ai can be selected; model, effort and provenance travel. Fable consumed the Private-repo-h PNGs through a real image tool result. | Multimodality is not yet a declared/routable Fleet capability. Pi/GLM has no image-path canary and remains attended-only. |
| Product experiment | private-repo-h W0-W7 is startable and technically proved on its promoted plan. | The owner rejected its experiential quality; green technical proof does not reverse that judgment. |
| Game-quality loop | Private-repo-h Act 1 proved reproducible named-shot capture and actual multimodal Fable diagnosis on a clean product tree. | Builder, blind A/B Critic and owner disposition are paused; one diagnosis does not yet prove an improving loop. |
| Learning loop | Work trails, receipts and outcomes carry much of the join substrate. | No repeated analysis promotes game-quality lessons or pack changes. |

Primary-source research sharpened, but did not replace, the local evidence:

- [Raccoon Heist](https://github.com/simonw/raccoon-heist) has a public transcript in which
  screenshot reads return actual `image/png` tool results and lead to camera, lighting and mobile
  revisions.
- [Claude of Duty](https://github.com/mshumer/Claude-of-Duty) makes named shots and reproducible
  capture public; its pixel diff proves only regression/neutrality, never aesthetics.
- [abagames' one-button workflow](https://github.com/abagames/claude-one-button-game-creation)
  makes game-feel skills, fresh per-game context and browser smoke checks public while leaving
  ranking and publishing to the human.
- The public GLM one-shot examples support a narrow-prompt hypothesis, not a durable harness claim;
  without an API transcript, model identity and single-pass provenance remain creator claims.

## The missing organizational layer

The proposed relationship is:

```text
Program / Origin artifact
  what we want, why, success, non-goals and owner decisions
        -> Working-circle profile
  how this kind of work is organized: roles, tools, quality paths, owner touchpoints
        -> Context Plan
  the smallest relevant context + capabilities for this session and act
        -> MAIN / direct work / specialist worker
  execute the smallest sensible path
        -> Outcome + Work Trail
  result, proof, detours and proposed context/rule improvements
```

`Program` says **what and why**. The working-circle profile says **how this kind of work is
organized**. The Context Plan says **what this particular participant needs to know and use**.

## Compose profiles; do not fork studios

`game-studio`, `private-repo-p`, `roblox-studio` and `steam-studio` are likely mostly the same
organization. Avoid separate monoliths and the later cross-product of `ios-game-studio`, etc.
Compose thin dimensions over one Product Studio core:

| Dimension | Examples |
|---|---|
| Domain | game · app · tool |
| Platform/runtime | web/Three.js · iOS · Roblox · Steam |
| Phase | discovery · production · release · maintenance |
| Role | creative · research · context · code · art · audio · review · release |
| Quality/touchpoint | technical · visual · audio · gameplay · store · owner tasting |

A small Three.js game is therefore Product Studio + game + web/Three.js + the roles and quality
paths its current phase actually needs. A later iOS game can reuse game knowledge while swapping
the platform layer.

## Roles are modes, not a permanent agent army

Potential specialist roles include:

- adaptive MAIN/producer: keeps the program coherent and chooses direct work, worker, lane or job;
- research worker: closes a bounded knowledge gap and reports evidence and unknowns;
- context curator: proposes the relevant packs, anchors, tools and omissions for a Context Plan;
- creative director: protects product identity and translates owner tasting into usable direction;
- code/gameplay, art and audio workers: produce and integrate their respective artifacts;
- visual/audio reviewers and playtest workers: inspect the actual build, not only source or prose;
- release worker: platform builds, store artifacts and release checks;
- learning curator: proposes small pack/rule improvements from repeated Work-Trail evidence.

Fleet instantiates only the roles needed for the current act. A role is primarily a context,
capability and output contract; the same harness may serve several roles at different times.

For coupled visible work, roles are ordered rather than fanned out indiscriminately:

```text
owner-confirmed direction -> Director -> one Builder pass -> fresh blind Critic -> Owner taste
```

The Director owns coherence. A Builder owns an exclusive writable surface. A Critic begins fresh
and read-only. The owner decides promotion and stop. Parallel proposals are useful before a
direction is chosen; parallel mutations to lighting, camera, UI and tone are not.

## Smart session enrichment: information layers

Do not dump the whole project into every session. A Context Plan should select across layers:

1. **Owner/program:** intention, success, non-goals, taste and confirmed decisions.
2. **Domain:** game/app principles and maintained external knowledge.
3. **Project:** architecture, gameplay pillars, art direction, asset and system contracts.
4. **Task:** exact done criterion, affected surfaces, dependencies and proof path.
5. **Role:** only the discipline and examples needed by this specialist.
6. **Live facts:** current HEAD, concurrent work, collisions, harness capabilities and events.
7. **Learning:** relevant prior outcomes, recurring detours and promoted lessons.

Specialist agents may **propose** enrichment of these layers. Context Packs remain focused,
versioned references selected for a reason; they are not a growing universal prompt. Noise and
inspiration that never became a decision stay out.

## Owner tasting is a first-class production loop

The owner is not removed from questions of identity, feel and commercial judgment. Fleet should
bring small, real, playable/usable increments to an explicit tasting point:

```text
real production slice -> owner tastes the build -> feedback is interpreted
  -> concrete decision / direction / task -> targeted iteration -> taste again
```

Informal feedback such as "generic", "movement feels cheap" or "keep exactly this explosion"
is useful input. A creative/context role translates it into explicit product decisions, positive
and negative examples, or proposed project context. It must not silently promote one reaction into
a universal rule.

Technical agents may help prepare screenshots, video, audio comparisons and playtest evidence,
but an AI judging AI output is not the sole proof of taste.

## First proving slice: Visual Loop v0

The first slice changes no Fleet schema and builds no general harness. It uses private-repo-h as a
known fixture, not as a commitment to rescue that product.

1. The owner confirms a short `VISUAL_DIRECTION.md`: intended machine fantasy, motion/weight,
   sensor readability, UI hierarchy, desired and forbidden mood, plus any reference images.
2. The game repository exposes one reproducible `run-overview` state. A capture produces a short
   clip for human motion judgment, a fixed key-frame/contact sheet for models, one still image, and
   a manifest containing repo HEAD, seed, viewport, frame indices and content hashes. Capture
   artifacts remain outside the repository.
3. A Fable Director demonstrably receives the images as multimodal content and returns a closed,
   provider-neutral diagnosis. It may recommend `keep-2d`, `test-2.5d` or `test-3d`, but never starts
   a renderer migration.
4. Exactly one visual Builder pass owns the named rendering/UI surface. It does not add gameplay,
   progression, audio, content volume or a new engine.
5. The identical state is captured again. A fresh Fable Critic sees anonymized A/B bundles and the
   confirmed direction. The owner makes the final A/B disposition.
6. No clear owner preference means discard the candidate lane. A preference for B permits one
   ordinary product commit; it does not by itself promote a general Fleet rule.

The critic response is deliberately solution-aware but authority-poor:

```json
{
  "observations": [],
  "primaryCause": "tuning | visual-direction | ui | dimensionality | core-concept",
  "recommendation": "keep-2d | test-2.5d | test-3d",
  "confidence": "low | medium | high",
  "evidence": [],
  "smallestExperiment": "",
  "mustNotBuildYet": []
}
```

A 3D recommendation must name the visible problem it would solve, its expected production cost and
the smallest isolated scene that could falsify it. It creates a proposal, never a migration task.
Pixel diff has one role only: prove capture reproducibility or detect an unintended change. An ugly
image can be bit-identical; an improvement must often break the diff.

**Visual Loop v0 is done** only when the same-tree capture repeats, image delivery to Fable is
observed, the single candidate pass is recaptured, the fresh critic returns the schema, and the
owner records A/B/indifferent. Technical tests continue to prove the game tree; they do not prove
the taste outcome.

### Measured Act 1 and the new cut line

Act 1 ran on private-repo-h `3105685` without changing its repository. A Fable 5 Program-MAIN
used two fresh browser loads of `run-overview@f120`; both full-page PNGs and both canvas hashes
matched byte-for-byte. Fable then received the PNGs as image tool results and diagnosed the scene,
rather than reasoning from filenames or prose. Its primary cause was `visual-direction`, followed
by `ui`; it recommended `keep-2d`, not a dimensionality experiment. The proposed direction was
"aufgeraeumter Werkhof, warm und physisch". `bun test` remained 89/89 and the product tree clean.

The evidence bundle is deliberately session scratch, not a product artifact yet. It proves that
the capture and image-consumption path can work; it does not prove a reusable command, a better
candidate, or a quality-improving loop.

The owner accepted this as a useful first attempt but changed the order before authorizing the
Builder: first make the communication between roles dependable, then establish the profile,
context and harness routing well enough to attempt a small polished artillery game. The Private-repo-h
Visual Program remains paused at its direction gate. No visual candidate is built merely to finish
the diagram.

## Communication before more orchestration

"Agent communication" is three different contracts. Treating all three as pane text is the
failure:

| Contract | Current truth | Required next truth |
|---|---|---|
| Work and artifacts | Program, Task, brief, Context Receipt, Outcome and ProgramExecutionView already carry most exact joins. | Keep these as the durable work path; chat must never become a second task or artifact store. |
| Worker -> Program-MAIN question | `clarification-request` is built on the existing FleetEvent transport with server-stamped worker/task/origin/program and exact receiver identity. | Close its three named edges: send-uncertain on reply; owner `/send` must not erase `awaiting:"main"`; steward visibility must follow role rather than `worktree`. |
| Program-MAIN -> owner attention | Not built. Today the owner or this supervisory session notices pane prose manually. | One closed request type for `decision`, `blocked` or `review-ready`, bound to exact Program and MAIN occupant, durable, deduplicated and visible without typing into the owner's composer. |
| Owner/supervisor -> Program-MAIN answer | Raw `/send` reaches a pane but its journal row lacks `openedAt`, `sessionId` and a send ID; success means PTY send, not model consumption. | Idempotent reply to the exact attention request, with a typed receiver receipt and honest `send-uncertain`; never replay an unknown mutation. |
| Operational outcomes | Lane/merge/audit/deploy already become typed, ackable FleetEvents. | Show the small owner-relevant subset in the same attention surface; do not create a second bus or make pane injection the source of truth. |
| Semantic completion | `done-looking` classifies git/idle facts only and correctly cannot know whether a lane is done, in a suite or waiting. | Worker reports remain advisory but typed as `review-ready`/`blocked`; MAIN reads the report plus tree/proof before acting. No pane parser. |

### Minimal communication program

Run these as narrow landable cuts, with a stop after each:

1. **Clarification correctness.** Add reply `send-uncertain`, preserve `awaiting:"main"` through
   unrelated owner sends, and make the steward's request view role-correct. Existing successful
   behavior and all legacy rows stay unchanged.
2. **Program-MAIN attention.** Extend the existing clarification/Event machinery with the exact
   mirror needed by a bound Program-MAIN: closed reason, short question/report, Program ID and
   sender occupant stamped by the server. It reaches an owner inbox/poll fact first, not a pane.
3. **Exact owner reply and send receipt.** The owner answers a request ID; delivery targets the
   same MAIN occupant or fails. `/send` separately returns and journals `sendId`, receiver slot,
   `openedAt`, `sessionId` and whether Enter was submitted. This attests PTY transport only.
4. **Thin owner/supervisor surface.** Render unresolved attention and important red/terminal
   operations once, with acknowledge/reply. A supervisory Codex session may consume that same
   typed projection, but it is not made the authority by being automated.

Done is an end-to-end counter-probe: a worker questions MAIN, MAIN answers; MAIN requests an owner
decision, the exact owner reply returns; slot recycling, restart, busy panes and a partial send
cannot misroute, duplicate or silently close either request. No generic inter-agent chat, no
event-sourcing, no second queue and no automatic approval.

## Harness routing, including Pi

Harness choice is part of the experiment, not an identity claim about a model:

| Role in the first calibrated studio run | Default | Why / boundary |
|---|---|---|
| Owner-side supervisor | the current Codex conversation | Maintains the cross-program view and asks the owner only for real decisions; typed facts must replace manual pane watching. |
| Program-MAIN / integrative Director | Fable | Strong long-context synthesis and the image path are locally proven. Use it for direction and integration, not every cheap inspection. |
| Gameplay/technical Builder | one Opus or Sol lane chosen by the MAIN for the bounded slice | One writable owner for coupled code. Choice is recorded so model, harness and context are separable later. |
| Visual Builder | one capable Claude/Codex lane after a direction gate | Serial ownership over camera/render/UI/feel surfaces; never several visual agents editing the same scene. |
| Fresh visual Critic | Fable for the first proof; Pi/Z.ai only after H0 | The first must be known-good. Fable is not assumed best or cheapest at vision merely because it worked once. |
| Small aggregation | Spark workers already routed for summary/digest/commit-message/enhance | Useful for bounded compression and bookkeeping, never taste or promotion. |

Pi should enter through the already built `pi-zai` adapter, not through a new provider wrapper. Its
GLM-5.3 coding/tool/session path and architecture reasoning are measured, but `automatable:false`,
no Fleet transcript worker channel, and no image canary are current facts. The first Pi act is
therefore an attended, read-only **vision H0**:

1. Feed two randomly named, near-identical PNGs with one known visual difference.
2. Prove exact `zai/glm-5.3`, actual image tool results and the expected image hashes; no filename
   clue and no mutation.
3. Require the same closed diagnosis schema and a loud unsupported/format/provider failure.
4. Then let GLM and Fable judge one anonymized Private-repo-h A/B bundle independently. The owner evaluates
   usefulness, not agreement for its own sake.

Only after H0 may Pi/GLM become the cheaper first-pass Critic. Automation eligibility and a Pi
transcript adapter are separate later decisions; neither is smuggled in by one good visual answer.

## Ambitious context without ambition theatre

"AAA quality" can increase effort, but by itself it is not a quality contract. It invites broad
scope, copied spectacle and self-congratulating reports. Use it only as a bounded comparison such
as **"AAA-grade readability and feedback for this one interaction"**, paired with a visible
reference and a falsifiable gate. For the first studio run the more honest bar is **premium indie
arcade polish in one tiny slice**.

The repo-local, owner-promoted context set should be small and strong:

- **Product promise:** the first-minute fantasy, target player, emotional verb and what must be
  understandable without explanation.
- **Quality bar:** "functional but generic is a failure"; cut systems before cutting clarity,
  feel or presentation; three to five observable bars rather than a prestige adjective.
- **Creative direction:** a few positive references, explicit negative references, palette,
  silhouettes, motion/weight and the one visual hierarchy.
- **Game feel:** input response, timing, anticipation, impact, camera, particles/audio where in
  scope, plus named states to capture.
- **UI/readability:** first glance order, contrast, scale, state/health/turn information and
  controller/mouse constraints.
- **Domain mechanics:** only the artillery, terrain, projectile and turn invariants needed by the
  current slice.
- **Capture and critique:** named shots/short clips, seeds, viewport/frame facts, blind A/B schema
  and owner disposition.
- **Role overlays:** Director protects coherence; Builder changes one owned surface; Critic is
  fresh/read-only and must name evidence, uncertainty and the smallest experiment.

These are pointers into tracked target-repo sources, not copied prose in Fleet. Before they are
delivered to task lanes, Fleet must close the known foreign-dispatch bug and receipt the exact
repo-local pointers. A new-repo initializer may create the small files and commands, but only an
owner decision promotes their content.

## Next product calibration: a small turn-based artillery game

This is a genre probe, not a Worms clone: no copied name, characters, art, levels or distinctive
content. The working title and engine remain open until the Creative and Technical directions
have met.

The first production-capable slice is intentionally one arena:

- two teams/characters and local alternating turns;
- aim, power and fire with one immediately readable projectile arc;
- destructible terrain because it is the genre's one high-value technical/visual risk;
- at most two meaningfully different weapons and one light environmental modifier such as wind;
- health, turn state, impact feedback, win, immediate rematch;
- no online multiplayer, AI campaign, inventory, progression, multiple maps or asset volume.

Quality gates precede breadth:

1. **Promise + static direction:** two small visual/mood proposals, target bar and negative
   examples; owner promotes one before engine selection.
2. **Risk spike:** destructible terrain, collision and projectile path in one ugly throwaway
   scene; it proves feasibility only and contributes no visual direction.
3. **Named-shot scene:** one composed arena view and HUD at the promoted quality bar; actual image
   review before the whole round exists.
4. **One-turn feel:** aim -> anticipation -> fire -> impact -> terrain/health response. If that
   interaction is not satisfying, adjust or stop before adding systems.
5. **Tiny match:** a complete local match and rematch, then blind visual/feel review and owner
   tasting. Only a clear go licenses expansion.

The MAIN chooses the stack after Gates 1 and 2, with reversibility, browser capture and the target
quality in view. Three.js is allowed, not preselected; a 2D Canvas/WebGL/game framework may be the
better answer. The choice must explain which quality/risk fact it serves, not merely which library
the model knows.

## Durable development, not a stream of throwaway demos

A vertical slice means the first small **production-capable** version on the real stack, not a
disposable prototype. Work should accumulate in:

- a small reusable studio kit (build/run, input, audio, settings, persistence, debugging and asset
  pipeline where appropriate);
- a deliberately simple product architecture containing only what this product needs;
- reversible experiments that are either integrated, refined or removed after tasting.

For an initial web game, the current hypothesis is a simple established browser stack plus selected
open libraries, not a custom engine or premature universal framework. Three.js is one candidate,
not a precondition. The exact renderer, libraries and target platform remain product decisions and
must be justified against the promoted direction and the smallest technical risk spike.

Fleet must also be able to say that continued polishing is not earning clarity. After bounded,
explicit iterations it should present choices: improve the current core, simplify it, reuse the
working pieces in a better direction, or stop. Changing direction must preserve reusable code,
assets, decisions and lessons rather than resetting the studio to zero.

## Anti-slop contract

Nearly autonomous production is not maximum output. Avoid AI slop by construction:

- few strong product/gameplay pillars before content volume;
- one coherent creative direction and explicit negative examples;
- curation, rejection and deletion are normal production acts;
- no bulk asset or feature generation merely to occupy parallel agents;
- integrate and taste small real slices before widening scope;
- preserve human judgment at identity, feel and commercial decision points;
- verification matches the artifact: code/build checks plus visual, audio or playtest review where
  those claims are made.

The target is self-directed refinement under taste constraints, not unattended content volume.

## Ordered roadmap, not a second queue

### Now — communication is the prerequisite

1. Keep Private-repo-h Visual Loop v0 paused after its reproducible baseline and multimodal diagnosis; do
   not spend the Builder pass yet.
2. Build the four-cut communication program above, beginning with clarification correctness and
   stopping after each land for a real counter-probe.
3. Fix foreign task dispatch so it never delivers Fleet context pointers as if they belonged to a
   target repository.

### Then — calibrate the working-circle inputs

1. Run Pi/Z.ai vision H0 and compare one read-only GLM criticism with the proven Fable path.
2. Materialize the smallest owner-promoted Product/Quality/Creative/Game-Feel/UI/Capture pointers
   in a product repo and receipt them through ContextPlan; no generic context registry.
3. Configure Director -> one Builder -> fresh Critic -> Owner as a thin profile over existing
   Programs, tasks, lanes, clarifications, events and outcomes.
4. Resume Private-repo-h only if it remains the cheapest way to validate a particular loop field. One clear
   owner preference may land the visual candidate; `indifferent` discards it.

### Then — the artillery calibration game

1. Create a fresh Program and Fable Program-MAIN with quality targets before architecture.
2. Run the five gates above; maintain serial ownership for coupled visual/game-feel surfaces.
3. Preserve prompt/pack receipts, harness/model/effort, captures, decisions, corrections and owner
   disposition as the first full Game-Studio Work Trail.
4. Only after tasting, compare against a narrow one-shot or an alternate harness run; do not spend
   a second product merely to produce a benchmark.
5. Perform the repeated Work-Trail review: unnecessary tools, detours, late questions, visual
   revisions, context actually opened and which recommendations the owner accepted.

### Adjacent Fleet corridors — real, but not prerequisites after communication

- repeatable session/work-trail analysis with propose/promote learning, never self-mutating rules;
- working-circle initialization for new repos and measured repo-local Context Packs;
- Self-Land shadow classification on existing gate/audit facts before any autonomy flip;
- calibrated harness routing across Fable/Opus, Codex/Sol, Pi/GLM and bounded Spark workers;
- Graphify as source-checked navigation where it demonstrably helps, not a mandatory source of
  architecture truth;
- private operating guidance cleanup: remove superseded tmux/watch prose and make the existing
  Event/Ack and question-to-sensor evidence ladder explicit without enlarging portable
  `AGENTS.md`;
- product selection across game, iOS, Roblox and Steam after the quality loop works on one product.

### Parked until evidence changes

- a universal custom game harness, permanent agent roster or generic Context Registry/compiler;
- autonomous task waves, a second event bus, self-promoting packs or rules;
- a full private-repo-h 3D migration, feature/content expansion or rescue roadmap;
- simultaneous production of multiple games during the first studio calibration;
- GLM as an unattended Builder, a Spark game-building sandbox, custom engine work or local-model
  infrastructure undertaken only because it is technically interesting.

## Choosing the first revenue experiment

Do not choose a platform because a studio profile already exists. Compare a small set of current
opportunities using at least:

- time to a genuinely sellable release;
- target user and concrete promise;
- reachable distribution channel and monetization clarity;
- production, review and release burden;
- how reliably agents can build and test it;
- reusable capability for follow-up products;
- owner taste, domain insight and willingness to keep iterating;
- explicit time/downside limit and a cheap simplification or exit path.

No current winner is asserted here. iOS, Roblox, Steam and web economics, policies and discovery
change; the selection requires a fresh market/store/competition research program. Optimize for the
simplest **credible money experiment**, not theoretical market size or infrastructure elegance.

## Provenance and cut line

The STARBATTLE/TokenGremlin tweet was a creative trigger, not evidence of a disclosed multi-agent
studio architecture. None of this program is presented as a finding about that project.

The former reactivation gate has been met; that licenses the one proving slice above, not the whole
roadmap. Every later layer remains propose/promote and evidence-gated. Live operational work remains
in Fleet's queue and the current product repository, never in this document.
