# Verdikt D2-bedienung — Board: Bedienung, Interaktion, Mobil

**Worker:** pi/gpt-5.6-sol  ·  **Baum:** 8767397  ·  2026-08-09
**Werkzeug-Probe:** ast-grep lief (`ast-grep 0.45.1`)  ·  rg ja

## Übersicht
| Zeile | Verdikt | Konfidenz | Größe | Einzeiler |
|---|---|---|---|---|
| `16d5e973` | bauen | hoch | M | Globale Navigation fehlt; die Palette muss Task-Texte beim Öffnen über die bestehende Route laden statt sie im Poll zu erfinden. |
| `34205199` | bauen | mittel | S | Xterm erhält weiterhin nur Vorder-/Hintergrund und damit kein bewusstes ANSI-Vokabular. |
| `5d55bcfc` | bauen | mittel | S | Die Bewegungsprämisse stimmt, aber das Done muss auch bestehende Keyframes und DOM-Einfügung korrekt behandeln. |
| `f6e085d5` | bauen | hoch | M | Mobile blendet den ganzen Brief aus, obwohl Fakten und Aktionen schon existieren. |
| `6ebb4c85` | unklar | hoch | S | Die Lücke existiert auf HEAD, doch eine Owner-Branch entfernt gerade die Gast-Konsole vollständig. |
| `5aafbee4` | bauen | mittel | M | Die Dateifläche existiert serverseitig; der nennenswerte Rest ist eine ehrlich normalisierte Hunk-Sicht. |
| `577b26fa` | unklar | hoch | M | F6 ist gelandet, hat dabei aber genau die Ablageprämisse von F7 umgedreht; der Owner-Wunsch bleibt offen. |
| `9bf62ae6` | unklar | hoch | L | Der unsent Draft ist global, nicht server-synchron; Ziel, Persistenz und eigentlicher Misfire-Guard sind trotzdem unentschieden. |
| `df5b74ba` | unklar | hoch | L | Archiv/Revive fehlt weiterhin, aber zwei ausdrücklich vorgelagerte Produktentscheide ändern den Vertrag. |
| `190e5705` | streichen | hoch | S | Das ist ausdrücklich ein manueller Messlauf vor einem möglichen Bau, kein dispatchbarer Bauauftrag. |

## Je Zeile

### `16d5e973` — bauen, Konfidenz hoch
- **Warum:** Die zwei globalen `window`-Handler reagieren nur auf Escape; eine globale Navigation oder Kommandopalette gibt es nicht. Kuerzel und Palette teilen die sicherheitsrelevante Fokusgrenze und sind deshalb als eine Client-Lane vertretbar. Eine Prämisse muss korrigiert werden: Der 2-s-Poll trägt bewusst keine Task-Texte, daher muss Textsuche beim Öffnen das vorhandene `GET /api/tasks` verwenden und darf nicht aus `tasksList` behauptet werden.
- **Beleg:** `src/client.ts:821-823`, `src/client.ts:2947-2956`; vorhandene Ziele `src/client.ts:2931-2945`, `src/client.ts:6196-6214`, `src/client.ts:7557-7596`; textloser Poll-Vertrag `server.ts:1405-1443`, `server.ts:12473-12476`; Hinweisfläche `public/index.html:114-116`.
- **Kosten, wenn nicht gebaut:** Häufige Wechsel zwischen bis zu neun sichtbaren Slots, Queue, Aktivität, Board und Composer bleiben maus- beziehungsweise touchgebunden und unterbrechen den Terminalfluss.
- **Größe:** M (eine Lane) — ein Fokuswächter, Kuerzel, Shell-Liste, bestehender On-open-Fetch und Browser-Checks; kein Serverpfad.
- **Was noch fehlt, bevor man es starten kann:** Hartes Done: Alt+1…9/Alt+Q/A/B/Enter erreichen ihre vorhandenen Ziele; Cmd/Ctrl+K öffnet eine tastaturbedienbare Liste und lädt Task-Texte höchstens einmal pro Öffnung über `GET /api/tasks`; in xterms Helper-Textarea, `input`, `textarea`, `contenteditable` und Live-Typing feuert nichts. Escape schließt nur die oberste Fläche. Der Owner bestätigt lediglich die konkrete Kuerzelbelegung gegen Browser-/OS-Belegungen.
- **Nicht geprüft:** Kein echter Browser, kein Hardware-Keyboard und keine OS-weite Alt/Cmd-Kollision; den abgeleiteten Brief sieht eine Lane nicht.

### `34205199` — bauen, Konfidenz mittel
- **Warum:** Der heutige Terminal-Konstruktor setzt tatsächlich nur `background` und `foreground`; alle ANSI-Rollen bleiben xterms Defaults. Die Änderung ist mechanisch klein und auf einen Renderparameter begrenzt, auch wenn der Wert des konkreten Farbgeschmacks beim Owner liegt.
- **Beleg:** `src/client.ts:304-312`; das vorhandene Rollen-Vokabular steht in `public/index.html:247-252`, zusätzliche wiederkehrende Cyan-/Purple-Rollen unter anderem in `public/index.html:270`, `public/index.html:1138-1141`.
- **Kosten, wenn nicht gebaut:** Die größte Inhaltsfläche benutzt für Diff, Fehler und Tool-Ausgaben weiter ein unabhängiges Farbsystem, obwohl der umgebende Client dieselben semantischen Rollen bewusst benennt.
- **Größe:** S (<1 h).
- **Was noch fehlt, bevor man es starten kann:** Hartes Done: eine typisierte `TERM_THEME`-Konstante setzt alle 16 ANSI-Felder sowie Cursor, Cursor-Kontrast und Auswahl; ein ANSI-Fixture zeigt Normal/Bright und Auswahl vor dunklem Hintergrund. Der Owner bestätigt die Bright-Abstufungen, statt dass eine Lane sechs Hausfarben zu zehn weiteren Geschmacksentscheiden hochrechnet.
- **Nicht geprüft:** Keine Sichtprüfung in xterm WebGL/Canvas und keine Kontrastmessung; die Aussage über den Gewinn ist deshalb schwächer als die Aussage über den Aufwand.

### `5d55bcfc` — bauen, Konfidenz mittel
- **Warum:** Die angeforderte Ein-Kommando-Probe bestätigt: `index.html` hat zwei Keyframes, genau eine Drawer-Transition und keine Reduced-Motion-Klausel, während Share und Landing eine besitzen. Der vorgeschlagene CSS-Schnitt braucht aber zwei Korrekturen: Die bereits laufenden `paneflash`/`workpulse` müssen ebenfalls abschaltbar werden, und eingefügte `.slot.stacked`-Rows können nicht durch eine bloße Transition vom nicht vorhandenen DOM-Zustand herüberblenden; dafür braucht es eine Eintritts-Keyframe oder einen erhaltenen DOM-Zustand.
- **Beleg:** `public/index.html:137-138`, `public/index.html:993-994`, `public/index.html:1187-1190`; Vorbilder `public/share.html:35-38`, `public/share.html:221-223`, `public/landing.html:57-58`; der Stack wird beim Öffnen neu angehängt in `src/client.ts:4582-4597`.
- **Kosten, wenn nicht gebaut:** Nutzer mit Reduced-Motion-Präferenz bekommen weiterhin die bestehenden Pane-/Arbeitsimpulse, und neu erscheinende Stapel/Overlays bleiben ohne räumliche Kausalität.
- **Größe:** S (<1 h), wenn Stack-Eintritt als CSS-Keyframe statt als unmögliche Insert-Transition geschnitten wird.
- **Was noch fehlt, bevor man es starten kann:** Hartes Done: Alle neu hinzugefügte Bewegung liegt unter `prefers-reduced-motion: no-preference`; `reduce` unterbindet zusätzlich die beiden bestehenden Animationen; Stack-Eintritt, Overlay und Shell werden an einem echten DOM-Öffnen sichtbar geprüft, und die ruhige Variante verändert weder Sichtbarkeit noch Bedienbarkeit. Keine max-height-Regel ohne einen messbaren Startzustand.
- **Nicht geprüft:** Kein Browser-Render, keine Gerätepräferenz und keine Owner-Entscheidung über die gewünschte Bewegungsmenge; gemessen wurden nur CSS und DOM-Lebenszyklus.

### `f6e085d5` — bauen, Konfidenz hoch
- **Warum:** `applyBoard()` schaltet die Board-Klasse mobil ab und `renderBoard()` kehrt mobil vor jedem Fetch zurück. Gleichzeitig existieren die mobile Row-Aktionsleiste sowie `/brief`- und `/worktrees`-Leser bereits; der tragende Schnitt ist daher Extraktion und Re-Hosting derselben Fakten, nicht eine neue Datenfläche. Die Zeile unterschätzt lediglich, dass Identity/To-land/Lanes heute überwiegend inline in einem großen Renderer stehen und erst zu gemeinsamen Buildern werden müssen.
- **Beleg:** `src/client.ts:1297-1313`, `src/client.ts:2145-2174`, Inline-Sektionen `src/client.ts:2208-2470` und `src/client.ts:2551-2678`; mobile Aktionen `src/client.ts:4868-4884`, `public/index.html:1197-1206`.
- **Kosten, wenn nicht gebaut:** Auf dem Telefon bleiben Branch, HEAD, dirty/ahead/behind und die benachbarten Lanes unsichtbar, während Land/Shelve bereits angeboten werden; der Owner entscheidet dort weiter erst nach oder neben der Aktion.
- **Größe:** M (eine Lane) — drei gemeinsame Section-Builder, mobiles Sheet, CSS und Desktop-/Phone-Render-Fixture.
- **Was noch fehlt, bevor man es starten kann:** Hartes Done: Ein Row-Tipp an Phone-Breite öffnet für genau den gewählten Slot ein Sheet mit Identity, To-land und Lanes aus denselben Buildern wie Desktop; Slotwechsel während eines Fetchs malt nie alte Fakten; Explorer/Agents/Outline bleiben draußen; Desktop-Reihenfolge und Row-Aktionen bleiben unverändert. Das Fixture muss Null-Fakt, dirty, ahead/behind und mehrere Lanes abdecken.
- **Nicht geprüft:** Kein Browser oder echtes Telefon; die Owner-Nutzung vom Telefon wurde aus dem Zeilentext gelesen, nicht aus einem Ledger. `CLAUDE.md` sieht eine Lane nicht.

### `6ebb4c85` — unklar, Konfidenz hoch
- **Warum:** Auf aktuellem HEAD ist der Befund klar: `guestSection()` wird nur hinter dem mobilen Board-Guard gerendert, obwohl `#sidetools` im Drawer erreichbar ist. Aber die bereits committe Owner-Branch `fleet/260808114656-6e86` entfernt mit `e1a9a20` die komplette Gast-Konsole, ihre Routen und ihre Skripte aus einer ausdrücklichen Produktentscheidung. Vor deren Land-/Verwerfungsentscheid wäre Mobile-Zugang entweder direkt tote Arbeit oder ein kleiner Zusatz zu `f6e085d5`.
- **Beleg:** heutige Aufrufer `src/client.ts:1731-1763`, `src/client.ts:2159-2164`, `src/client.ts:2686-2687`; mobile erreichbarer Wirt `public/index.html:1268-1280`; Gegenrichtung `e1a9a20` (Body: „die Gast-Konsole fällt ganz“, Owner-Entscheid 2026-08-08). `docs/triage/verdict-D1-sicht.md:91` benennt dieselbe Fremd-Branch ausdrücklich.
- **Größe:** S (<1 h) als marginale Ergänzung zum mobilen Sheet, falls die Gast-Konsole bleibt; sonst keine Arbeit.
- **Was noch fehlt, bevor man es starten kann:** Zuerst Owner/Host: Wird `e1a9a20` gelandet oder verworfen? Falls verworfen, mit `f6e085d5` zusammenlegen und einen maschinenweiten Guest-Einstieg definieren, der auch ohne fokussierten Slot funktioniert und `guestAbsent` respektiert.
- **Nicht geprüft:** Der serverseitige Land-Status der Fremd-Branch und ihre Konfliktlage; keine Route wurde aufgerufen. Deshalb trotz starkem Konfliktbeleg kein `streichen`: Auf diesem HEAD existiert die Konsole noch.

### `5aafbee4` — bauen, Konfidenz mittel
- **Warum:** Der Server berechnet bereits dreiwertige Dateiflächen anderer Lanes, aber nur für Analyse-/Drift-Kontexte; der Client hat keine Paaransicht. Der Hunk-Schnitt ist nennenswert, weil „gleiche Datei“ die konkrete Parallelitätsfrage nicht beantwortet. Er muss enger als die Skizze sein: beide Lanes auf einen gemeinsamen Merge-Base-Koordinatenraum normalisieren, committed plus dirty Änderungen einbeziehen und unbekannte Basis/Reads als `unknown` ausgeben.
- **Beleg:** gemeinsame Fläche `server.ts:8510-8552`; der Fix `86f99f1` trennt operative Basis von historischem `baseSha` und belegt die Dreiwertigkeit; vorhandene Dateisplitter/-renderer `src/client.ts:5161-5203`; weder `server.ts` noch `src/client.ts` enthält eine `/api/lane-overlap`-Route.
- **Kosten, wenn nicht gebaut:** Vor Freigabe einer zweiten Lane bleibt „beide nennen dieselbe Datei“ von „beide ändern dieselben Zeilen“ ununterscheidbar, sodass sichere Parallelität unnötig blockiert oder echte Hunk-Kollision übersehen wird.
- **Größe:** M (eine Lane), wenn der erste Schnitt auf zwei aktive Lanes desselben Repos begrenzt wird; ein allgemeiner Branch-Diff-Browser wäre L und gehört nicht hinein.
- **Was noch fehlt, bevor man es starten kann:** Hartes Done: Zwei aktive Lane-IDs desselben Repos liefern pro Pfad `onlyA|both|onlyB|unknown`; ein Fixture deckt disjunkte Hunks derselben Datei, überlappende Änderung, gleiche Einfügeposition, dirty/untracked und unauflösbare Basis ab. Die Route akzeptiert keine freien Repo-Pfade, mutiert kein Git und cappt Diff/Dateien. Vor Start muss festgelegt werden, ob „overlap“ Zeilen im gemeinsamen Basistext oder auch konkurrierende Einfügungen am selben Anker meint.
- **Nicht geprüft:** Kein präpariertes Git-Fixture und keine Laufzeitkostenmessung; `otherOpenLanes()` selbst wurde nicht auf einer Live-Lane aufgerufen.

### `577b26fa` — unklar, Konfidenz hoch
- **Warum:** Die harte Vorbedingung ist erfüllt: F5 ist `ed5c352`, F6 ist seit `86cecf7` gelandet. F6 entschied aber nachweislich das Gegenteil seines alten Briefs: Uploads landen nun im Worktree unter `drops/`, nicht außerhalb, und die Route verweigert Repos, die dieses Verzeichnis nicht ignorieren. F7s verbleibender Mehrwert wäre deshalb gezielte Dateierzeugung an einer Explorer-Position; dafür sind Ziel-, Überschreib-, Gitignore-/Land- und Binärsemantik sowie der weiterhin offene Owner-Wunsch nicht entschieden.
- **Beleg:** `86cecf7` samt Commit-Body; heutiger Client-Drop `src/client.ts:8150-8234`; feste Upload-Ablage und Landability-Gate `server.ts:13679-13728`; die F5-Schreibroute editiert nur vorhandene Textdateien `server.ts:13618-13675`; offene Frage `docs/work-register-2026-08-06.md:216`, alte F7-Quelle `briefs/ui-next-level-2026-08-06.md:316-320`.
- **Größe:** M (eine Lane), falls der Owner gezielte Explorer-Ablage nach F6 weiterhin will.
- **Was noch fehlt, bevor man es starten kann:** Owner bestätigt F7 nach dem realen F6-Entscheid. Danach Done neu schreiben: Ziel ist ein ausgewähltes Verzeichnis, Server baut/säubert den Dateinamen, Pfad-Escape und Symlink-Escape sind 400, Kollision ist kein stilles Überschreiben, Binärdateien nutzen die gecappte Upload-Mechanik, und die UI sagt ausdrücklich, ob die neue Datei untracked/land-blockierend oder bewusst ignoriert ist.
- **Nicht geprüft:** Keine aktuelle Owner-Antwort und kein Browser-Drop auf den Explorer; die ursprüngliche Queue/der Brief sind nicht als aktueller Produktentscheid behandelt worden.

### `9bf62ae6` — unklar, Konfidenz hoch
- **Warum:** Der Juli-Befund ist nicht durch Alter widerlegt: Der Composer ist ein einziges globales `ta`; `focusPane()` ändert nur den Placeholder, und `doSend()` leert dieses globale Feld nach dem `await`. Der Text bleibt bei einem Panewechsel zwar sichtbar — „verliert“ ist zu stark —, verliert aber seine Slot-Zuordnung und kann in den neuen Slot gesendet oder durch den alten Send gelöscht werden. Der Owner-Kommentar „schon slot/server synced“ trifft nur auf gesendete Prompt-History zu, nicht auf den unsent Draft. Trotzdem ist die Zeile nicht startklar: localStorage-Done und server-synced Owner-Kommentar widersprechen sich, Slot-Recycling ist ungeklärt, und der namensgebende Misfire-Confirm fehlt im Done.
- **Beleg:** globales Feld `src/client.ts:19-23`; Fokuswechsel ohne Draft-Swap `src/client.ts:2891-2906`; asynchrones Leeren `src/client.ts:8019-8050`; serverseitig gespeichert wird ausdrücklich die History gesendeter Prompts `src/client.ts:7896-7920`, `server.ts:12598-12601`; `armReload()` sichert nichts `src/client.ts:4920-4933`. `c366c66` klassifizierte Item 11 am 2026-08-07 nach Baumabgleich als offen; seitdem zeigt die relevante Historie keinen Draft-Bau.
- **Größe:** L (mehrere Lanes / erst Entscheidung) für server-synchrone Drafts samt Identität/Restart; M für bewusst gerätelokale Drafts.
- **Was noch fehlt, bevor man es starten kann:** Owner entscheidet (1) pro Gerät oder server-synchron, (2) Identität/Lebenszeit bei Slot-Kill und Recycling und (3) ob der eigentliche zweistufige Misfire-Guard aus dem Juli-Item mitgebaut wird. Erst dann ein hartes Race-Done für Send→Slotwechsel→Tippen, offenen Auto-Dialog, History-Cycle und Reload; ein Draft darf nie anhand bloßer Slotnummer an den nächsten Occupant vererbt werden.
- **Nicht geprüft:** Kein Browser-Race und kein aktueller Owner-Dialog außerhalb des im Batch kopierten Kommentars; `fleet.json` und die Prompt-Ledger sieht eine Lane nicht.

### `df5b74ba` — unklar, Konfidenz hoch
- **Warum:** Auch diese Attic-Herkunft ist als Statusquelle alt, ihr technischer Befund wurde aber am 2026-08-07 in `c366c66` gegen den Baum geprüft und als offen in genau diese Queue-Zeile überführt. Heute löscht `killSlot()` weiterhin cwd/Label/History/sessionId, und `ensureSlot()` startet bei fehlendem Transcript bewusst frisch. Die Verbesserung wäre nennenswert, ist aber nicht startbar: Retention sowie die Semantik von `sessionId:null` sind im Text ausdrücklich Owner-Entscheide; ein Fresh-Start-Fallback widerspricht zudem dem Done „niemals still frisch“ nicht, braucht aber eine eigene sichtbare Handlung.
- **Beleg:** Resume/Fresh-Abzweig `server.ts:2830-2862`; Teardown `server.ts:3072-3113`; Nulltreffer für die vorgeschlagenen Symbole `archiveSlot` und `revive` in `server.ts`; Baumabgleich und Überführung `c366c66`; offene Fragen im historischen Beleg `docs/attic/backlog-2026-07.md:382-421`.
- **Größe:** L (mehrere Lanes / erst Entscheidung) — persistente Datei mit eigener Write-Chain, Kill-Integration, sichere Revive-Route, UI und Restart-/Security-Fixtures.
- **Was noch fehlt, bevor man es starten kann:** Owner entscheidet Retention (Zahl/Alter), Verhalten für nicht resumierbare Harnesses und `sessionId:null`, sowie ob Kill standardmäßig archiviert oder eine getrennte „archive session“-Geste bekommt. Danach Done je sechs Edge-Cases, einschließlich Doppel-Revive, gelöschtem cwd/Transcript und Restart-Race; kein Pfad darf eine frische Session als Revive ausgeben.
- **Nicht geprüft:** Keine echten Transcript-Dateien, keine Restart-Probe und keine Archivpopulation; die Ledger sieht eine Lane nicht. Der Befund stützt sich auf Code und Commit-Body, nicht auf die Juli-Zählung „3 von 11“.

### `190e5705` — streichen, Konfidenz hoch
- **Warum:** **Belegtyp (c), widerlegt als Bauauftrag:** Die maßgebliche Empfehlung sagt ausdrücklich „Den Agenten heute noch nicht bauen“, verlangt zuerst einen zweiten Lauf von Hand und beschreibt R3 als read-only/manuell. Eine `kind=lane`-Zeile würde gerade einen Agent-Bau beziehungsweise eine Agent-Session dispatchen und widerspricht damit ihrem eigenen Messdesign. Der manuelle Lauf kann sinnvoll sein, gehört aber als Owner-Akt außerhalb der Bauqueue erhalten.
- **Beleg:** `briefs/mitarbeiter-2026-08-07.md:296-314`, `briefs/mitarbeiter-2026-08-07.md:370-383`; der kopierte Auftrag selbst sagt „von Hand“ und „kein Code“ in `docs/triage/batch-D2-bedienung.md:226-236`. Ein `briefs/mitarbeiter-lauf2-*.md` existiert im heutigen Baum nicht, also wird nicht fälschlich „erledigt“ behauptet.
- **Größe:** S (<1 h) — aus der Bauqueue nehmen; der Owner kann den manuellen Report separat beauftragen.
- **Was noch fehlt, bevor man es starten kann:** Kein Baukriterium. Für den menschlichen Messlauf bleiben das versiegelte Maß und der Reportpfad aus R3 gültig; erst dessen Ergebnis darf eine neue Agent-Bauzeile erzeugen.
- **Nicht geprüft:** Keine Owner-Akte auf Vorschläge und kein zweiter manueller Lauf außerhalb des getrackten Baums; deshalb Belegtyp (c), nicht (a).

## Kalibrierung
- Meine drei stärksten Aussagen: `577b26fa` (die Vorbedingung ist mit `86cecf7` erfüllt, aber derselbe Commit widerlegt die zentrale Außerhalb-des-Worktrees-Prämisse); `f6e085d5` (Mobile-Guard, vorhandene Leser und vorhandene Aktionen liegen direkt im Code); `190e5705` (Quelle und Zeile nennen übereinstimmend einen menschlichen Lauf vor jedem Agent-Bau).
- Meine schwächste Aussage (hier zuerst nachprüfen): `34205199` — der technische Aufwand ist klar S, aber ob das abweichende ANSI-Vokabular heute eine nennenswerte Verbesserung ist, bleibt stärker Owner-Geschmack als bei den datengetragenen UI-Zeilen.
- Was ich gemessen vs. nur gelesen habe: Gemessen wurden HEAD/Datum, ast-grep-Version, die geforderte `rg -n 'prefers-reduced-motion|@keyframes' public/ src/`-Probe, globale Handler, Theme-/Transition-/Route-/Symboltreffer, Git-Historie seit Anlage und die Branch `fleet/260808114656-6e86`. Gelesen wurden gezielte Client-/HTML-/Serverbereiche, `86cecf7`, `86f99f1`, `c366c66`, `e1a9a20`, der alte Backlog und die fertigen Verdikte D1/C. Keine Suite, kein Browser, keine Route, kein Ledger und kein `fleet.json`.

## Batch-Ebene
- Zusammenlegungen, die ich sehe: Auf heutigem Entscheidstand keine formale `zusammenlegen mit`-Zeile. Falls `e1a9a20` verworfen wird, ist `f6e085d5` der tragende mobile Sheet-Schnitt und `6ebb4c85` fügt darin nur den maschinenweiten Guest-Einstieg samt No-slot-Fall hinzu; zwei getrennte Overlay-Frameworks wären Doppelarbeit. Batchübergreifend bleibt `f6e085d5` von D1-`b3a81fd0` getrennt: dessen Deploy/Gate-Warnung muss auch bei geschlossenem Sheet und ohne fokussierten Slot sichtbar sein (`docs/triage/verdict-D1-sicht.md:91`). D1-`3a622ea1` verändert dagegen genau den Identity-Fakt, den `f6e085d5` extrahiert; gemeinsam oder D1 zuerst bauen. D1-`f551f930` soll seine Reduced-Motion-Regel aus `5d55bcfc` beziehen, ist aber als Land-Zustand keine Teilmenge. `16d5e973` soll nach D1-`4d7aba33`/`6440c392` nur deren endgültige Activity-Öffner aufrufen. C-`6d07877f`/`8235c4bc` betreffen deklarierte Task-Dateien/Wellen; `5aafbee4` misst laufende Lane-Hunks und ist keine Zusammenlegung damit.
- Reihenfolge, falls eine Zeile eine andere voraussetzt: Für `577b26fa` sind F5 `ed5c352` und F6 `86cecf7` technisch gelandet; jetzt kommt zuerst der neue Owner-Entscheid, nicht weiterer Code. `5d55bcfc` vor oder zusammen mit D1-`f551f930`; D1-`3a622ea1` vor der Identity-Extraktion in `f6e085d5`; `6ebb4c85` wartet zwingend auf Land/Verwerfung von `e1a9a20`. `16d5e973`, `34205199` und `5aafbee4` sind semantisch unabhängig, kollidieren aber praktisch mit den vielen D1-Client-Lanes und sollten seriell laufen.
- Was diesem Batch als GANZEM fehlt: Ein gemeinsames Browser-Fixture für Desktop/Phone, Fokusgrenzen, Reduced Motion und Race-Wechsel; außerdem drei Owner-Entscheide: Gast-Konsole behalten/entfernen, F7 nach dem realen F6 noch wollen und Draft-Persistenz/Lebenszeit. Keine Zeile öffnet etwas aus `docs/work-register-2026-08-06.md` §7 wieder.
