# Verdikt E-harness — Harness, Isolation, Container, Worker-Modelle

**Worker:** pi/gpt-5.6-sol hinter sandbox-exec  ·  **Baum:** d92a1aa  ·  2026-08-09
**Werkzeug-Probe:** ast-grep 0.45.1 lief  ·  rg ja (15.1.0)

## Übersicht
| Zeile | Verdikt | Konfidenz | Größe | Einzeiler |
|---|---|---|---|---|
| `54af57d6` | bauen | mittel | L | Das app-server-Ziel lebt; zuerst nur den Read-Approval-Kern widerlegen oder bestätigen. |
| `b634236c` | bauen | mittel | L | Der Container macht aus der Wrapper-Disziplin eine Grenze; Image/Launcher vor Live-Aktivierung schneiden. |
| `d9b9b4c4` | bauen | hoch | M | Die Absenz ist am Writer bestätigt, während der Slot den Harness bis zum Terminalereignis kennt. |
| `29cd2610` | unklar | niedrig | L | Nutzen ja, aber Transport, Datenträger-Invariante und Maschinen-Grenze sind noch keine baubare Scheibe. |
| `34a12839` | streichen | hoch | S | Der Boot adoptiert tmux-Sessions, nicht verwaiste Worktrees; Re-Attach ist ein expliziter Request. |

## Je Zeile

### `54af57d6` — bauen, Konfidenz mittel
- **Warum:** Als Zielbild ist die Richtung weiterhin richtig, aber noch nicht als zweiter Slot-Typ: Der heutige Codex-Adapter erzwingt `--ask-for-approval never`, während das installierte `codex-cli 0.147.0` weiterhin einen als `[experimental]` markierten `app-server` mit `ws://`-Listener, Schema-Generator und Remote-TUI anbietet. Der erste baubare Schritt ist deshalb genau der enge Spike aus der Zeile; erst er entscheidet, ob der zentrale Mehrwert „Fleet kann einen Read verweigern“ existiert. `codex --help` nennt `cat` unter `untrusted` ausdrücklich als ohne Nachfrage vertrauenswürdig, also darf eine Approval-Anfrage bei Reads nicht angenommen werden.
- **Beleg:** `server.ts:741-756` zeigt den heutigen Pane-Adapter samt festem `never`; direkte Binärprobe: `codex --version` → `codex-cli 0.147.0`, `codex app-server --help` → `[experimental]`, `--listen` mit `stdio://|unix://|ws://IP:PORT`, und `codex --help` → `--remote <ADDR>`. Das Protokoll wurde nicht aus einer alten Queue-Zeile als fortbestehend angenommen.
- **Kosten, wenn nicht gebaut:** Fleet bleibt bei Codex ohne mechanische Read-Policy; zugleich bleibt ungeklärt, ob der vorgeschlagene Kontrollpunkt real ist oder nur bei schreibenden/ausführenden Aktionen feuert.
- **Größe:** L (mehrere Lanes / erst Entscheidung) für das Zielbild; der erste Spike ist S (<1 h).
- **Was noch fehlt, bevor man es starten kann:** Hartes Spike-Done: eine gepinnte Codex-Version fährt genau einen Turn gegen einen Canary außerhalb des Workspace; der Client protokolliert jede Approval-/Permissions-Anfrage und antwortet ablehnend. Bestanden nur, wenn der Read mechanisch scheitert und der Turn kontrolliert weiterläuft; keine Fleet-Codeänderung und kein zurückbleibender Daemon/Port. Fällt keine Anfrage an, ist das Zielbild in dieser Form widerlegt und wird nicht zum Slot-Typ hochgestuft.
- **Nicht geprüft:** Keinen app-server gestartet, kein Schema erzeugt und keinen Turn gefahren, weil das Daemon/Port/Auth-Zustand außerhalb der reinen Triage berührt hätte. Ob Approval bei Reads greift, ist ausdrücklich **nicht** gemessen.

### `b634236c` — bauen, Konfidenz mittel
- **Warum:** Als Zielbild ist die Richtung richtig: Der vorhandene Wrapper verspricht, den geerbten Fleet-Env nicht zu lesen, aber der heutige Subprocess läuft auf dem Host im Repo-cwd; ein Container kann Dateisystem- und Env-Absenz durchsetzen statt sie dem Python-Code zu überlassen. Die per-Repo-Auswahl ist bereits vorhanden, daher ist der erste Schnitt ein dediziertes minimales Worker-Image plus ein einzelnes absolutes Launcher-Executable — ohne den Live-Override schon umzulegen.
- **Beleg:** `worker-deepseek.py:4-18` beschreibt den Subprocess-Vertrag und das freiwillige Nichtlesen des Fleet-Env; `worker-deepseek.py:30-37` bindet Key, API und Modell. `server.ts:5071-5081` spawnt das gewählte Executable heute direkt mit Host-cwd und geerbtem Env; `server.ts:1305-1348` sowie `b64cd54` zeigen die bereits gebaute enge per-Repo-Naht. `2a86cec` benennt den Container-Worker ausdrücklich als noch fehlende Stufe 2. `docs/container.md:68-99` hält fest, dass Fleet Container weder erstellt noch startet, mountet oder probt — der Live-Zug bleibt ein Owner-Akt.
- **Kosten, wenn nicht gebaut:** Der DeepSeek-Worker bleibt entweder ungenutzt oder als Host-Prozess nur durch seine eigene Disziplin davon abgehalten, Repo-Dateien und geerbte Server-Geheimnisse zu lesen.
- **Größe:** L (mehrere Lanes / erst Entscheidung) bis zur sicheren Live-Aktivierung; der inerte Image-/Launcher-Schnitt ist M (eine Lane).
- **Was noch fehlt, bevor man es starten kann:** Erster Schnitt: Image und Launcher landen, aber kein `POST /api/repo-worker`. Hartes Done für einen anschließenden Owner-Lauf: derselbe argv/stdin/stdout-Vertrag aus `server.ts:5071-5081` liefert eine gültige Antwort; im Container ist nur die Key-Datei read-only sichtbar, kein Repo-Mount und kein `FLEET_*`-Env; eine Default-Deny-Egress-Sonde erreicht den gewählten DeepSeek-Endpunkt und weist einen unabhängigen Kontroll-Endpunkt ab. Erst danach separat den per-Repo-Override setzen und den vorhandenen `messageFallback:true`-Pfad bei gestopptem Container prüfen. Vorher muss der Owner den Domain-Egress-Mechanismus wählen; der vorhandene `guest-firewall.sh:12-27` ist ausdrücklich eine Internet-offene Denylist und erfüllt „alles andere zu“ nicht.
- **Nicht geprüft:** Keine Colima-/Docker-Kontexte, Images, laufenden Container, Key-Datei oder Live-Repo-Worker-Einstellung berührt. Der aktuelle Override braucht `fleet.json`, die eine Lane nicht sieht; die Maschinen- und Credential-Fakten aus der Zeile wurden daher nicht als heutiger Zustand bestätigt.

### `d9b9b4c4` — bauen, Konfidenz hoch
- **Warum:** Die behauptete Code-Absenz ist bestätigt: `LaneOutcome` trägt `model`, aber kein `harness`, und `buildLaneOutcome` schreibt nur `model`. Die offene Schnittfrage (a) ist am heutigen Code bereits beantwortet: Der Slot hält `harness` persistiert, und der Outcome wird vor `killSlot` gebaut; der Recorder kann den Wert am Terminalereignis also ehrlich lesen.
- **Beleg:** `server.ts:7673-7728` (`LaneOutcome`, `model`, kein `harness`), `server.ts:7833-7914` (`buildLaneOutcome`, Rückgabe mit `model`, kein `harness`), `server.ts:1221-1230` (persistierter Slot-Harness), `server.ts:2693-2720` (Outcome vor Teardown), `server.ts:3072-3102` (`killSlot` löscht den Harness erst danach). Auch der Land-Zettel-Writer hat kein Feld dafür: `server.ts:6959-6980`.
- **Kosten, wenn nicht gebaut:** Pi-, Codex-, Container- und Default-Lanes bleiben in der Outcome-Auswertung nicht belastbar trennbar; Nutzungs- und Abbruchraten der neuen Harnesses sind rückwirkend nicht rekonstruierbar.
- **Größe:** M (eine Lane).
- **Was noch fehlt, bevor man es starten kann:** Enges Done: Jede neu geschriebene live-lane Outcome-Zeile enthält das Feld `harness` mit dem Slotwert (`"pi"`, `"codex"`, `"container"` oder explizit `null` für den Default-Adapter); alte Zeilen und ein ohne Live-Slot gebauter Revert dürfen das Feld weiter missen. Lifecycle-Checks decken landed/killed/shelved sowie Default-vs.-benannt ab. Land-Note und `state.sh` sind getrennte Folgeschnitte, nicht Voraussetzung dieses Recorders.
- **Nicht geprüft:** Das Ledger selbst (`lane-outcomes.jsonl`) sehe ich nicht; ich beurteile nur den Code. Die historischen „177 Zeilen / 0 Treffer“ und die tatsächlichen heutigen Rows sind deshalb nicht bestätigt. `post-land-audits.jsonl` und git-notes-Bestand wurden ebenfalls nicht gezählt.

### `29cd2610` — unklar, Konfidenz niedrig
- **Warum:** Die Verbesserung ist real, aber die Zeile ist noch kein baubarer Schnitt. Der heutige Container-Harness verlangt gerade den identisch bind-gemounteten Host-Worktree, damit Host-git weiter alle Lane-Fakten und den Land-Pfad bedienen kann; ein Volume-Klon auf einer anderen Maschine braucht somit einen neuen Zustands-/Git-/Rückkanal und nicht nur einen Client-Knopf. Zudem ist „Dateien berühren nie die Host-Platte“ ungeklärt: Ein Colima-Volume liegt typischerweise in der VM-Disk auf der Dev-Maschine; ob „nicht als Host-Datei sichtbar“ oder wirklich „keine Bytes auf dem Datenträger“ gemeint ist, ändert die Architektur.
- **Beleg:** `docs/container.md:28-40` hält Host-tmux, Host-git und den Transcript-Verlust als heutigen Schnitt fest; `docs/container.md:68-99` verlangt laufenden Container plus identischen Bind-Mount; `docs/container.md:142-175` zeigt, dass die Clone-Naht weiterhin über vom Host gespiegelte Refs funktioniert. `server.ts:635-651` setzt das als `docker exec -w "$PWD"` um. Die direkte Codex-Probe bestätigt zwar weiter `--remote ws://…`, entscheidet aber weder Volume-Persistenz noch Push-Back.
- **Größe:** L (mehrere Lanes / erst Entscheidung).
- **Was noch fehlt, bevor man es starten kann:** Der Owner muss zuerst drei Invarianten wählen und abnehmbar machen: (1) Schutz gegen welchen Leser — Agent, Dev-Host-Prozesse oder auch Dev-Datenträger/Administrator; (2) autoritativer Git-Transport und Konfliktsemantik bei Netzverlust; (3) wo Fleet die git-Fakten für UI, Done-Erkennung und Land misst. Erst danach wäre der erste baubare Auftrag ein transport-only Proof zwischen einem unkritischen Main-Repo und einem Wegwerf-Volume, ohne Client-Knopf und ohne Codex.
- **Nicht geprüft:** Braucht die Main-Maschine, den dortigen Repo-/Git-Endpoint, Tailscale-/Auth-Konfiguration, Docker-/Colima-Volumes und deren Datenträgerlayout, die eine Lane nicht sieht. Nichts davon wurde berührt. Ob ein solcher Transport organisatorisch gewollt oder bereits extern vorhanden ist, kann nur der Owner/Host sagen.

### `34a12839` — streichen, Konfidenz hoch
- **Warum:** Typ (c), widerlegt: Der behauptete automatische Wiederbelebungsmechanismus existiert am heutigen Code nicht. `killSlot` löscht die Slot-Zuordnung und beendet die tmux-Session; beim Boot werden nur persistierte Slotbewohner und noch existierende tmux-Sessions übernommen, nicht freie Worktrees. Ein Orphan wird nur in der Worktree-Liste gezeigt und erst durch einen expliziten `attach`-Request wieder in einen Slot gesetzt. Der Owner-Nachtrag im Batch zieht die damalige Neustart-Zuschreibung ebenfalls zurück und ordnet die beobachteten Ghost-Slots eigenen Klicks zu.
- **Beleg:** Typ (c): `server.ts:3072-3102` (Slotzustand leeren und tmux-Session töten), `server.ts:9613-9630` (Boot-Kommentar und Zustandsrestore), `server.ts:10010-10028` (Adoption ausschließlich vorhandener `sN`-tmux-Sessions), `server.ts:12916-12962` (Orphans nur als Listenzeilen), `server.ts:12975-13022` (Re-Attach nur durch explizites `POST /api/lanes` mit `attach`).
- **Größe:** S (<1 h) — Queue-Zeile archivieren; keinen Fix dispatchen.
- **Was noch fehlt, bevor man es starten kann:** Nichts. Falls „on-disk“-Rows trotz korrekter Semantik weiter missverständlich sind, braucht es einen neuen UI-Auftrag mit aktuellem Screenshot und Done-Kriterium; das wäre nicht die hier widerlegte Boot-Wiederbelebung.
- **Nicht geprüft:** Das damalige `audit.jsonl` sehe ich nicht und habe die historischen Klicks nicht rekonstruiert; der Typ-(c)-Beleg steht unabhängig davon im heutigen Kontrollfluss.

## Kalibrierung
- Meine drei stärksten Aussagen: (1) `34a12839` ist durch Boot-/Kill-/Attach-Kontrollfluss widerlegt; (2) bei `d9b9b4c4` fehlt `harness` sowohl im Typ als auch an der Schreibstelle; (3) `54af57d6` beschreibt ein weiterhin vorhandenes, weiterhin experimentelles Binärprotokoll, dessen Read-Approval aber erst der Spike beweisen darf.
- Meine schwächste Aussage (hier zuerst nachprüfen): `29cd2610` — insbesondere, welche Bedeutung der Owner „Host-Platte“ gibt und ob ein bestehender Main-Maschinen-Gitdienst den größten Transportteil bereits löst, ist aus dieser Lane nicht feststellbar.
- Was ich gemessen vs. nur gelesen habe: Gemessen: `ast-grep 0.45.1`; `codex-cli 0.147.0` samt app-server-/remote-/Approval-Help; Netz offen (`https://example.com` → 200); Worktree schreibbar, Git-Metadaten nicht (`git commit --dry-run --allow-empty` → `fatal: Unable to create '…/index.lock': Operation not permitted`); `~/.claude` lesbar, aber nicht schreibbar (`test -w` → nein). Die unabhängige Claude-Code-Zweitmeinung scheiterte passend dazu vor dem Start an `EPERM … mkdir '~/.claude/projects/…'`. Gelesen: die genannten Code-/Doc-Bereiche und Commit-Bodies. Nicht gefahren: Suite, Serverroute, Docker/Colima, app-server-Daemon oder Live-Ledger.

## Batch-Ebene
- Zusammenlegungen, die ich sehe: Keine echte Teilmenge im Batch. `54af57d6` liefert möglicherweise einen Transportbaustein für `29cd2610`, beantwortet aber dessen Volume-/Git-/Maschinengrenze nicht. Keine der fünf IDs wird in `verdict-B-rueckkanal.md`, `verdict-C-queue.md` oder `verdict-G-notizen.md` erwähnt; die dortigen Urteile wurden nicht übernommen.
- Reihenfolge, falls eine Zeile eine andere voraussetzt: `d9b9b4c4` zuerst als Messgrundlage; dann der S-Spike aus `54af57d6`; nur bei positivem Read-Policy-Ergebnis app-server-Architektur gegen `29cd2610` bewerten. `b634236c` ist unabhängig und trennt Image/Launcher, Host-Probe und Live-Aktivierung. `34a12839` wird nicht gebaut.
- Was diesem Batch als GANZEM fehlt: Zielbilder, Spikes und Live-Operationen sind in denselben Rows vermischt. Für die zwei Maschinen-/Container-Ziele fehlen owner-eigene, nebenwirkungsarme Host-Proben und explizite Schutzinvarianten; `fleet.json`, `lane-outcomes.jsonl` und die Docker-/Netz-Wirklichkeit sind aus einer Lane keine belastbare Zustandsquelle.
